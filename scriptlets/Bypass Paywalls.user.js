// ==UserScript==
// @name         Bypass Paywalls
// @match        https://*.360dx.com/*
// @match        https://*.abqjournal.com/*
// @match        https://*.accountingtoday.com/*
// @match        https://*.acm.media/*
// @match        https://*.adage.com/*
// @match        https://*.adelaidenow.com.au/*
// @match        https://*.adweek.com/*
// @match        https://*.afr.com/*
// @match        https://*.ajc.com/*
// @match        https://*.al-monitor.com/*
// @match        https://*.al.com/*
// @match        https://*.americanbanker.com/*
// @match        https://*.amp.scmp.com/*
// @match        https://*.app.historytoday.com/*
// @match        https://*.appan.newscientist.com/*
// @match        https://*.architecturaldigest.com/*
// @match        https://*.archive.today/*
// @match        https://*.artnet.com/*
// @match        https://*.asia.nikkei.com/*
// @match        https://*.audiop.bizjournals.com/*
// @match        https://*.autocar.co.uk/*
// @match        https://*.autonews.com/*
// @match        https://*.autosport.com/*
// @match        https://*.axios.com/*
// @match        https://*.azcentral.com/*
// @match        https://*.backpacker.com/*
// @match        https://*.balkaninsight.com/*
// @match        https://*.baltimoresun.com/*
// @match        https://*.barandbench.com/*
// @match        https://*.barrons.com/*
// @match        https://*.barrons.djmedia.djservices.io/*
// @match        https://*.bendigoadvertiser.com.au/*
// @match        https://*.benefitnews.com/*
// @match        https://*.benzinga.com/*
// @match        https://*.betamtb.com/*
// @match        https://*.betternutrition.com/*
// @match        https://*.betterprogramming.pub/*
// @match        https://*.bhaskar.com/*
// @match        https://*.bicycling.com/*
// @match        https://*.billboard.com/*
// @match        https://*.bizjournals.com/*
// @match        https://*.bloomberg.com/*
// @match        https://*.bloombergadria.com/*
// @match        https://*.bnd.com/*
// @match        https://*.bonappetit.com/*
// @match        https://*.bondbuyer.com/*
// @match        https://*.bordermail.com.au/*
// @match        https://*.bostonglobe.com/*
// @match        https://*.bostonherald.com/*
// @match        https://*.brisbanetimes.com.au/*
// @match        https://*.buffalonews.com/*
// @match        https://*.business-standard.com/*
// @match        https://*.businessdailyafrica.com/*
// @match        https://*.businessdesk.co.nz/*
// @match        https://*.businessinsider.com/*
// @match        https://*.businessinsider.jp/*
// @match        https://*.businessoffashion.com/*
// @match        https://*.businesspost.ie/*
// @match        https://*.businesstimes.com.sg/*
// @match        https://*.cairnspost.com.au/*
// @match        https://*.canberratimes.com.au/*
// @match        https://*.capital.bg/*
// @match        https://*.capitalgazette.com/*
// @match        https://*.caravanmagazine.in/*
// @match        https://*.centralwesterndaily.com.au/*
// @match        https://*.charlotteobserver.com/*
// @match        https://*.chicagobusiness.com/*
// @match        https://*.chicagotribune.com/*
// @match        https://*.chronicle.com/*
// @match        https://*.cincinnati.com/*
// @match        https://*.cleaneatingmag.com/*
// @match        https://*.cleveland.com/*
// @match        https://*.climbing.com/*
// @match        https://*.cnbc.com/*
// @match        https://*.cnn.com/*
// @match        https://*.cntraveler.com/*
// @match        https://*.codesports.com.au/*
// @match        https://*.columbian.com/*
// @match        https://*.commercialappeal.com/*
// @match        https://*.computerweekly.com/*
// @match        https://*.cosmopolitan.com/*
// @match        https://*.countryliving.com/*
// @match        https://*.courant.com/*
// @match        https://*.courier-journal.com/*
// @match        https://*.couriermail.com.au/*
// @match        https://*.crainscleveland.com/*
// @match        https://*.crainsdetroit.com/*
// @match        https://*.crainsgrandrapids.com/*
// @match        https://*.crainsnewyork.com/*
// @match        https://*.crikey.com.au/*
// @match        https://*.csmonitor.com/*
// @match        https://*.ctinsider.com/*
// @match        https://*.ctpost.com/*
// @match        https://*.curbed.com/*
// @match        https://*.cyclingnews.com/*
// @match        https://*.dailyadvertiser.com.au/*
// @match        https://*.dailyherald.com/*
// @match        https://*.dailyliberal.com.au/*
// @match        https://*.dailymail.com/*
// @match        https://*.dailypress.com/*
// @match        https://*.dailyrecord.co.uk/*
// @match        https://*.dailytelegraph.com.au/*
// @match        https://*.dailywire.com/*
// @match        https://*.dallasnews.com/*
// @match        https://*.daytondailynews.com/*
// @match        https://*.decanter.com/*
// @match        https://*.defector.com/*
// @match        https://*.delish.com/*
// @match        https://*.democratandchronicle.com/*
// @match        https://*.denik.cz/*
// @match        https://*.denverpost.com/*
// @match        https://*.desmoinesregister.com/*
// @match        https://*.detroitnews.com/*
// @match        https://*.dig-in.com/*
// @match        https://*.digiday.com/*
// @match        https://*.discovermagazine.com/*
// @match        https://*.dispatch.com/*
// @match        https://*.dn.no/*
// @match        https://*.dnevnik.bg/*
// @match        https://*.dwell.com/*
// @match        https://*.e.infogram.com/*
// @match        https://*.eastbaytimes.com/*
// @match        https://*.economictimes.com/*
// @match        https://*.economictimes.indiatimes.com/*
// @match        https://*.economist.com/*
// @match        https://*.elle.com/*
// @match        https://*.elledecor.com/*
// @match        https://*.elnuevoherald.com/*
// @match        https://*.enotes.com/*
// @match        https://*.entrepreneur.com/*
// @match        https://*.epaper.indiatimes.com/*
// @match        https://*.epaper.thetimes.com/*
// @match        https://*.epicurious.com/*
// @match        https://*.epoch.org.il/*
// @match        https://*.espn.com/*
// @match        https://*.esquire.com/*
// @match        https://*.euobserver.com/*
// @match        https://*.european-rubber-journal.com/*
// @match        https://*.europower.no/*
// @match        https://*.examiner.com.au/*
// @match        https://*.express.co.uk/*
// @match        https://*.expressnews.com/*
// @match        https://*.fastcompany.com/*
// @match        https://*.fieldandstream.com/*
// @match        https://*.financial-planning.com/*
// @match        https://*.financialexpress.com/*
// @match        https://*.firstthings.com/*
// @match        https://*.fiskeribladet.no/*
// @match        https://*.fmrmagazine.com/*
// @match        https://*.fnlondon.com/*
// @match        https://*.forbes.com/*
// @match        https://*.forbes.com.au/*
// @match        https://*.forbes.ua/*
// @match        https://*.foreignaffairs.com/*
// @match        https://*.foreignpolicy.com/*
// @match        https://*.fortune.com/*
// @match        https://*.foxnews.com/*
// @match        https://*.freedium-mirror.cfd/*
// @match        https://*.freep.com/*
// @match        https://*.fresnobee.com/*
// @match        https://*.frontline.thehindu.com/*
// @match        https://*.ft.com/*
// @match        https://*.ftm.eu/*
// @match        https://*.gbnews.com/*
// @match        https://*.geelongadvertiser.com.au/*
// @match        https://*.genomeweb.com/*
// @match        https://*.gitflic.ru/*
// @match        https://*.glossy.co/*
// @match        https://*.goldcoastbulletin.com.au/*
// @match        https://*.goodhousekeeping.com/*
// @match        https://*.gq.com/*
// @match        https://*.granta.com/*
// @match        https://*.grubstreet.com/*
// @match        https://*.haaretz.co.il/*
// @match        https://*.haaretz.com/*
// @match        https://*.harpers.org/*
// @match        https://*.harpersbazaar.com/*
// @match        https://*.hbr.org/*
// @match        https://*.heraldsun.com.au/*
// @match        https://*.hilltimes.com/*
// @match        https://*.hindustantimes.com/*
// @match        https://*.historyextra.com/*
// @match        https://*.historytoday.com/*
// @match        https://*.housebeautiful.com/*
// @match        https://*.houstonchronicle.com/*
// @match        https://*.hydrogeninsight.com/*
// @match        https://*.iai.tv/*
// @match        https://*.illawarramercury.com.au/*
// @match        https://*.images.thewest.com.au/*
// @match        https://*.images2.dwell.com/*
// @match        https://*.inc.com/*
// @match        https://*.inc42.com/*
// @match        https://*.independent.ie/*
// @match        https://*.indianexpress.com/*
// @match        https://*.indiatoday.in/*
// @match        https://*.indystar.com/*
// @match        https://*.infzm.com/*
// @match        https://*.inkl.com/*
// @match        https://*.inquirer.com/*
// @match        https://*.insidehighered.com/*
// @match        https://*.insights.citeline.com/*
// @match        https://*.interestingengineering.com/*
// @match        https://*.intrafish.com/*
// @match        https://*.intrafish.no/*
// @match        https://*.investors.com/*
// @match        https://*.ipolitics.ca/*
// @match        https://*.irishexaminer.com/*
// @match        https://*.jacksonville.com/*
// @match        https://*.japantimes.co.jp/*
// @match        https://*.jgnt.co/*
// @match        https://*.jobs.reachplc.com/*
// @match        https://*.journal-news.com/*
// @match        https://*.journalnow.com/*
// @match        https://*.journalstar.com/*
// @match        https://*.jpost.com/*
// @match        https://*.jsonline.com/*
// @match        https://*.kansas.com/*
// @match        https://*.kansascity.com/*
// @match        https://*.kathimerini.gr/*
// @match        https://*.kentucky.com/*
// @match        https://*.knoxnews.com/*
// @match        https://*.kompas.id/*
// @match        https://*.kystens.no/*
// @match        https://*.latimes.com/*
// @match        https://*.lehighvalleylive.com/*
// @match        https://*.literaryreview.co.uk/*
// @match        https://*.livelaw.in/*
// @match        https://*.livemint.com/*
// @match        https://*.lrb.co.uk/*
// @match        https://*.macrobusiness.com.au/*
// @match        https://*.madison.com/*
// @match        https://*.magazine.atavist.com/*
// @match        https://*.magazine.thediplomat.com/*
// @match        https://*.manoramaonline.com/*
// @match        https://*.marketwatch.com/*
// @match        https://*.masslive.com/*
// @match        https://*.mcall.com/*
// @match        https://*.mcclatchydc.com/*
// @match        https://*.mediaconcierge.co.uk/*
// @match        https://*.medium.com/*
// @match        https://*.medscape.com/*
// @match        https://*.menshealth.com/*
// @match        https://*.mercurynews.com/*
// @match        https://*.mexiconewsdaily.com/*
// @match        https://*.miamiherald.com/*
// @match        https://*.mid-day.com/*
// @match        https://*.mlive.com/*
// @match        https://*.mnimarkets.com/*
// @match        https://*.modernhealthcare.com/*
// @match        https://*.modernretail.co/*
// @match        https://*.motorsportmagazine.com/*
// @match        https://*.mwatch.djmedia.djservices.io/*
// @match        https://*.nation.africa/*
// @match        https://*.nationalgeographic.com/*
// @match        https://*.nationalmortgagenews.com/*
// @match        https://*.nationalreview.com/*
// @match        https://*.nature.com/*
// @match        https://*.nautil.us/*
// @match        https://*.ndtvprofit.com/*
// @match        https://*.newcastleherald.com.au/*
// @match        https://*.newcriterion.com/*
// @match        https://*.newrepublic.com/*
// @match        https://*.news-press.com/*
// @match        https://*.newscientist.com/*
// @match        https://*.newsday.com/*
// @match        https://*.newslaundry.com/*
// @match        https://*.newsobserver.com/*
// @match        https://*.newstatesman.com/*
// @match        https://*.newsweek.com/*
// @match        https://*.newyorker.com/*
// @match        https://*.nhregister.com/*
// @match        https://*.niagarafallsreview.ca/*
// @match        https://*.nj.com/*
// @match        https://*.nola.com/*
// @match        https://*.northerndailyleader.com.au/*
// @match        https://*.northjersey.com/*
// @match        https://*.ntnews.com.au/*
// @match        https://*.nv.ua/*
// @match        https://*.nwitimes.com/*
// @match        https://*.nybooks.com/*
// @match        https://*.nydailynews.com/*
// @match        https://*.nymag.com/*
// @match        https://*.nypost.com/*
// @match        https://*.nypost.nypost.djservices.io/*
// @match        https://*.nysun.com/*
// @match        https://*.nytimes.com/*
// @match        https://*.nzherald.co.nz/*
// @match        https://*.observer.co.uk/*
// @match        https://*.ocregister.com/*
// @match        https://*.oklahoman.com/*
// @match        https://*.omaha.com/*
// @match        https://*.on3.com/*
// @match        https://*.oprahdaily.com/*
// @match        https://*.oregonlive.com/*
// @match        https://*.orlandosentinel.com/*
// @match        https://*.outlookbusiness.com/*
// @match        https://*.outlookindia.com/*
// @match        https://*.outsideonline.com/*
// @match        https://*.oxygenmag.com/*
// @match        https://*.palmbeachpost.com/*
// @match        https://*.pennlive.com/*
// @match        https://*.philanthropy.com/*
// @match        https://*.philonomist.com/*
// @match        https://*.pilotonline.com/*
// @match        https://*.pionline.com/*
// @match        https://*.plasticsnews.com/*
// @match        https://*.popularmechanics.com/*
// @match        https://*.precisionmedicineonline.com/*
// @match        https://*.pressenterprise.com/*
// @match        https://*.prevention.com/*
// @match        https://*.project-syndicate.org/*
// @match        https://*.puck.news/*
// @match        https://*.readmedium.com/*
// @match        https://*.rechargenews.com/*
// @match        https://*.reuters.com/*
// @match        https://*.reviewjournal.com/*
// @match        https://*.richmond.com/*
// @match        https://*.roadandtrack.com/*
// @match        https://*.rollingstone.com/*
// @match        https://*.rotowire.com/*
// @match        https://*.rubbernews.com/*
// @match        https://*.rugbypass.com/*
// @match        https://*.runnersworld.com/*
// @match        https://*.sacbee.com/*
// @match        https://*.sandiegouniontribune.com/*
// @match        https://*.scholastic.com/*
// @match        https://*.science.org/*
// @match        https://*.scientificamerican.com/*
// @match        https://*.scmp.com/*
// @match        https://*.scotsman.com/*
// @match        https://*.seattletimes.com/*
// @match        https://*.sfchronicle.com/*
// @match        https://*.sfstandard.com/*
// @match        https://*.shreveportbossieradvocate.com/*
// @match        https://*.silive.com/*
// @match        https://*.skimag.com/*
// @match        https://*.slate.com/*
// @match        https://*.slideshare.net/*
// @match        https://*.sloanreview.mit.edu/*
// @match        https://*.sltrib.com/*
// @match        https://*.smartcompany.com.au/*
// @match        https://*.smh.com.au/*
// @match        https://*.sofrep.com/*
// @match        https://*.sourcingjournal.com/*
// @match        https://*.spglobal.com/*
// @match        https://*.sportico.com/*
// @match        https://*.springfieldnewssun.com/*
// @match        https://*.standard.net.au/*
// @match        https://*.standardmedia.co.ke/*
// @match        https://*.star-telegram.com/*
// @match        https://*.staradvertiser.com/*
// @match        https://*.startribune.com/*
// @match        https://*.statesman.com/*
// @match        https://*.static.ffx.io/*
// @match        https://*.statnews.com/*
// @match        https://*.stcatharinesstandard.ca/*
// @match        https://*.stereogum.com/*
// @match        https://*.stltoday.com/*
// @match        https://*.stocknews.com/*
// @match        https://*.straitstimes.com/*
// @match        https://*.stratfor.com/*
// @match        https://*.study.com/*
// @match        https://*.stylist.co.uk/*
// @match        https://*.sun-sentinel.com/*
// @match        https://*.swarajyamag.com/*
// @match        https://*.syracuse.com/*
// @match        https://*.techinasia.com/*
// @match        https://*.techtarget.com/*
// @match        https://*.telegraph.co.uk/*
// @match        https://*.tennessean.com/*
// @match        https://*.tes.com/*
// @match        https://*.the-american-interest.com/*
// @match        https://*.the-scientist.com/*
// @match        https://*.the-tls.com/*
// @match        https://*.theadvocate.com/*
// @match        https://*.theadvocate.com.au/*
// @match        https://*.theage.com.au/*
// @match        https://*.theamericanconservative.com/*
// @match        https://*.theamericanscholar.org/*
// @match        https://*.theatlantic.com/*
// @match        https://*.theaustralian.com.au/*
// @match        https://*.thebanner.com/*
// @match        https://*.thebulletin.org/*
// @match        https://*.thechronicle.com.au/*
// @match        https://*.thecourier.com.au/*
// @match        https://*.thecut.com/*
// @match        https://*.thedailybeast.com/*
// @match        https://*.thediplomat.com/*
// @match        https://*.thedispatch.com/*
// @match        https://*.theglobeandmail.com/*
// @match        https://*.thehill.com/*
// @match        https://*.thehindu.com/*
// @match        https://*.thehindubusinessline.com/*
// @match        https://*.theinformation.com/*
// @match        https://*.thejuggernaut.com/*
// @match        https://*.thelampmagazine.com/*
// @match        https://*.thelawyer.com/*
// @match        https://*.theleaflet.in/*
// @match        https://*.thelogic.co/*
// @match        https://*.themandarin.com.au/*
// @match        https://*.themarker.com/*
// @match        https://*.themercury.com.au/*
// @match        https://*.thenewatlantis.com/*
// @match        https://*.thenewslens.com/*
// @match        https://*.thenewsminute.com/*
// @match        https://*.thenewworld.co.uk/*
// @match        https://*.thepeterboroughexaminer.com/*
// @match        https://*.thepointmag.com/*
// @match        https://*.thequint.com/*
// @match        https://*.therecord.com/*
// @match        https://*.thesaturdaypaper.com.au/*
// @match        https://*.thescottishsun.co.uk/*
// @match        https://*.thespec.com/*
// @match        https://*.thestage.co.uk/*
// @match        https://*.thestar.com/*
// @match        https://*.thestate.com/*
// @match        https://*.thesun.co.uk/*
// @match        https://*.thetimes.com/*
// @match        https://*.theweek.com/*
// @match        https://*.thewest.com.au/*
// @match        https://*.thewrap.com/*
// @match        https://*.thisismoney.co.uk/*
// @match        https://*.timeshighereducation.com/*
// @match        https://*.timesunion.com/*
// @match        https://*.tirebusiness.com/*
// @match        https://*.tomshardware.com/*
// @match        https://*.towardsdatascience.com/*
// @match        https://*.townandcountrymag.com/*
// @match        https://*.townsvillebulletin.com.au/*
// @match        https://*.tradewindsnews.com/*
// @match        https://*.trailrunnermag.com/*
// @match        https://*.tri-cityherald.com/*
// @match        https://*.triathlete.com/*
// @match        https://*.tucson.com/*
// @match        https://*.tulsaworld.com/*
// @match        https://*.twincities.com/*
// @match        https://*.unherd.com/*
// @match        https://*.upstreamonline.com/*
// @match        https://*.utech-polyurethane.com/*
// @match        https://*.uxdesign.cc/*
// @match        https://*.vanityfair.com/*
// @match        https://*.variety.com/*
// @match        https://*.vegetariantimes.com/*
// @match        https://*.vice.com/*
// @match        https://*.vikatan.com/*
// @match        https://*.vogue.co.uk/*
// @match        https://*.vogue.com/*
// @match        https://*.voguebusiness.com/*
// @match        https://*.vox.com/*
// @match        https://*.vulture.com/*
// @match        https://*.warontherocks.com/*
// @match        https://*.washingtonexaminer.com/*
// @match        https://*.washingtonpost.com/*
// @match        https://*.watoday.com.au/*
// @match        https://*.weeklytimesnow.com.au/*
// @match        https://*.wellandtribune.ca/*
// @match        https://*.westernadvocate.com.au/*
// @match        https://*.winnipegfreepress.com/*
// @match        https://*.wired.com/*
// @match        https://*.womenshealthmag.com/*
// @match        https://*.womensrunning.com/*
// @match        https://*.worldeconomics.com/*
// @match        https://*.wsj.com/*
// @match        https://*.wwd.com/*
// @match        https://*.ynet.co.il/*
// @match        https://*.yogajournal.com/*
// @match        https://*.yorkshirepost.co.uk/*
// @run-at       document-start
// @grant        none
// ==/UserScript==
// Shared helpers and site-specific handlers follow.
var func_post,
  domain,
  csDoneOnce,
  fetch_headers = {},
  mobile = window.navigator.userAgent.toLowerCase().includes('mobile'),
  cs_param = {};
function removeDOMElement(...elements) {
  for (let element of elements) element && element.remove();
}
function hideDOMElement(...elements) {
  for (let element of elements) element && (element.style = 'display:none !important;');
}
function hideDOMStyle(selector, id = 1) {
  if (!document.querySelector('head > style#ext' + id) && document.head) {
    let sheet = document.createElement('style');
    ((sheet.id = 'ext' + id),
      (sheet.innerText = selector + ' {display: none !important;}'),
      document.head.appendChild(sheet));
  }
}
function addStyle(css, id = 1) {
  if (!document.querySelector('head > style#add' + id) && document.head) {
    let sheet = document.createElement('style');
    ((sheet.id = 'add' + id), (sheet.innerText = css), document.head.appendChild(sheet));
  }
}
function waitDOMElement(selector, tagName = '', callback, multiple = !1) {
  new window.MutationObserver(function (mutations) {
    for (let mutation of mutations)
      for (let node of mutation.addedNodes)
        (tagName && node.tagName !== tagName) ||
          (node.matches(selector) && (callback(node), multiple || this.disconnect()));
  }).observe(document, { subtree: !0, childList: !0 });
}
function waitDOMAttribute(selector, tagName = '', attributeName = '', callback, multiple = !1) {
  let targetNode = document.querySelector(selector);
  targetNode &&
    new window.MutationObserver(function (mutations) {
      for (let mutation of mutations)
        mutation.target.attributes[attributeName] &&
          (callback(mutation.target), multiple || this.disconnect());
    }).observe(targetNode, { attributes: !0, attributeFilter: [attributeName] });
}
function matchDomain(domains, hostname = window.location.hostname) {
  return (
    'string' == typeof domains && (domains = [domains]),
    domains.find((domain) => hostname === domain || hostname.endsWith('.' + domain)) || !1
  );
}
function urlHost(url) {
  if (/^http/.test(url))
    try {
      return new URL(url).hostname;
    } catch (e) {}
  return url;
}
function matchUrlDomain(domains, url) {
  return matchDomain(domains, urlHost(url));
}
function makeFigure(url, caption_text, img_attrib = {}, caption_attrib = {}) {
  let elem = document.createElement('figure'),
    img = document.createElement('img');
  img.src = url;
  for (let attrib in img_attrib) img_attrib[attrib] && img.setAttribute(attrib, img_attrib[attrib]);
  if ((elem.appendChild(img), caption_text)) {
    let caption = document.createElement('figcaption');
    for (let attrib in caption_attrib)
      caption_attrib[attrib] && caption.setAttribute(attrib, caption_attrib[attrib]);
    let cap_par = document.createElement('p');
    ((cap_par.innerText = caption_text), caption.appendChild(cap_par), elem.appendChild(caption));
  }
  return elem;
}
function makeLink(url, title, style = '') {
  let a_link = document.createElement('a');
  return ((a_link.href = url), (a_link.innerText = title), style && (a_link.style = style), a_link);
}
function header_nofix(header, cond_sel = '', msg = '', url = '') {
  return !1;
}
function clearPaywall(paywall, paywall_action) {
  if (paywall)
    if (paywall_action)
      for (let elem of paywall)
        paywall_action.rm_class
          ? elem.classList.remove(paywall_action.rm_class)
          : paywall_action.rm_attrib && elem.removeAttribute(paywall_action.rm_attrib);
    else removeDOMElement(...paywall);
}
function getArticleSrc(
  url,
  url_src,
  proxy,
  base64,
  selector,
  text_fail = '',
  selector_source = selector,
  selector_archive = selector
) {
  let url_fetch = url_src || url;
  GM.xmlHttpRequest({
    method: 'GET',
    url: url_fetch,
    headers: fetch_headers,
    onload: function (response) {
      let recursive,
        html = response.responseText;
      (proxy && base64 && ((html = decode_utf8(atob(html))), (selector_source = 'body')),
        url.startsWith('https://archive.') &&
          url_fetch.includes('/https') &&
          (html.includes('<div class="TEXT-BLOCK"')
            ? ((url_src = html
                .split('<div class="TEXT-BLOCK"')[1]
                .split('</div>')[0]
                .split('href="')[1]
                .split('"')[0]),
              getArticleSrc(
                url,
                url_src,
                proxy,
                base64,
                selector,
                text_fail,
                selector_source,
                selector_archive
              ),
              (recursive = !0))
            : (html = '')),
        recursive ||
          replaceDomElementExtSrc(
            url,
            url_src,
            html,
            proxy,
            base64,
            selector,
            text_fail,
            selector_source,
            selector_archive
          ));
    },
  });
}
function getArchive(
  url,
  paywall_sel,
  paywall_action = '',
  selector,
  text_fail = '',
  selector_source = selector,
  selector_archive = selector
) {
  let url_archive =
      'https://' +
      archiveRandomDomain() +
      '/' +
      (url.includes('/#/') ? encodeURIComponent(url.split('?')[0]) : url.split(/[#\?]/)[0]),
    paywall = document.querySelectorAll(paywall_sel);
  paywall.length &&
    (clearPaywall(paywall, paywall_action),
    replaceDomElementExt(
      url_archive,
      !0,
      !1,
      selector,
      text_fail,
      selector_source,
      selector_archive
    ));
}
function getExtFetch(url, json_key = '', options = {}, callback = '', args = []) {
  GM.xmlHttpRequest({
    method: options.method || 'GET',
    url: url,
    headers: options.headers || {},
    data: options.body || '',
    onload: function (response) {
      let html = response.responseText;
      if (json_key)
        try {
          let json = JSON.parse(html);
          json && (html = getNestedKeys(json, json_key));
        } catch (err) {}
      callback(url, html, ...args);
    },
  });
}
var selector_level = !1;
function replaceDomElementExt(
  url,
  proxy,
  base64,
  selector,
  text_fail = '',
  selector_source = selector,
  selector_archive = selector
) {
  let article = document.querySelector(selector),
    archive_match = url.match(/https:\/\/archive\.\w{2}\//);
  article
    ? proxy
      ? (text_fail ||
          (archive_match
            ? (text_fail =
                'BPC > Try for full article text (no need to report issue for external site):\r\n')
            : matchUrlDomain(window.location.hostname, url) ||
              (text_fail = 'BPC > failed to load from external site:\r\n')),
        getArticleSrc(
          url,
          '',
          proxy,
          base64,
          selector,
          text_fail,
          selector_source,
          selector_archive
        ))
      : fetch(url, { headers: fetch_headers })
          .then((response) => {
            let article = document.querySelector(selector);
            response.ok
              ? response.text().then((html) => {
                  replaceDomElementExtSrc(
                    url,
                    '',
                    html,
                    !1,
                    base64,
                    selector,
                    text_fail,
                    selector_source
                  );
                })
              : replaceTextFail(url, article, proxy, text_fail);
          })
          .catch(function (err) {
            replaceTextFail(url, article, proxy, text_fail);
          })
    : archive_match && document.body && document.body.firstChild.before(archiveLink(url));
}
function getSelectorLevel(selector) {
  return (
    selector.replace(/,\s+/g, ',').match(/[>\s]+/) &&
      !selector.includes(':has(>') &&
      (selector = selector
        .replace(/,\s+/g, ',')
        .split(',')
        .map((x) => (x.match(/[>\s]+/) ? x + ', ' + x.split(/[>\s]+/).pop() : x))
        .join(', ')),
    selector
  );
}
function replaceDomElementExtSrc(
  url,
  url_src,
  html,
  proxy,
  base64,
  selector,
  text_fail = '',
  selector_source = selector,
  selector_archive = selector
) {
  let article = document.querySelector(selector),
    article_link = document.querySelector(selector_archive);
  if (html) {
    !proxy && base64 && ((html = decode_utf8(atob(html))), (selector_source = 'body'));
    let parser = new DOMParser();
    window.setTimeout(function () {
      if (url.startsWith('https://archive.') && url_src) {
        let domain_archive = url.match(/^https:\/\/(archive\.\w{2})/)[1],
          pathname = new URL(url_src).pathname;
        html = html
          .replace(
            new RegExp('https:\\/\\/' + domain_archive.replace('.', '\\.') + '\\/o\\/\\w+\\/', 'g'),
            ''
          )
          .replace(
            new RegExp('(src="|background-image:url\\(\')' + pathname.replace('/', '\\/'), 'g'),
            '$1https://' + domain_archive + pathname
          );
      }
      let doc = parser.parseFromString(html, 'text/html');
      selector_level && (selector_source = getSelectorLevel(selector_source));
      let article_new = doc.querySelector(selector_source);
      if (article_new) {
        if (article && article.parentNode) {
          if (url.startsWith('https://archive.')) {
            let arch_dom =
              selector_archive !== selector
                ? article_new.querySelector(selector_archive) ||
                  document.querySelector(selector_archive)
                : article_new;
            if (arch_dom) {
              arch_dom.firstChild && (arch_dom = arch_dom.firstChild);
              let arch_div = document.createElement('div');
              (arch_div.appendChild(archiveLink_renew(url_src)),
                arch_div.appendChild(
                  archiveLink(
                    window.location.href.split(/[#\?]/)[0],
                    'BPC > Full article text fetched from (no need to report issue for external site):\r\n'
                  )
                ),
                (arch_div.style = 'margin: 0px 0px 50px;'),
                arch_dom.before(arch_div));
            }
            let targets = article_new.querySelectorAll(
              'a[target="_blank"][href^="' + window.location.origin + '"]'
            );
            for (let elem of targets) elem.removeAttribute('target');
            removeDOMElement(...article_new.querySelectorAll('link[rel*="preload"]:not([href])'));
          }
          window.setTimeout(function () {
            article.parentNode &&
              (article.parentNode.replaceChild(article_new, article), func_post && func_post());
          }, 200);
        }
      } else
        replaceTextFail(
          url,
          article_link,
          proxy,
          text_fail.replace(':', '&nbsp;| no article content found! | :')
        );
    }, 200);
  } else
    replaceTextFail(
      url,
      article_link,
      proxy,
      url_src ? text_fail.replace(':', '&nbsp;| no article content found! | :') : text_fail
    );
}
function replaceTextFail(url, article, proxy, text_fail) {
  return !1;
}
function amp_images_replace() {
  window.setTimeout(function () {
    let amp_images = document.querySelectorAll('figure amp-img[src^="http"]');
    for (let amp_image of amp_images) {
      let elem = document.createElement('img');
      ((elem.src = amp_image.getAttribute('src')),
        (elem.alt = amp_image.getAttribute('alt')),
        (elem.style = 'width: 100%;'),
        amp_image.parentNode.replaceChild(elem, amp_image));
    }
  }, 1e3);
}
function amp_iframes_replace(weblink = !1, source = '') {
  let par,
    elem,
    amp_iframes = document.querySelectorAll(
      'amp-iframe' + (source ? '[src*="' + source + '"]' : '')
    );
  for (let amp_iframe of amp_iframes)
    weblink
      ? ((par = document.createElement('p')),
        (par.style = 'margin: 20px 0px;'),
        (elem = document.createElement('a')),
        (elem.innerText = 'Media-link'),
        elem.setAttribute('href', amp_iframe.getAttribute('src')),
        elem.setAttribute('target', '_blank'),
        par.appendChild(elem),
        amp_iframe.parentNode.replaceChild(par, amp_iframe))
      : amp_iframe.offsetHeight > 10 &&
        ((elem = document.createElement('iframe')),
        (elem.src = amp_iframe.getAttribute('src').replace(/^http:/, 'https:')),
        (elem.style = 'height: ' + amp_iframe.offsetHeight + 'px; width: 100%; border: 0px;'),
        amp_iframe.getAttribute('sandbox') && (elem.sandbox = amp_iframe.getAttribute('sandbox')),
        amp_iframe.parentNode.replaceChild(elem, amp_iframe));
}
function amp_redirect_not_loop(amphtml) {
  check_loop()
    ? header_nofix(
        (document.body && document.body.firstChild) || document.documentElement,
        '',
        'BPC > redirect to amp failed (disable amp-to-html extension/add-on or browser setting)'
      )
    : (window.location.href = amphtml.href);
}
function amp_redirect(paywall_sel, paywall_action = '', amp_url = '') {
  let paywall = document.querySelectorAll(paywall_sel),
    amphtml = document.querySelector('head > link[rel="amphtml"]');
  (!amphtml && amp_url && (amphtml = { href: amp_url }),
    paywall.length &&
      amphtml &&
      (clearPaywall(paywall, paywall_action), amp_redirect_not_loop(amphtml)));
}
function amp_unhide_subscr_section(
  amp_ads_sel = '',
  replace_iframes = !0,
  amp_iframe_link = !1,
  source = ''
) {
  removeDOMElement(...document.querySelectorAll('[subscriptions-section="content-not-granted"]'));
  let subscr_section = document.querySelectorAll('[subscriptions-section="content"]');
  for (let elem of subscr_section) elem.removeAttribute('subscriptions-section');
  (amp_ads_sel && hideDOMStyle(amp_ads_sel, 5),
    replace_iframes && amp_iframes_replace(amp_iframe_link, source));
}
function amp_unhide_access_hide(
  amp_access = '',
  amp_access_not = '',
  amp_ads_sel = '',
  replace_iframes = !0,
  amp_iframe_link = !1,
  source = ''
) {
  let access_hide = document.querySelectorAll(
    '[amp-access' +
      amp_access +
      '][amp-access-hide]:not([amp-access="error"], [amp-access^="message"], .piano)'
  );
  for (let elem of access_hide) elem.removeAttribute('amp-access-hide');
  (amp_access_not &&
    removeDOMElement(...document.querySelectorAll('[amp-access' + amp_access_not + ']')),
    amp_ads_sel && hideDOMStyle(amp_ads_sel, 6),
    replace_iframes && amp_iframes_replace(amp_iframe_link, source));
}
function ampToHtml() {
  window.setTimeout(function () {
    let canonical = document.querySelector('head > link[rel="canonical"][href]');
    canonical && (window.location.href = canonical.href);
  }, 1e3);
}
function check_loop(interval = 2e3) {
  let loop = !0,
    loop_date = Number(sessionStorage.getItem('###_loop'));
  return (
    (loop_date && Date.now() - loop_date < interval) ||
      (sessionStorage.setItem('###_loop', Date.now()), (loop = !1)),
    loop
  );
}
function refreshCurrentTab(not_loop = !0, not_loop_msg = !0) {
  not_loop && check_loop(5e3)
    ? not_loop_msg &&
      header_nofix(
        (document.body && document.body.firstChild) || document.documentElement,
        '',
        'BPC > refresh loop stopped'
      )
    : window.setTimeout(function () {
        window.location.reload(!0);
      }, 500);
}
function archiveRandomDomain() {
  return 'archive.' + ['fo', 'is', 'li', 'md', 'ph', 'vn'][randomInt(6)];
}
function archiveLink(url, text_fail = '') {
  return document.createTextNode('');
}
function archiveLink_renew(url, text_fail = '') {
  return document.createTextNode('');
}
function googleSearchToolLink(url, text_fail = '') {
  return document.createTextNode('');
}
function freediumLink(url, text_fail = '') {
  return document.createTextNode('');
}
function readMediumLink(url, text_fail = '') {
  return document.createTextNode('');
}
function externalLink(domains, ext_url_templ, url, text_fail = '') {
  return document.createTextNode('');
}
function removeClassesByPrefix(el, prefix) {
  let el_classes = el.classList;
  for (let el_class of el_classes) el_class.startsWith(prefix) && el_classes.remove(el_class);
}
function removeClassesList(list) {
  for (let class_item of list) {
    let elems = document.querySelectorAll('.' + class_item);
    for (let elem of elems) elem.classList.remove(class_item);
  }
}
function cookieExists(name) {
  return document.cookie.split(';').some(function (item) {
    return 0 === item.trim().indexOf(name + '=');
  });
}
function matchCookies(name) {
  return document.cookie
    .split(';')
    .filter((x) => x.trim().match(name))
    .map((y) => y.split('=')[0].trim());
}
function setCookie(names, value, domain = '', path = '/', days = 0, localstorage_hold = !1) {
  var max_age = 24 * days * 60 * 60;
  let ck_names = Array.isArray(names) ? names : [];
  names instanceof RegExp
    ? (ck_names = matchCookies(names))
    : 'string' == typeof names && (ck_names = [names]);
  for (let ck_name of ck_names)
    document.cookie =
      ck_name +
      '=' +
      (value || '') +
      (domain ? '; domain=' + domain : '') +
      (path ? '; path=' + path : '') +
      '; max-age=' +
      max_age;
  localstorage_hold || (window.localStorage.clear(), window.sessionStorage.clear());
}
function insert_script(func, insertAfterDom) {
  if (!document.querySelector('script#bpc_script')) {
    let script = document.createElement('script');
    (script.setAttribute('id', 'bpc_script'),
      script.appendChild(document.createTextNode('(' + func + ')();')),
      (insertAfterDom || document.body || document.head || document.documentElement).appendChild(
        script
      ));
  }
}
function getSourceJsonScript(filter, attributes = ':not([src], [type])') {
  'string' == typeof filter && (filter = new RegExp(filter));
  let scripts = document.querySelectorAll('script' + attributes);
  for (let script of scripts) if (script.text.match(filter)) return script;
  return !1;
}
function getArticleJsonScript() {
  let json_script,
    scripts = document.querySelectorAll('script[type="application/ld+json"]');
  for (let script of scripts)
    if (script.innerText.match(/"(articlebody|text)":/i)) {
      json_script = script;
      break;
    }
  return json_script;
}
function restorePugpigLink(node, art_link_sel = '') {
  let art_link = art_link_sel ? node.querySelector(art_link_sel) : node;
  art_link && (art_link.onmousedown = (x) => (window.location.href = art_link.href));
}
function restorePugpigPage() {
  let art_link_sel = 'a.pp-widget-article, a.pp-related__link';
  (document.querySelectorAll(art_link_sel).forEach((e) => restorePugpigLink(e)),
    waitDOMElement(art_link_sel, 'A', restorePugpigLink, !0),
    waitDOMElement(
      'li[class^="collection_type-"]',
      'LI',
      (node) => restorePugpigLink(node, art_link_sel),
      !0
    ),
    hideDOMStyle('section.modal'),
    document.querySelector('div.paywall') && refreshCurrentTab());
}
function getArticleQuintype() {
  let article_new,
    json_script = document.querySelector('script#static-page');
  if (json_script)
    try {
      article_new = document.createElement('div');
      let parser = new DOMParser(),
        json = JSON.parse(json_script.text),
        slug = decodeURIComponent(json.qt.data.story.slug);
      slug && !decodeURIComponent(window.location.pathname).includes(slug) && refreshCurrentTab();
      let pars = json.qt.data.story.cards;
      for (let par of pars) {
        let story_elements = par['story-elements'];
        for (let elem of story_elements) {
          let par_elem;
          if (['text', 'title'].includes(elem.type) && elem.text)
            par_elem = parser
              .parseFromString('<div style="margin: 25px 0px">' + elem.text + '</div>', 'text/html')
              .querySelector('div');
          else if ('image' === elem.type) {
            if (elem['image-s3-key']) {
              par_elem = document.createElement('figure');
              let img = document.createElement('img');
              if (
                ((img.src = 'https://media.assettype.com/' + elem['image-s3-key']),
                par_elem.appendChild(img),
                elem.title)
              ) {
                let caption = document.createElement('figcaption');
                if (elem.title.includes('</')) {
                  let doc = parser.parseFromString('<div>' + elem.title + '</div>', 'text/html');
                  caption.appendChild(doc.querySelector('div'));
                } else caption.innerText = elem.title;
                par_elem.appendChild(caption);
              }
            }
          } else
            'jsembed' === elem.type
              ? 'tweet' === elem.subtype &&
                elem.metadata &&
                elem.metadata['tweet-url'] &&
                ((par_elem = document.createElement('a')),
                (par_elem.href = par_elem.innerText = elem.metadata['tweet-url']),
                (par_elem.target = '_blank'))
              : 'youtube-video' === elem.type
                ? elem['embed-url'] &&
                  ((par_elem = document.createElement('iframe')),
                  (par_elem.src = elem['embed-url']),
                  (par_elem.style = 'width: 100%; height: 400px;'))
                : 'file' === elem.type
                  ? elem.url &&
                    elem['file-name'] &&
                    ((par_elem = document.createElement('a')),
                    (par_elem.href = elem.url),
                    (par_elem.innerText = elem['file-name']),
                    (par_elem.target = '_blank'))
                  : ['widget'].includes(elem.type);
          par_elem && article_new.appendChild(par_elem);
        }
      }
      article_new.hasChildNodes() || (article_new = '');
    } catch (err) {}
  return article_new;
}
function mediafin_init_auth() {
  (localStorage.setItem('###_auth_date', ''), localStorage.setItem('###_auth_token', ''));
}
function mediafin_reset_auth(article = '') {
  (mediafin_init_auth(),
    mediafin_get_auth(),
    article && header_nofix(article, '', 'BPC > no data yet (refresh page)'));
}
function mediafin_get_auth() {
  let ls_token,
    now_date = new Date().toISOString().split('T')[0],
    ls_date = localStorage.getItem('###_auth_date') || '';
  return ls_date && ls_date === now_date
    ? ((ls_token = localStorage.getItem('###_auth_token') || ''), ls_token)
    : (getExtFetch(
        'https://api.mediafin.be/oktaproxy/oauth/token',
        '',
        {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: atob(
            ''
          ),
        },
        function (url, data) {
          try {
            if (data) {
              mediafin_init_auth();
              let json = JSON.parse(data);
              json &&
                json.token_type &&
                json.access_token &&
                (localStorage.setItem('###_auth_date', now_date),
                localStorage.setItem('###_auth_token', json.token_type + ' ' + json.access_token));
            }
          } catch (err) {}
        }
      ),
      '');
}
function mediafin_main(url, data, article) {
  try {
    if (data) {
      let json = JSON.parse(data);
      if (json && json.content && json.content.paragraphs) {
        let embedded = json.embedded;
        if (
          embedded &&
          embedded.images &&
          embedded.images[0] &&
          embedded.images[0].aspectRatios['16/9']
        ) {
          let img_item = embedded.images[0],
            img_ratio = img_item.aspectRatios['16/9'].pop();
          if (img_ratio && img_ratio.url) {
            let lead_img = makeFigure(
              img_ratio.url,
              (img_item.caption || '') + (img_item.credit ? ' ©' + img_item.credit : ''),
              { style: 'width: 100%;' },
              { style: 'margin: 10px;' }
            );
            article.appendChild(lead_img);
          }
        }
        let parser = new DOMParser(),
          pars = json.content.paragraphs;
        for (let par of pars) {
          let elem;
          if ('text' === par.type) {
            let doc = parser.parseFromString(
                par.content.replace(/\[\[urn:issue:\d+\]\]/g, ''),
                'text/html'
              ),
              elem_type_match = par.content.match(/^<(\w+)/);
            if (elem_type_match) {
              let elem_type = elem_type_match[1];
              elem = doc.querySelector(elem_type);
            }
          } else if ('heading' === par.type) {
            let sub_type = par.subType || 'h2';
            ((elem = document.createElement(sub_type)), (elem.innerText = par.content));
          } else if ('image' === par.type) {
            if (par.urn && par.aspectRatio && embedded && embedded.images) {
              let img_item = embedded.images.find(
                (x) => x.urn === par.urn && x.aspectRatios && x.aspectRatios[par.aspectRatio]
              );
              if (img_item) {
                let img_ratio = img_item.aspectRatios[par.aspectRatio].pop();
                img_ratio &&
                  img_ratio.url &&
                  (elem = makeFigure(
                    img_ratio.url,
                    (img_item.caption || '') + (img_item.credit ? ' ©' + img_item.credit : ''),
                    {},
                    { style: 'margin: 10px;' }
                  ));
              }
            }
          } else if ('inlineStory' === par.type) {
            if (par.urn && embedded && embedded.inlinestories) {
              let story = embedded.inlinestories.find(
                (x) => x.urn === par.urn && x.callToAction && x.callToAction.url
              );
              if (story) {
                ((elem = document.createElement('div')),
                  (elem.style = 'font-weight: bold; margin: 20px 0px;'));
                let span = document.createElement('span');
                span.innerText = story.callToAction.label + ': ';
                let a_link = document.createElement('a');
                ((a_link.href = a_link.innerText = story.callToAction.url),
                  (a_link.target = '_blank'),
                  elem.append(span, a_link));
              }
            }
          } else if ('relatedArticles' === par.type) {
            if (par.urn && embedded && embedded.relatedArticles) {
              let articles = embedded.relatedArticles.find(
                (x) => x.urn === par.urn && x.articles && x.articles.length
              );
              if (articles) {
                ((elem = document.createElement('div')), (elem.style = 'margin: 10px 0px'));
                let intro = document.createElement('h2');
                ((intro.innerText = matchDomain('lecho.be') ? 'Sur ce sujet' : 'LEES MEER'),
                  elem.appendChild(intro));
                for (let art of articles.articles)
                  if (
                    art.content &&
                    art.content.main &&
                    art.content.main.title &&
                    art.paths &&
                    art.paths[0] &&
                    art.paths[0].path
                  ) {
                    let a_par = document.createElement('div');
                    a_par.style = 'font-weight: bold; margin: 10px 0px;';
                    let a_link = document.createElement('a');
                    ((a_link.href = art.paths[0].path),
                      (a_link.innerText = art.content.main.title),
                      (a_link.target = '_blank'),
                      a_par.appendChild(a_link),
                      elem.appendChild(a_par));
                  }
              }
            }
          } else
            [
              'advertisement',
              'applicationHook',
              'citation',
              'html',
              'insetbox',
              'numbercitation',
              'tweet',
            ].includes(par.type);
          elem && article.appendChild(elem);
        }
      } else mediafin_reset_auth(article);
    } else mediafin_reset_auth(article);
  } catch (err) {}
}
function filterObject(
  obj,
  filterFn,
  mapFn = function (val, key) {
    return [key, val];
  }
) {
  return Object.fromEntries(
    Object.entries(obj)
      .filter(([key, val]) => filterFn(val, key))
      .map(([key, val]) => mapFn(val, key))
  );
}
function matchKeyJson(key, keys) {
  let match = !1;
  return (
    'string' == typeof keys
      ? (match = key === keys)
      : Array.isArray(keys)
        ? (match = keys.includes(key))
        : keys instanceof RegExp && (match = keys.test(key)),
    match
  );
}
function findKeyJson(json, keys, min_val_len = 0) {
  let source = '';
  if (Array.isArray(json))
    for (let elem of json) source = source || findKeyJson(elem, keys, min_val_len);
  else if ('object' == typeof json)
    for (let elem in json) {
      let json_elem = json[elem];
      if ('string' == typeof json_elem && matchKeyJson(elem, keys)) {
        if (json_elem.length > min_val_len) return json_elem;
      } else {
        if (Array.isArray(json_elem) && json_elem.length > 1 && matchKeyJson(elem, keys))
          return json_elem;
        source = source || findKeyJson(json_elem, keys, min_val_len);
      }
    }
  return source;
}
function getNestedKeys(obj, key) {
  if (key in obj) return obj[key];
  let keys = key.split('.'),
    value = obj;
  for (let i = 0; i < keys.length && ((value = value[keys[i]]), void 0 !== value); i++);
  return value;
}
function getJsonUrlText(
  article,
  callback,
  article_id = '',
  key = '',
  url_rest = !1,
  url_slash = !1
) {
  let json_url,
    json_url_dom = document.querySelector(
      'head > link[rel="alternate"][type="application/json"][href]'
    );
  (json_url_dom && (json_url = json_url_dom.href),
    !json_url &&
      article_id &&
      (json_url = window.location.origin + '/wp-json/wp/v2/posts/' + article_id),
    url_rest
      ? (json_url = json_url.replace('/wp-json/', '/?rest_route=/'))
      : url_slash && (json_url = json_url.replace('/wp-json/', '//wp-json/')),
    json_url &&
      fetch(json_url).then((response) => {
        response.ok &&
          response.text().then((html) => {
            try {
              let json = JSON.parse(html.replace(/<script>[\S\s]+<\/script>/g, '')),
                json_text = parseHtmlEntities(
                  key ? getNestedKeys(json, key) : json.content.rendered
                );
              json_text && 'undefined' !== json_text && callback(json_text, article);
            } catch (err) {}
          });
      }));
}
function getJsonUrlAdd(json_text, article, art_options = {}) {
  let art_type = 'div',
    art_attrib = '';
  Object.keys(art_options).length &&
    (art_options.art_type && (art_type = art_options.art_type),
    art_options.art_class && (art_attrib += ' class="' + art_options.art_class + '"'),
    art_options.art_id && (art_attrib += ' id="' + art_options.art_id + '"'),
    art_options.art_style && (art_attrib += ' style="' + art_options.art_style + '"'),
    art_options.func_text && (json_text = art_options.func_text(json_text)));
  let article_new = new DOMParser()
    .parseFromString(
      '<' + art_type + art_attrib + '>' + json_text + '</' + art_type + '>',
      'text/html'
    )
    .querySelector(art_type);
  (article_new
    .querySelectorAll('iframe[allow*="picture-in-picture clipboard-write;"]')
    .forEach((e) =>
      e.setAttribute('allow', e.getAttribute('allow').replace(/[\w-]+ clipboard-write;/g, ''))
    ),
    art_options.art_append || !article.parentNode
      ? (art_options.art_hold || (article.innerHTML = ''), article.appendChild(article_new))
      : article.parentNode.replaceChild(article_new, article),
    func_post && func_post());
}
function getJsonUrl(
  paywall_sel,
  paywall_action = '',
  article_sel,
  art_options = {},
  article_id = '',
  key = '',
  url_rest = !1,
  url_slash = !1
) {
  let paywall = document.querySelectorAll(paywall_sel),
    article = document.querySelector(article_sel);
  paywall.length &&
    article &&
    (clearPaywall(paywall, paywall_action),
    getJsonUrlText(
      article,
      (json_text, article) => {
        json_text && article && getJsonUrlAdd(json_text, article, art_options);
      },
      article_id,
      key,
      url_rest,
      url_slash
    ));
}
function randomInt(max) {
  return Math.floor(Math.random() * Math.floor(max));
}
function randomIP(range_low = 0, range_high = 223) {
  let rndmIP = [];
  for (let n = 0; n < 4; n++)
    0 === n
      ? rndmIP.push(range_low + randomInt(range_high - range_low + 1))
      : rndmIP.push(randomInt(255) + 1);
  return rndmIP.join('.');
}
function pageContains(selector, text) {
  let elements = document.querySelectorAll(selector);
  return Array.prototype.filter.call(elements, function (element) {
    return RegExp(text).test(element.textContent);
  });
}
function findOverlap(a, b) {
  return 0 === b.length ? '' : a.endsWith(b) ? b : findOverlap(a, b.substring(0, b.length - 1));
}
function breakText(str, headers = !1) {
  return (
    (str = str.replace(
      /(?:^|[A-Za-z\"“”\)])(\.+|\?|!)(?=[A-ZÖÜ„”\d][A-Za-zÀ-ÿ„\d]{1,})/gm,
      '$&\n\n'
    )),
    headers && (str = str.replace(/(([a-z]{2,}|[\"“]))(?=[A-Z](?=[A-Za-zÀ-ÿ]+))/gm, '$&\n\n')),
    str
  );
}
function parseHtmlEntities(encodedString) {
  return new DOMParser()
    .parseFromString('<textarea>' + encodedString + '</textarea>', 'text/html')
    .querySelector('textarea').value;
}
function encode_utf8(str) {
  return unescape(encodeURIComponent(str));
}
function decode_utf8(str) {
  return decodeURIComponent(escape(str));
}
function ads_hide() {
  var overlay = document.querySelector('body.didomi-popup-open');
  (overlay && overlay.classList.remove('didomi-popup-open'),
    hideDOMStyle(
      'div.OUTBRAIN, div[id^="taboola-" i], div.ad-container, div[class*="-ad-container"], div[class*="_ad-container"], div.arc_ad, div[id^="poool-"]:empty, amp-ad, amp-embed[type="mgid"], amp-embed[type="outbrain"], amp-embed[type="taboola"]',
      10
    ));
}
function leaky_paywall_unhide() {
  if (
    document.querySelector(
      'head > link[href*="/leaky-paywall"], script[src*="/leaky-paywall"], div[id^="issuem-leaky-paywall-"]'
    )
  ) {
    let js_cookie = document.querySelector('script#leaky_paywall_cookie_js-js-extra');
    if (js_cookie && js_cookie.text.includes('"post_container":"')) {
      let post_sel = js_cookie.text.split('"post_container":"')[1].split('"')[0];
      if (post_sel) {
        let post = document.querySelector(post_sel);
        post && post.removeAttribute('class');
      }
    }
  }
}
!(function () {
  if (
    window.top === window.self ||
    matchDomain(['app.historytoday.com', 'appan.newscientist.com'])
  ) {
    var usa_adv_local_domains = [
      'al.com',
      'cleveland.com',
      'lehighvalleylive.com',
      'masslive.com',
      'mlive.com',
      'nj.com',
      'oregonlive.com',
      'pennlive.com',
      'silive.com',
      'syracuse.com',
    ];
    if (matchDomain(['adage.com', 'autonews.com', 'pionline.com'])) {
      function crain_main() {
        window.Fusion &&
          ((window.Fusion.globalContent._id = 0),
          (window.Fusion.globalContent.content_restrictions = {}));
      }
      window.setTimeout(function () {
        insert_script(crain_main);
      }, 100);
    } else if (matchDomain('nzherald.co.nz')) {
      function nzherald_main() {
        window.Fusion && (window.Fusion.globalContent.isPremium = !1);
      }
      (window.setTimeout(function () {
        insert_script(nzherald_main);
      }, 100),
        hideDOMStyle('#premium-toaster, div[id$="article-body-ad"]'));
    } else if (matchDomain(usa_adv_local_domains)) {
      function adv_main() {
        window.adiData && (window.adiData.entryTags = 0);
      }
      window.setTimeout(function () {
        insert_script(adv_main);
      }, 100);
    }
    window.setTimeout(function () {
      var leaky_paywall_unhide_disable;
      if (
        matchDomain('gitflic.ru') &&
        window.location.pathname.startsWith('/project/magnolia1234/bpc_uploads') &&
        document.head
      ) {
        let sheet = document.createElement('style'),
          path_short = window.location.pathname.replace('/project/magnolia1234/bpc_uploads', '');
        (path_short
          ? path_short.match(/^\/(blob|file)/) &&
            (sheet.innerText =
              'div.project-files-tree, div.project-files-list {flex: 0 0 50% !important; max-width: 50% !important;}')
          : (sheet.innerText =
              'div[data-cell-type="commit"], div[data-cell-type="date"] {display: none !important;} div[data-cell-type="filename"] {flex: 0 0 100% !important;}'),
          sheet.innerText && document.head.appendChild(sheet));
      }
      if (
        matchDomain('medium.com') ||
        matchDomain(['betterprogramming.pub', 'towardsdatascience.com']) ||
        document.querySelector('head > link[href*=".medium.com/"]')
      ) {
        let url = window.location.href,
          paywall = document.querySelector('article.meteredContent');
        if (paywall) {
          paywall.removeAttribute('class');
          let header = paywall.querySelector('h1');
          header &&
            header.before(
              externalLink(
                ['freedium-mirror.cfd', 'readmedium.com', 'archive.today'],
                'https://{domain}/{url}',
                url,
                'BPC > Try for full article text:'
              )
            );
        }
        window.setTimeout(function () {
          let banner = pageContains('div > div > p', /author made this story available to/);
          banner.length && removeDOMElement(banner[0].parentNode.parentNode);
        }, 1e3);
      } else if (window.location.hostname.match(/\.(au|nz)$/) || matchDomain(['afr.com'])) {
        if (matchDomain('afr.com')) {
          document.querySelector('div[data-testid="DefaultError"]') && refreshCurrentTab();
          let article_sel = '#endOfArticle:not(:empty)',
            article = document.querySelector(article_sel);
          (article ||
            ((article_sel = '#body-content'), (article = document.querySelector(article_sel))),
            article &&
              window.setTimeout(function () {
                let pars = article.querySelectorAll(
                    'p:not([class]), p[class*="-defaultWrapper"], figure:not(:empty)'
                  ),
                  pagination = document.querySelector('div > span#pagination-top');
                if ((pars.length && pars.length < 5) || pagination) {
                  if (pagination) removeDOMElement(pagination.parentNode);
                  else {
                    let loading = pageContains(article_sel + ' div', 'Loading...');
                    removeDOMElement(...pars, ...loading);
                  }
                  let url = window.location.href.split(/[#\?]/)[0];
                  fetch(url)
                    .then((response) => {
                      response.ok &&
                        response.text().then((html) => {
                          if (html.includes('__REDUX_STATE__='))
                            try {
                              let json = JSON.parse(
                                html
                                  .split('__REDUX_STATE__=')[1]
                                  .split('};')[0]
                                  .replace(/:undefined([,}])/g, ':0$1')
                                  .replace(/new\sMap\(\[[^{]*\]\)/g, 0) + '}'
                              );
                              if (json) {
                                let placeholders;
                                function find_item(match, p1, offset, string) {
                                  let placeholder_id = p1,
                                    result = '';
                                  if (placeholder_id && placeholders[placeholder_id]) {
                                    let item = placeholders[placeholder_id];
                                    if (item.data)
                                      if (['linkArticle', 'linkExternal'].includes(item.type))
                                        item.data.text &&
                                          (result = item.data.url
                                            ? '<a href="' +
                                              item.data.url +
                                              '"' +
                                              (item.data.newTab ? 'target="_blank"' : '') +
                                              '>' +
                                              item.data.text +
                                              '</a>'
                                            : item.data.text);
                                      else if ('image' === item.type) {
                                        if (item.data.fileName) {
                                          let credit =
                                            (item.data.credit && item.data.credit.trim()) ||
                                            item.data.source;
                                          result =
                                            '<figure><img src="https://static.ffx.io/images/w_960/' +
                                            item.data.fileName +
                                            '" style="width: 100%;"><figcaption>' +
                                            (item.data.caption ? item.data.caption : '') +
                                            (credit
                                              ? '<span style="font-weight: bold;">&nbsp;' +
                                                credit +
                                                '</span>'
                                              : '') +
                                            '</figcaption></figure>';
                                        }
                                      } else if ('omny' === item.type)
                                        item.data.src &&
                                          (result =
                                            '<embed src="' +
                                            item.data.src +
                                            '/embed" style="width:100%" frameborder="0">');
                                      else if ('youtube' === item.type)
                                        item.data.url &&
                                          (result = item.data.url.includes('watch?v=')
                                            ? '<iframe src="' +
                                              item.data.url.replace('watch?v=', 'embed/') +
                                              '" style="width: 100%; height: 400px;"></iframe>'
                                            : '<a href="' +
                                              item.data.url +
                                              '" target="_blank">' +
                                              item.data.url +
                                              '</a>');
                                      else if (['instagram', 'twitter'].includes(item.type))
                                        item.data.url &&
                                          (result =
                                            '<a href="' +
                                            item.data.url +
                                            '" target="_blank">' +
                                            item.data.url +
                                            '</a>');
                                      else if ('iframe' === item.type) {
                                        if (item.data.url) {
                                          let height = 400;
                                          (item.data.url.includes('/headshots/') && (height = 100),
                                            (result =
                                              '<iframe src="' +
                                              item.data.url +
                                              '" style="width: 100%; height: ' +
                                              height +
                                              'px; border: none;"></iframe>'));
                                        }
                                      } else
                                        [
                                          'callout',
                                          'domainListing',
                                          'quote',
                                          'relatedStory',
                                          'video',
                                        ].includes(item.type);
                                  }
                                  return result;
                                }
                                let json_text = json.page.content.asset.body;
                                if (json_text) {
                                  ((placeholders = json.page.content.asset.bodyPlaceholders),
                                    placeholders &&
                                      (json_text = json_text.replace(
                                        /<x-placeholder id="(\w+)"><\/x-placeholder>/g,
                                        find_item
                                      )));
                                  let content_new = new DOMParser()
                                    .parseFromString('<div>' + json_text + '</div>', 'text/html')
                                    .querySelector('div');
                                  if (article_sel.startsWith('#endOfArticle')) {
                                    let widget = document.querySelector(
                                      'div[class$="-wrapper"] > div#WidgetContainer'
                                    );
                                    widget && content_new.firstChild.before(widget.parentNode);
                                    let author = article.querySelector(
                                      'section[class$="-articleBylineToolsWrapper"]'
                                    );
                                    (author && content_new.firstChild.before(author),
                                      article.parentNode.replaceChild(content_new, article),
                                      window.setTimeout(function () {
                                        content_new.id = 'endOfArticle';
                                      }, 500));
                                  } else article.firstChild.before(content_new);
                                  addStyle(article_sel + ' p {margin: 20px 0px;}');
                                } else {
                                  let parser = new DOMParser(),
                                    posts = json.page.content.asset.posts;
                                  article.innerHTML = '';
                                  for (let post of posts) {
                                    let asset = post.asset;
                                    if (asset && asset.body) {
                                      let json_text = asset.body;
                                      ((placeholders = asset.bodyPlaceholders),
                                        placeholders &&
                                          (json_text = json_text.replace(
                                            /<x-placeholder id="(\w+)"><\/x-placeholder>/g,
                                            find_item
                                          )));
                                      let header,
                                        par = parser
                                          .parseFromString(
                                            '<section>' + json_text + '</section>',
                                            'text/html'
                                          )
                                          .querySelector('section');
                                      asset.headlines &&
                                        asset.headlines.headline &&
                                        ((header = document.createElement('h2')),
                                        (header.innerText = asset.headlines.headline),
                                        (header.id = post.id));
                                      let byline = document.createTextNode('');
                                      (asset.byline &&
                                        ((byline = document.createElement('p')),
                                        (byline.innerText = asset.byline),
                                        (byline.style = 'margin-bottom: 24px;')),
                                        article.append(header, byline, par));
                                    }
                                  }
                                  addStyle(
                                    article_sel + ' :is(p, h2) {margin: 20px 0px !important;}'
                                  );
                                  let key_posts =
                                    document.querySelectorAll('li > a[href*="?post="]');
                                  for (let elem of key_posts)
                                    elem.href = elem.href.replace('?post=', '#');
                                }
                              }
                            } catch (err) {}
                        });
                    })
                    .catch(function (err) {});
                }
              }, 1e3));
        } else if (matchDomain('businessdesk.co.nz'))
          if (mobile) {
            func_post = function () {
              let article = document.querySelector(article_sel);
              article &&
                ((article.style = 'width: 90%; margin: 20px;'),
                article
                  .querySelectorAll('img[loading="lazy"]')
                  .forEach((e) => (e.style = 'width: 100%;')));
            };
            let url = window.location.href,
              article_sel = 'main > section > div > div > div';
            getArchive(url, 'div.signup-box-container', '', article_sel);
          } else {
            let paywall = document.querySelector('div.paywall');
            if (paywall) {
              (paywall.classList.remove('paywall'),
                removeDOMElement(document.querySelector('div.signup-box-container')));
              let url = window.location.href.split(/[#\?]/)[0];
              fetch(url).then((response) => {
                response.ok &&
                  response.text().then((html) => {
                    let match = html.match(/:query="'([^"]+)'"/);
                    if (match) {
                      let parser = new DOMParser(),
                        src_text = breakText(parseHtmlEntities(match[1]))
                          .replace(/\n\n/g, '<br><br>')
                          .replace(/\.([^\s\d]|&)/g, '. $1'),
                        content_new = parser
                          .parseFromString('<div>' + src_text + '</div>', 'text/html')
                          .querySelector('div');
                      ((paywall.innerHTML = ''), paywall.appendChild(content_new));
                    }
                  });
              });
            }
          }
        else if (matchDomain(['crikey.com.au', 'smartcompany.com.au', 'themandarin.com.au'])) {
          let paywall_sel = 'div[id^="pmc-zephr-"]';
          if (document.querySelector(paywall_sel)) {
            let article_sel = 'div.entry-content',
              article = document.querySelector(article_sel);
            article &&
              (getJsonUrl(paywall_sel, '', article_sel + ' > p'),
              (article.style['max-height'] = 'none'));
          }
          hideDOMStyle('div.wp-block-pm-ad-placeholder-block, div[style*="linear-gradient"]');
        } else if (matchDomain('forbes.com.au'))
          (setCookie('blaize_session', '', 'forbes.com.au', '/', 0),
            getJsonUrl('div[class*="_gate"]', '', 'div.article-page__content-body'),
            removeDOMElement(
              document.querySelector('div[style*="background-image: linear-gradient"]')
            ));
        else if (matchDomain('macrobusiness.com.au')) {
          let paywall = pageContains('div > p', 'The full text of this article is available');
          if (paywall[0]) {
            let fade = document.querySelector('div.bg-gradient-to-t');
            removeDOMElement(paywall[0].parentNode, fade);
            let json_script = document.querySelector('script#__NUXT_DATA__');
            if (json_script)
              try {
                let json_text = JSON.parse(json_script.text).filter(
                  (x) => 'string' == typeof x && x.match(/(<|\\u003C)p>/)
                )[0];
                if (json_text) {
                  let content_new = new DOMParser()
                      .parseFromString('<div>' + json_text + '</div>', 'text/html')
                      .querySelector('div'),
                    article = document.querySelector('div.content');
                  article && ((article.innerHTML = ''), article.appendChild(content_new));
                } else refreshCurrentTab();
              } catch (err) {}
          }
        } else if (matchDomain('nzherald.co.nz')) hideDOMStyle('#premium-toaster');
        else if (matchDomain('thesaturdaypaper.com.au')) hideDOMStyle('div[data-ad-type]');
        else if (
          matchDomain(['brisbanetimes.com.au', 'smh.com.au', 'theage.com.au', 'watoday.com.au'])
        )
          hideDOMStyle('div[data-testid="ad"]');
        else if (
          matchDomain([
            'bendigoadvertiser.com.au',
            'bordermail.com.au',
            'canberratimes.com.au',
            'centralwesterndaily.com.au',
            'dailyadvertiser.com.au',
            'dailyliberal.com.au',
            'examiner.com.au',
            'illawarramercury.com.au',
            'newcastleherald.com.au',
            'northerndailyleader.com.au',
            'standard.net.au',
            'theadvocate.com.au',
            'thecourier.com.au',
            'westernadvocate.com.au',
          ]) ||
          document.querySelector('div#footer a[href^="https://acm.media/"]')
        ) {
          let mask = document.querySelector('div[class^="gradient-mask-"]');
          if (mask) {
            mask.removeAttribute('class');
            let div_hidden = document.querySelectorAll('div.flex-col div.hidden');
            for (let elem of div_hidden) elem.classList.remove('hidden');
          } else {
            let subscribe_truncate = document.querySelector('.subscribe-truncate');
            subscribe_truncate && subscribe_truncate.classList.remove('subscribe-truncate');
            let subscriber_hiders = document.querySelectorAll('.subscriber-hider');
            for (let subscriber_hider of subscriber_hiders)
              subscriber_hider.classList.remove('subscriber-hider');
          }
          let noscroll = document.querySelectorAll('html[style], body[style]');
          for (let elem of noscroll) elem.removeAttribute('style');
          hideDOMStyle(
            '.story-generic__iframe, div.blocker, div.transition-all, div[id^="headlessui-dialog"], .ad-placeholder, .sticky, [id*="-container"], #hindsight-ads-iframe, div.vf3-conversations-list__promo, div#tbl-next-up, iframe[data-ad-id]'
          );
        } else if (window.location.hostname.endsWith('.com.au'))
          if (
            matchDomain([
              'adelaidenow.com.au',
              'cairnspost.com.au',
              'codesports.com.au',
              'couriermail.com.au',
              'dailytelegraph.com.au',
              'geelongadvertiser.com.au',
              'goldcoastbulletin.com.au',
              'heraldsun.com.au',
              'ntnews.com.au',
              'theaustralian.com.au',
              'thechronicle.com.au',
              'themercury.com.au',
              'townsvillebulletin.com.au',
              'weeklytimesnow.com.au',
            ])
          ) {
            let url = window.location.href;
            if (
              window.location.pathname.startsWith('/subscribe/') &&
              !url.includes('/digitalprinteditions')
            ) {
              let og_url = document.querySelector('head > meta[property="og:url"][content]');
              if (og_url) {
                let og_url_obj = new URL(decodeURIComponent(og_url.content)),
                  url_new =
                    og_url_obj.origin +
                    '/' +
                    og_url_obj.pathname.replace(/^\//, '').replace(/\//g, '%2F');
                window.setTimeout(function () {
                  window.location.href = url_new;
                }, 500);
              }
            } else if (window.location.search.match(/[&\?]amp/)) {
              amp_unhide_subscr_section('[id^="ad-mrec-"]', !1);
              let figure_stretch = document.querySelectorAll('figure.stretch');
              for (let elem of figure_stretch) elem.classList.remove('stretch');
              removeDOMElement(document.querySelector('#comments-load, .comments-module'));
            } else
              (window.location.pathname.includes('/video/') &&
                document.querySelector('div.vms-premium-video') &&
                header_nofix('div.video-hub'),
                hideDOMStyle('.header_ads-container, .ad-block'));
          } else if (
            matchDomain('thewest.com.au') ||
            document.querySelector('head > link[href="https://images.thewest.com.au"]')
          ) {
            let paywall_sel =
              'div.paywall div[data-testid*="BreachScreen"], div[class*="StyledBreachWallContent"]';
            (setInterval(function () {
              let paywall = document.querySelector(paywall_sel);
              paywall &&
                (function (node) {
                  let filter = /^window\.PAGE_DATA\s?=\s?/,
                    json_script = getSourceJsonScript(filter);
                  if (json_script) {
                    let json_text = json_script.text.split(filter)[1];
                    json_text = json_text.replace(/:undefined([,}])/g, ':"undefined"$1');
                    try {
                      let json_pub,
                        json_article = JSON.parse(json_text);
                      for (let key in json_article) {
                        let json_resolution = json_article[key].data.result.resolution;
                        if (json_resolution && json_resolution.publication) {
                          json_pub = json_resolution.publication;
                          break;
                        }
                      }
                      let url_loaded,
                        json_content = [];
                      json_pub
                        ? ((json_content = json_pub.content.blocks), (url_loaded = json_pub._self))
                        : refreshCurrentTab();
                      let par_elem,
                        par_sub1,
                        par_sub2,
                        url = window.location.href;
                      (url_loaded && url.includes(url_loaded.slice(-10))) || refreshCurrentTab();
                      let par_dom = document.createElement('div');
                      for (let par of json_content) {
                        if (((par_elem = ''), 'text' === par.kind))
                          ((par_elem = document.createElement('p')),
                            (par_elem.innerText = par.text));
                        else if ('subhead' === par.kind)
                          ((par_elem = document.createElement('h2')),
                            (par_elem.innerText = par.text));
                        else if ('pull-quote' === par.kind)
                          ((par_elem = document.createElement('i')),
                            (par_elem.innerText =
                              (par.attribution ? par.attribution + ': ' : '') + par.text));
                        else if ('embed' === par.kind)
                          par.reference.includes('https://omny.fm/') ||
                          par.reference.includes('https://docdro.id/')
                            ? ((par_elem = document.createElement('embed')),
                              (par_elem.src = par.reference),
                              (par_elem.style = 'height:500px; width:100%'),
                              (par_elem.frameborder = '0'))
                            : ((par_elem = document.createElement('a')),
                              (par_elem.href = par.reference),
                              (par_elem.innerText = par.reference.split('?')[0]));
                        else if ('unordered-list' === par.kind) {
                          if (par.items) {
                            par_elem = document.createElement('ul');
                            for (let item of par.items)
                              item.text &&
                                ((par_sub1 = document.createElement('li')),
                                item.intentions[0] && item.intentions[0].href
                                  ? ((par_sub2 = document.createElement('a')),
                                    (par_sub2.href = item.intentions[0].href))
                                  : (par_sub2 = document.createElement('span')),
                                (par_sub2.innerText = item.text),
                                par_sub1.appendChild(par_sub2),
                                par_elem.appendChild(par_sub1));
                          }
                        } else if ('inline' === par.kind)
                          'image' === par.asset.kind &&
                            par.asset.original &&
                            par.asset.original.reference &&
                            (par_elem = makeFigure(
                              par.asset.original.reference,
                              par.asset.captionText,
                              { style: 'width:100%' }
                            ));
                        else if ('inline-related' === par.kind) {
                          if (((par_elem = document.createElement('p')), par.publications))
                            for (let elem of par.publications) {
                              let par_link = document.createElement('a');
                              ((par_link.href = elem._self),
                                (par_link.innerText = elem.heading),
                                par_elem.appendChild(par_link),
                                par_elem.appendChild(document.createElement('br')));
                            }
                        } else
                          ((par_elem = document.createElement('p')),
                            (par_elem.innerText = par.text));
                        par_elem && par_dom.appendChild(par_elem);
                      }
                      let content = document.querySelector('div[class*="StyledArticleContent"]');
                      content
                        ? ((content.innerHTML = ''), content.appendChild(par_dom))
                        : (par_dom.setAttribute('style', 'margin: 20px;'), node.before(par_dom));
                    } catch (err) {}
                  }
                  removeDOMElement(node);
                })(paywall);
            }, 1e3),
              hideDOMStyle('div.headerAdvertisement, div.ad-no-notice'));
          } else if (
            document.querySelector(
              'head > link[rel="dns-prefetch"][href="//static.ew.mmg.navigacloud.com"]'
            )
          ) {
            let paywall = document.querySelector('div#content-Load-message');
            if (paywall) {
              removeDOMElement(paywall);
              let lockable = document.querySelectorAll('div[id^="lockable-"]');
              for (let elem of lockable)
                (elem.removeAttribute('style'), elem.removeAttribute('id'));
              let gradient = document.querySelector('div.gradienttext');
              gradient && gradient.removeAttribute('class');
            }
          }
      } else if (
        (window.location.hostname.match(/\.(ie|uk)$/) && !matchDomain(['vogue.co.uk'])) ||
        matchDomain([
          'autosport.com',
          'dailymail.com',
          'decanter.com',
          'fnlondon.com',
          'ft.com',
          'gbnews.com',
          'granta.com',
          'iai.tv',
          'irishexaminer.com',
          'motorsportmagazine.com',
          'newstatesman.com',
          'scotsman.com',
          'tes.com',
          'the-tls.com',
          'thelawyer.com',
          'thetimes.com',
          'unherd.com',
          'worldeconomics.com',
        ])
      )
        if (matchDomain('autocar.co.uk')) {
          let paywall = document.querySelector('div.ms-block, div.register-block');
          if (paywall) {
            removeDOMElement(paywall);
            let json_script = getArticleJsonScript();
            if (json_script) {
              let json = JSON.parse(json_script.text);
              if (json) {
                let json_text = json.articleBody,
                  article = document.querySelector('div.block-node');
                if (json_text && article) {
                  article.innerHTML = '';
                  let article_new = document.createElement('p');
                  ((article_new.innerText = json_text),
                    removeDOMElement(
                      document.querySelector('div.article-section > div[style*="max-height"]')
                    ));
                  let url = window.location.href;
                  article.append(
                    article_new,
                    'Text-only > for missing media/links: ',
                    googleSearchToolLink(url)
                  );
                }
              }
            }
          }
          hideDOMStyle('div[class*="-ads-"]');
        } else if (matchDomain('autosport.com'))
          header_nofix('div.ms-article-content > p', 'div.ms-piano_article-banner');
        else if (matchDomain('businesspost.ie')) {
          function bpie_main() {
            if ($) {
              let article_id,
                article_id_dom = document.querySelector('article#article[data-uuid]');
              if (
                (article_id_dom && (article_id = article_id_dom.getAttribute('data-uuid')),
                article_id)
              ) {
                let bp_ajaxurl = 'https://www.businesspost.ie/wp-admin/admin-ajax.php',
                  data_ajax = {
                    action: 'fetch_article_content',
                    type: 'POST',
                    data: { id: article_id },
                    dataType: 'json',
                    contentType: 'application/json',
                  };
                $.ajax({
                  type: 'POST',
                  url: bp_ajaxurl,
                  data: data_ajax,
                  success: function (data) {
                    ((data = data.replace('controls style="display: none;"', 'controls')),
                      $('main article .article-body-section').html(data));
                  },
                });
              }
            } else refreshCurrentTab();
          }
          (window.setTimeout(function () {
            let article_id,
              paywall = document.querySelector('div#bp_paywall_content'),
              article_id_dom = document.querySelector('article#article[data-uuid]');
            (article_id_dom && (article_id = article_id_dom.getAttribute('data-uuid')),
              (paywall || article_id) && (removeDOMElement(paywall), insert_script(bpie_main)));
          }, 500),
            hideDOMStyle('div[id^="Inline-MPU-article-"]'));
        } else if (matchDomain('decanter.com')) {
          let paywall = document.querySelector('div[id^="react_subscriber_content_"]');
          if (paywall) {
            removeDOMElement(paywall);
            let data = document.querySelector(
              'div[data-dom-id^="react_subscriber_content_"][data-props]'
            );
            if (data)
              try {
                let json = JSON.parse(data.getAttribute('data-props'));
                if (json && json.content) {
                  let content = decode_utf8(atob(json.content)),
                    content_new = new DOMParser()
                      .parseFromString('<div>' + content + '</div>', 'text/html')
                      .querySelector('div');
                  (data.before(content_new),
                    header_nofix('div.collection-wrapper', '', 'BPC > no fix for reviews'),
                    hideDOMStyle('div.piano-container-fade'));
                }
              } catch (err) {}
          }
        } else if (matchDomain('fnlondon.com')) {
          window.location.pathname.startsWith('/amp/') && ampToHtml();
          let paywall = document.querySelector('div#cx-snippet');
          (paywall &&
            (removeDOMElement(paywall),
            window.setTimeout(function () {
              let article = document.querySelector('div[data-article-id] section');
              if (article) {
                (article.removeAttribute('class'),
                  addStyle('div[data-article-id] section p {margin: 20px 0px !important;}'));
                let article_id_dom = document.querySelector(
                  'head > meta[name="article.id"][content]'
                );
                article_id_dom &&
                  getExtFetch(
                    'https://fn.djmedia.djservices.io/apps/finnews/theaters/default-article?screen_ids=' +
                      article_id_dom.content,
                    '',
                    {
                      headers: {
                        'app-identifier': 'com.news.screens',
                        'device-type': 'phone',
                        'x-access-token':
                          'eyJ0eXAiOiJKV1QiLCJhbGciOiJSUzI1NiJ9.eyJBVVRIX1BIUkFTRV9QUk9EX0lPUyI6IlZlRXRaemRIaGF4bzhKRWpzaHlEIn0.bxWGVMaft1RN0_qWAnzoNBLx12sc0Jt4rLBaoG5n08AQTS9RibwlJlZrqUca_tm0lSCwl3Z1ehJcVepVH_YcNgRJsujt49JVFSGBO8B69zFDERS05x2RM_n0k8Jg9cyErhfsgWTsb8ObR6iRhHmw702_VcEzJrmdXwq44Bw3NgkBPOgIAZn37SA7hx__gvd8Hdxd5LLgZwxXTG1kWFW4S--vf4CxUt-uEY94m1VqaUyyMTEMvXTmktS2wReBH8mawDvBdyBDkQrPx7oaFP1zq4h-B1mQCj_wMRfR-QUls6BTpBPQUjO02FaTf-2RbHzAYn3xTpmxs0GE12iD4QBSbg',
                      },
                    },
                    fix_dowjones_fetch,
                    [article, 'div.media-layout img[src]']
                  );
              }
            }, 1e3)),
            waitDOMElement(
              'div[id^="continuous_article_"]',
              'DIV',
              function (node) {
                window.setTimeout(function () {
                  let signin_links = node.querySelectorAll(
                    'a[href^="https://www.fnlondon.com/client/login"][href*="target="]'
                  );
                  for (let elem of signin_links)
                    ((elem.href = '#'),
                      (elem.innerText = 'Open'),
                      elem.addEventListener('click', function () {
                        window.location.reload();
                      }));
                }, 500);
              },
              !0
            ),
            hideDOMStyle('div.grid__rail_top'));
        } else if (matchDomain('ft.com'))
          ((func_post = function () {
            (mobile &&
              (document
                .querySelectorAll('div[style^="z-index:"][style*=";width:"]')
                .forEach((e) => e.removeAttribute('style')),
              document
                .querySelectorAll('section[style^="column-gap:"] > figure')
                .forEach((e) => e.parentNode.removeAttribute('style')),
              document
                .querySelectorAll('div[style*="grid-template-areas"], article#site-content')
                .forEach((e) => (e.style = 'margin: 10px;')),
              document
                .querySelectorAll('aside[style], ul#onward-journey-collection > li[style]')
                .forEach((e) => e.removeAttribute('style')),
              document
                .querySelectorAll('div#o-topper [style*="color:rgb(255, 255, 255);"]')
                .forEach((e) => (e.style.color = ''))),
              hideDOMStyle('form[action*="/api/"]', 2));
            let lazy_images = document.querySelectorAll('figure > picture > img[loading="lazy"]');
            for (let elem of lazy_images)
              ((elem.style = 'width: 100%;'),
                mobile &&
                  (elem.parentNode.removeAttribute('style'),
                  elem.parentNode.parentNode.removeAttribute('style'),
                  elem.parentNode.parentNode
                    .querySelectorAll('figcaption')
                    .forEach((e) => e.removeAttribute('style'))),
                elem.src.startsWith('data:image/') &&
                  elem.getAttribute('currentsourceurl') &&
                  (elem.src = elem.getAttribute('currentsourceurl')));
            let charts = document.querySelectorAll('div[frameborder][old-src]:not([src])');
            for (let elem of charts) {
              let iframe = document.createElement('iframe');
              ((iframe.src = elem.getAttribute('old-src')),
                (iframe.style = 'width: 100%; height: 600px; border: none;'),
                elem.parentNode.replaceChild(iframe, elem));
            }
            let custom_codes = document.querySelectorAll('custom-code-component[title]');
            for (let elem of custom_codes) {
              let title = document.createElement('div'),
                title_main = document.createElement('div');
              ((title_main.innerText = elem.title),
                (title_main.style = 'font-weight: bold;'),
                title.append(
                  title_main,
                  document.createTextNode(elem.getAttribute('subtitle') || ''),
                  document.createElement('br'),
                  document.createTextNode(elem.getAttribute('credit') || '')
                ),
                elem.before(title),
                removeDOMElement(elem.querySelector('div[style*="align-items:"]')));
            }
            let videos = document.querySelectorAll(
              'div > video > source[src^="data:image/"][old-src]'
            );
            for (let elem of videos) {
              let video_new = document.createElement('video');
              ((video_new.src = elem.getAttribute('old-src')),
                video_new.setAttribute('controls', ''),
                (video_new.style = 'width: 100%;'));
              let container = elem.parentNode.parentNode;
              ((container.parentNode.style = 'margin: 20px;'),
                container.parentNode.replaceChild(video_new, container));
            }
            let tables = document.querySelectorAll('div[style]:not([id]) > table:not([id])');
            for (let elem of tables)
              (elem.parentNode.removeAttribute('style'),
                elem
                  .querySelectorAll('tr[style*="visibility:hidden"]')
                  .forEach((e) => (e.style.visibility = 'visible')));
            let title = document.querySelector('head > meta[property="og:title"][content]');
            title && (document.title = title.content);
          }),
            getArchive(
              window.location.href,
              'div#barrier-page',
              '',
              'div.n-layout__row--content',
              '',
              'div[style*="article-body"]',
              'body'
            ),
            hideDOMStyle('.js-article-ribbon, div.o-ads, pg-slot'));
        else if (matchDomain('gbnews.com'))
          hideDOMStyle(
            'div.ad--billboard, div[class^="ad--placeholder"], div.video-inbody, div.sticky-banner-wrapper, div.sidebar-ad-wrapper, div.vf3-conversations-list__promo'
          );
        else if (matchDomain('granta.com'))
          getJsonUrl('div.article-sign-up-container', '', 'div.article-excerpt');
        else if (matchDomain('iai.tv')) {
          let paywall = document.querySelector('div.article-paywall-main');
          if (paywall) {
            removeDOMElement(paywall);
            let article = document.querySelector('div > div.iai-article--content-inner');
            if (article) {
              let content_new = document.querySelector(
                'head > meta[name="twitter:description"][content]'
              );
              if (content_new) {
                let article_new = new DOMParser()
                  .parseFromString('<div>' + content_new.content + '</div>', 'text/html')
                  .querySelector('div');
                article.parentNode.replaceChild(article_new, article);
              }
            }
          }
        } else if (matchDomain('irishexaminer.com')) {
          setCookie('blaize_session', '', 'irishexaminer.com', '/', 0);
          let premium = document.querySelector('head > meta[name="isPremium"][content="true"]'),
            article = document.querySelector('article');
          if (premium && article) {
            function stripAppLinks(story) {
              story
                .querySelectorAll('a.readmore-event[href$="?type=app"]')
                .forEach((e) => (e.href = e.href.split('?')[0]));
            }
            removeDOMElement(premium);
            let url = window.location.href;
            if (window.location.search.startsWith('?type=app')) {
              let longread = document.querySelector(
                'head > meta[name="isLongRead"][content="true"]'
              );
              if (longread) {
                removeDOMElement(longread);
                let row = article.querySelector('div.row');
                row && row.append(googleSearchToolLink(url.split('?')[0]));
              } else {
                let div = document.createElement('div');
                div.style.margin = '20px';
                let home = document.createElement('a');
                ((home.innerText = window.location.hostname),
                  (home.href = 'https://' + window.location.hostname),
                  div.append(home),
                  article.before(div));
                let story = article.querySelector('story');
                (story && stripAppLinks(story), hideDOMStyle('.bbw-embed'));
              }
            } else {
              let paywall = document.querySelector('div#paywall-premium');
              if (paywall) {
                removeDOMElement(paywall, article.querySelector('style:not(:empty)'));
                let url_src = url.split(/[#\?]/)[0] + '?type=app',
                  story = article.querySelector('story');
                story
                  ? article.querySelector('.bbw-embed')
                    ? (window.location.href = url_src)
                    : fetch(url_src).then((response) => {
                        response.ok &&
                          response.text().then((html) => {
                            if (html.includes('<story')) {
                              let story_new = new DOMParser()
                                .parseFromString(html, 'text/html')
                                .querySelector('story');
                              story_new &&
                                (stripAppLinks(story_new),
                                story.parentNode.replaceChild(story_new, story));
                            }
                          });
                      })
                  : (window.location.href = url_src);
              }
            }
          }
        } else if (matchDomain('literaryreview.co.uk'))
          getJsonUrl('p.subscribe-for-more', '', 'div#_articlereview');
        else if (matchDomain('lrb.co.uk')) {
          let lrb_keys = Object.keys(window.localStorage).filter((x) => x.match(/^lrb_/));
          for (let item of lrb_keys) window.localStorage.removeItem(item);
        } else if (matchDomain('motorsportmagazine.com'))
          (getJsonUrl('aside.paywall', '', 'div[data-behaviour="post-content"]'),
            removeDOMElement(
              document.querySelector('div[data-behaviour="react-paywall-threshold"]')
            ),
            hideDOMStyle('aside.ad-space'));
        else if (matchDomain('newstatesman.com')) hideDOMStyle('div.ad');
        else if (matchDomain('observer.co.uk'))
          hideDOMStyle('div._minH-50px:has(> div#landing-top)');
        else if (matchDomain('stylist.co.uk')) {
          let paywall = document.querySelector('div[data-testid="paywall-component"]');
          if (paywall) {
            removeDOMElement(paywall);
            let paywall_inline = document.querySelector('div.paywall--inline');
            paywall_inline && paywall_inline.removeAttribute('class');
            let json_script = document.querySelector('script#__NEXT_DATA__');
            if (json_script)
              try {
                let json = JSON.parse(json_script.text);
                if (json.props.pageProps.data.post.acf.widgets) {
                  let url_next = json.props.pageProps.data.post.id;
                  url_next && !window.location.pathname.endsWith(url_next) && refreshCurrentTab();
                  let pars = json.props.pageProps.data.post.acf.widgets,
                    first_par = document.querySelector('main div[data-column="true"] > p');
                  if (first_par) {
                    let article = first_par.parentNode;
                    if (
                      (removeDOMElement(...article.querySelectorAll('div > p:not([class])')),
                      article)
                    ) {
                      let parser = new DOMParser();
                      for (let par of pars) {
                        let elem = document.createElement('p');
                        if (par.paragraph) {
                          let content = par.paragraph;
                          elem = parser
                            .parseFromString('<div>' + content + '</div>', 'text/html')
                            .querySelector('div');
                        } else if ('heading' === par.acf_fc_layout)
                          par.text && elem.appendChild(document.createTextNode(par.text));
                        else if (['image', 'interactive_image'].includes(par.acf_fc_layout)) {
                          let image_array = [];
                          par.image
                            ? (image_array = [par.image])
                            : par.image_collection && (image_array = par.image_collection);
                          for (let img_elem of image_array) {
                            let figure = makeFigure(
                              img_elem.url,
                              img_elem.caption
                                ? img_elem.caption + ' ' + img_elem.description
                                : img_elem.alt,
                              { alt: img_elem.alt, style: 'width: 95%;' }
                            );
                            elem.appendChild(figure);
                          }
                        } else if ('listicle' === par.acf_fc_layout) {
                          let ul = document.createElement('ul');
                          for (let sub_item of par.item) {
                            let li = document.createElement('li');
                            if (sub_item.url) {
                              let par_link = document.createElement('a');
                              ((par_link.href = sub_item.url),
                                (par_link.innerText = sub_item.title),
                                (par_link.target = '_blank'),
                                li.appendChild(par_link));
                            } else li.innerText = sub_item.title;
                            if (sub_item.paragraph) {
                              let content = sub_item.paragraph,
                                par_elem = parser
                                  .parseFromString('<div>' + content + '</div>', 'text/html')
                                  .querySelector('div');
                              li.appendChild(par_elem);
                            }
                            if (sub_item.image) {
                              let img = document.createElement('img');
                              ((img.src = sub_item.image.url),
                                (img.alt = sub_item.image.alt),
                                (img.style = 'width: 95%;'),
                                li.appendChild(img),
                                li.appendChild(document.createElement('br')));
                            }
                            ((li.style = 'font-size: 20px; margin: 20px 0px;'), ul.appendChild(li));
                          }
                          elem.appendChild(ul);
                        } else if (par.embed_link) {
                          let par_link = document.createElement('a');
                          ((par_link.href = par.embed_link),
                            (par_link.innerText = 'Embedded link: ' + par.embed_link),
                            (par_link.target = '_blank'),
                            elem.appendChild(par_link));
                        } else if ('divider' === par.acf_fc_layout)
                          elem.appendChild(document.createElement('hr'));
                        else if ('related_articles' === par.acf_fc_layout) {
                          if (par.posts)
                            for (let post of par.posts)
                              if (post.link && post.title.rendered) {
                                let par_link = document.createElement('a');
                                ((par_link.href = post.link),
                                  (par_link.innerText =
                                    'You may also like: ' + post.title.rendered),
                                  elem.appendChild(par_link),
                                  elem.appendChild(document.createElement('br')));
                              }
                        } else ['newsletter_signup', 'pull-quote'].includes(par.acf_fc_layout);
                        elem.hasChildNodes() &&
                          ((elem.style =
                            'font-family: "Source Serif Pro"; font-size: 20px; line-height: 34px;'),
                          article.appendChild(elem));
                      }
                    }
                  }
                }
              } catch (err) {}
          }
        } else if (matchDomain('telegraph.co.uk'))
          hideDOMStyle('[class^="subwall"], .advert, .commercial-unit');
        else if (matchDomain('tes.com')) {
          let paywall = document.querySelector('div.tg-paywall-message');
          if (paywall) {
            removeDOMElement(paywall);
            let overlay = document.querySelector('div.tg-paywall-body-overlay');
            overlay && overlay.removeAttribute('class');
          }
          removeDOMElement(document.querySelector('div.js-paywall-info'));
        } else if (matchDomain('the-tls.com'))
          (getJsonUrl('div.tls-single-article__closed-paywall', '', 'div.tls-article-body', {
            art_class: 'tls-article-body',
          }),
            hideDOMStyle(
              'div.tls-single-article__closed-paywall-wrapper, div[class*="tls-single-article__ad-slot"]'
            ));
        else if (matchDomain('thelawyer.com'))
          if (window.location.pathname.startsWith('/mda/'))
            header_nofix('div.sf-content__post', 'div.sf-login-form');
          else {
            let body = document.querySelector('body[class*="postid-"]');
            if (body) {
              let article_id = body.className.split('postid-')[1].split(' ')[0];
              article_id &&
                ((func_post = function () {
                  let lazy_images = document.querySelectorAll('img.lazy[data-src]:not([src])');
                  for (let elem of lazy_images)
                    ((elem.src = elem.getAttribute('data-src')), elem.removeAttribute('class'));
                }),
                getJsonUrl(
                  'div.sf-login-form',
                  '',
                  'div.sf-content-body__text',
                  { art_append: 1 },
                  article_id,
                  '',
                  !1,
                  !0
                ));
            }
          }
        else if (matchDomain('thenewworld.co.uk')) {
          let paywall = document.querySelector('div[data-show-fade-on-noaccess]');
          if (paywall) {
            removeDOMElement(paywall);
            let content = document.querySelector('div[data-show-has-access]');
            content && content.removeAttribute('data-show-has-access');
          }
          removeDOMElement(...document.querySelectorAll('div[data-show-subs-blocked]'));
        } else if (matchDomain('thenewslens.com')) {
          let paywall = document.querySelector('div.article-mask-box');
          if (paywall) {
            removeDOMElement(paywall);
            let json_script = getArticleJsonScript();
            if (json_script) {
              let json = JSON.parse(json_script.text);
              if (json) {
                let json_text = json.articleBody,
                  article = document.querySelector('section.article-body');
                if (json_text && article) {
                  let article_text = article.innerText.replace(/\n/g, ''),
                    split = json_text.split('。');
                  for (let elem of split)
                    if (!elem.includes('(function(') && !article_text.includes(elem)) {
                      let par_new = document.createElement('p');
                      ((par_new.innerText = elem + '。'), article.append(par_new));
                    }
                }
              }
            }
          }
        } else if (matchDomain('thestage.co.uk')) {
          func_post = function () {
            let paywall = document.querySelector(paywall_sel);
            if (paywall) {
              removeDOMElement(paywall);
              let article = document.querySelector(article_sel);
              article && article.before(googleSearchToolLink(url));
            }
          };
          let paywall_sel = 'div#ao-MeteringDNAllow',
            article_sel = 'div[id^="aos-FeatureArticle2Col-"], div[id^="aos-ReviewArticle-"]',
            url = window.location.href;
          getArchive(url, paywall_sel, '', article_sel);
        } else if (matchDomain(['thesun.co.uk', 'thescottishsun.co.uk']))
          hideDOMStyle('div.billboard, div.advert-wrapper');
        else if (matchDomain('thetimes.com')) {
          if (!matchDomain('epaper.thetimes.com')) {
            func_post = function () {
              let article = document.querySelector(article_sel);
              if (article) {
                if (
                  (article.querySelectorAll('div[style*="font-family:"]:not(:empty)').length < 5 &&
                    article.before(googleSearchToolLink(url)),
                  mobile)
                ) {
                  let figure_div = article.querySelector(
                    'figure > div[style] > div[style^="min-height:"]'
                  );
                  figure_div &&
                    (figure_div.removeAttribute('style'),
                    figure_div.parentNode.removeAttribute('style'));
                  let inline_images = article.querySelectorAll('img[style]');
                  for (let elem of inline_images) {
                    elem.style =
                      'display: block; margin-left: auto; margin-right: auto; width: 90%;';
                    let parent_node = elem.parentNode;
                    ('PICTURE' === parent_node.tagName && (parent_node = parent_node.parentNode),
                      parent_node.removeAttribute('style'),
                      elem.removeAttribute('sizes'));
                  }
                  (article
                    .querySelectorAll(
                      'div[style*="margin-left:"][style*="text-align:"], div[style*="grid-template-columns:"], figcaption, figure > span[style]'
                    )
                    .forEach((e) => (e.style = 'margin: 0px 20px')),
                    article
                      .querySelectorAll('div[style*=";width:"]')
                      .forEach((e) => (e.style.width = '90%')));
                  let author = document.querySelector(
                    article_sel +
                      ' div[style^="min-height:"]:has(a[href^="https://www.thetimes.com/profile/"])'
                  );
                  author && (author.style = 'width: 90%; margin: 20px;');
                  let header = article.querySelector('div[style] > h1');
                  header && header.parentNode.removeAttribute('style');
                }
                article
                  .querySelectorAll('img[src^="data:image/"][currentsourceurl]')
                  .forEach((e) => (e.src = e.getAttribute('currentsourceurl')));
                let charts = document.querySelectorAll('times-datawrapper[embed-code]');
                for (let elem of charts) {
                  let div = document.createElement('div');
                  mobile || (div.style = 'margin: 0px 20%;');
                  let iframe = document.createElement('iframe');
                  ((iframe.src = decodeURIComponent(elem.getAttribute('embed-code'))),
                    (iframe.style = 'width: 80%; height: 400px; border: none;'),
                    div.appendChild(iframe),
                    elem.parentNode.replaceChild(div, elem));
                }
                let embed_iframes = article.querySelectorAll('div > times-embed-iframe-max[src]');
                for (let elem of embed_iframes) {
                  let iframe_new,
                    iframe_src = elem.getAttribute('src');
                  (iframe_src.startsWith('https://youtube.com/')
                    ? ((iframe_new = document.createElement('iframe')),
                      (iframe_new.src = iframe_src),
                      (iframe_new.style = 'width: 100%; aspect-ratio: 16 / 9; border: none;'))
                    : iframe_src.startsWith('https://www.tiktok.com/')
                      ? ((iframe_new = document.createElement('iframe')),
                        (iframe_new.src = iframe_src),
                        (iframe_new.style = 'width: 400px; aspect-ratio: 1 / 2; border: none;'))
                      : iframe_src.startsWith('https://embed.acast.com/')
                        ? ((iframe_new = document.createElement('iframe')),
                          (iframe_new.src = iframe_src),
                          (iframe_new.style = 'width: 100%; border: none;'))
                        : ((iframe_new = document.createElement('a')),
                          (iframe_new.href = iframe_new.innerText = iframe_src),
                          (iframe_new.target = '_blank')),
                    elem.parentNode.removeAttribute('style'),
                    elem.parentNode.replaceChild(iframe_new, elem));
                }
                if (read_next) {
                  let art_bottom = article.querySelector('nav ~div');
                  art_bottom && art_bottom.after(read_next);
                }
              }
            };
            let read_next = document.querySelector('div.article-previous-next--sticky'),
              paywall_sel = 'div#paywall-portal-article-footer, div#paywall-takeover',
              article_sel = 'article[id]',
              url = window.location.href;
            (window.location.pathname.includes('/article/') &&
              !window.location.search.startsWith('?shareToken=') &&
              (getArchive(url, paywall_sel, '', article_sel),
              addStyle('html, body {overflow: auto !important; height: auto !important;}')),
              hideDOMStyle(
                'div#paywall-portal-page-footer, div.channel-header-ad, div[id^="advert-"], div[class*="InlineAdWrapper"], div[id^="ad-article-inline"], div:has(> div > div#ad-header)'
              ));
          }
        } else if (matchDomain('unherd.com')) {
          let preview = document.querySelector('div#premiumpreview');
          if (preview) {
            removeDOMElement(preview);
            let premium = document.querySelector('div#premiumcontent');
            premium && premium.removeAttribute('id');
          }
        } else if (matchDomain('worldeconomics.com')) {
          let paywall = document.querySelector('div#paywall');
          paywall &&
            (removeDOMElement(paywall),
            document.querySelectorAll('div.blurred').forEach((e) => e.classList.remove('blurred')),
            document
              .querySelectorAll('div.blurredImage')
              .forEach((e) => e.classList.remove('blurredImage')));
          let noscroll = document.querySelector('body.noscroll');
          (noscroll && noscroll.classList.remove('noscroll'),
            hideDOMStyle('div.StickyFooter, div#scrollable'));
        } else if (matchDomain(['dailymail.com', 'thisismoney.co.uk'])) {
          let paywall = document.querySelector('body.is-paywalled-article');
          (paywall && paywall.classList.remove('is-paywalled-article'),
            hideDOMStyle('ad-slot, div.billboard-container'));
        } else if (
          matchDomain(['scotsman.com', 'yorkshirepost.co.uk']) ||
          document.querySelector('footer a[href^="https://www.mediaconcierge.co.uk"]')
        ) {
          let premium = document.querySelector('div.premium.no-entitlement');
          (premium && premium.classList.remove('premium', 'no-entitlement'),
            hideDOMStyle(
              'div[class^="MarkupAds__Container-"], div[class*="_AdContainer-"], div[class^="Dailymotion__Wrapper-"], div.banner, aside.sidebar, div#mantis-carousel-wrapper:has( > mantis-ui-widget:empty)'
            ));
        } else
          (matchDomain(['dailyrecord.co.uk', 'express.co.uk']) ||
            document.querySelector('footer a[href="https://jobs.reachplc.com/jobs"]')) &&
            removeDOMElement(
              ...document.querySelectorAll(
                'div#superbanner, div.ad-wrapper, div[id^="div-gpt-ad-"], div.taboola-above-article, div#mantis-recommender-top-placeholder, div#ovp-primis, div[data-testid^="commercial-"]'
              )
            );
      else if (matchDomain(usa_adv_local_domains))
        (window.location.search.startsWith('?outputType=amp') ||
          (document
            .querySelectorAll('.article__paragraph--blur')
            .forEach((e) => e.classList.remove('article__paragraph--blur')),
          amp_redirect('div.paywall', '', window.location.pathname + '?outputType=amp')),
          hideDOMStyle('div.ad, div.ad-inner, div.ad-unit, div#below-toprail, div[id^="taboola"]'));
      else if (matchDomain('abqjournal.com')) {
        let paywall = document.querySelector('div.lab-paywall-locked');
        paywall && paywall.classList.remove('lab-paywall-locked');
      } else if (matchDomain('adweek.com')) {
        setCookie('blaize_session', '', '.www.adweek.com', '/', 0);
        let paywall = document.querySelector('div.paywall');
        if (paywall) {
          (removeDOMElement(paywall),
            hideDOMStyle('div.adw-article-body div[style*="linear-gradient"]'));
          let article = document.querySelector('div.non-paywall');
          if (article) {
            let json_script = getArticleJsonScript();
            if (json_script)
              try {
                let json = JSON.parse(json_script.text);
                if (json && json.sharedContent) {
                  let json_text = json.sharedContent.articleBody;
                  if (json_text.match(/\[[^\[]+]/)) {
                    let url = window.location.href;
                    (article.after(googleSearchToolLink(url)),
                      header_nofix('div#bpc_archive', '', 'BPC > no fix (missing (social) media)'));
                  }
                  ((json_text = json_text
                    .replace(/\[[^\[]+]/g, '')
                    .replace(/((\r)?\n)+/g, '\r\n\r\n')),
                    json_text && (article.innerText = json_text));
                }
              } catch (err) {}
          }
        }
      } else if (matchDomain('ajc.com')) {
        let paygate = document.querySelector('div.story-paygate_placeholder');
        if (paygate) paygate.removeAttribute('class');
        else {
          let article_paywall = document.querySelectorAll('article.is-paywalled');
          if (article_paywall.length) {
            let intro_len = document.querySelectorAll('article:not(.is-paywalled) > p').length,
              art_pars = document.querySelectorAll('article.is-paywalled > p');
            article_paywall.forEach((e) => e.classList.remove('is-paywalled'));
            let json_script,
              scripts = document.querySelectorAll('script:not([src], [type])'),
              script_start = 'self.__next_f.push([1,"';
            for (let script of scripts)
              if (
                script.text.startsWith(script_start) &&
                script.text.includes('{\\"contentData\\":')
              ) {
                json_script = script;
                break;
              }
            if (json_script) {
              let json_pars_text = json_script.text
                .split('{\\"contentData\\":')[1]
                .split(',\\"contentMeta\\":{')[0]
                .replace(/\\"/g, '"')
                .replace(/\\\"/g, '"');
              try {
                let json_pars = JSON.parse(json_pars_text).content_elements.filter(
                    (e) => 'text' === e.type && e.content
                  ),
                  par_nr = intro_len;
                for (let par of art_pars) ((par.innerHTML = json_pars[par_nr].content), par_nr++);
              } catch (err) {}
            }
          }
        }
        (removeDOMElement(document.querySelector('div.video-blocker')), hideDOMStyle('div.arc_ad'));
      } else if (matchDomain('al-monitor.com')) {
        func_post = function () {
          if (mobile) {
            let article = document.querySelector(article_sel);
            if (article) {
              let lazy_images = article.querySelectorAll('picture > img[loading="lazy"][style]');
              for (let elem of lazy_images)
                ((elem.style = 'width: 95%;'), elem.parentNode.removeAttribute('style'));
              let art_width = article.offsetWidth - 20 + 'px',
                header = document.querySelector('header');
              header && (header.style.width = art_width);
              let elems = article.querySelectorAll(':not(a, svg)[style*="width"]');
              for (elem of elems)
                ((elem.style.width = art_width), (elem.style['grid-template-columns'] = ''));
              let lang = article.querySelector('div[style] > ul[style*="align-items"]');
              lang && lang.parentNode.removeAttribute('style');
              let par = article.querySelector('div[style] > div[dir="ltr"]');
              par && (par.parentNode.style = 'width: ' + art_width);
            }
          }
        };
        let url = window.location.href,
          article_sel = 'article';
        getArchive(
          url,
          'div.node__paywall-cta',
          '',
          article_sel,
          '',
          article_sel,
          article_sel + ' > div'
        );
      } else if (
        matchDomain('americanbanker.com') ||
        matchDomain([
          'accountingtoday.com',
          'benefitnews.com',
          'bondbuyer.com',
          'dig-in.com',
          'financial-planning.com',
          'nationalmortgagenews.com',
        ])
      ) {
        let inline_gate = document.querySelector('.inline-gate');
        if (inline_gate) {
          inline_gate.classList.remove('inline-gate');
          let inline_gated = document.querySelectorAll('.inline-gated');
          for (let elem of inline_gated) elem.classList.remove('inline-gated');
        }
        removeDOMElement(...document.querySelectorAll('div.GoogleDfpAd'));
      } else if (matchDomain('artnet.com'))
        if (window.location.pathname.endsWith('/amp-page')) amp_unhide_subscr_section();
        else {
          let body_hidden = document.querySelector('.article-body');
          (body_hidden && (body_hidden.style = 'display:block;'),
            removeDOMElement(document.querySelector('div[id^="issuem-leaky-paywall-"]')));
        }
      else if (matchDomain('asia.nikkei.com')) {
        let paywall = document.querySelector('div#paywall-offer');
        if (paywall) {
          removeDOMElement(paywall);
          let article = document.querySelector('div#article-body-preview > div');
          if (article) {
            let json_script = document.querySelector('script#__NEXT_DATA__');
            if (json_script)
              try {
                let json = JSON.parse(json_script.text);
                if (json && json.props.pageProps.data.body) {
                  let json_text = json.props.pageProps.data.body;
                  json_text.includes('<div>') || (json_text = '<div>' + json_text + '</div>');
                  let article_new = new DOMParser()
                    .parseFromString(json_text, 'text/html')
                    .querySelector('div');
                  article.parentNode.replaceChild(article_new, article);
                } else refreshCurrentTab();
              } catch (err) {}
          }
        }
        hideDOMStyle('div#pianoj_ribbon, div#paywall-offer');
      } else if (matchDomain('axios.com')) {
        function axios_noscroll(node) {
          (node.removeAttribute('style'),
            hideDOMStyle('div[class^="Modal_paywall"], div[class^="Modal_cta"]', 2));
        }
        let noscroll = document.querySelector('html[style]');
        (noscroll && axios_noscroll(noscroll),
          waitDOMAttribute('html', 'HTML', 'style', axios_noscroll, !0),
          hideDOMStyle(
            'div[data-cy="pro-paywall"], div.apexAd, div[class*="NativeAd"], span[data-ad-type]'
          ));
      } else if (matchDomain('balkaninsight.com'))
        getJsonUrl('div.subscribeWrapper', '', 'div.post_teaser', { art_append: 1, art_hold: 1 });
      else if (matchDomain(['barandbench.com', 'theleaflet.in', 'thenewsminute.com'])) {
        let paywall = document.querySelector('div[id*="paywall-banner"]');
        if (paywall) {
          removeDOMElement(paywall);
          let article = document.querySelector('div.paywall');
          if (article) {
            let article_new = getArticleQuintype();
            article_new &&
              article.parentNode &&
              article.parentNode.replaceChild(article_new, article);
          }
        }
      } else if (matchDomain('barrons.com')) {
        if (window.location.pathname.startsWith('/livecoverage/'))
          window.setTimeout(function () {
            fix_dowjones_live();
          }, 1500);
        else {
          let paywall = document.querySelector(
            'div#cx-interstitial-snippet-container, div[data-id^="ArticleRoadblock_"]'
          );
          paywall &&
            (removeDOMElement(paywall),
            window.setTimeout(function () {
              let article = document.querySelector(
                'article > div.crawler > [data-testid="article-body"]'
              );
              if (article) {
                let article_id_dom = document.querySelector(
                  'head > meta[name="article.id"][content], head > meta[name="ChosenAdTarget"][content]'
                );
                if (article_id_dom) {
                  let article_id = article_id_dom.content;
                  (article_id.includes(';id:') &&
                    (article_id = article_id.split(';id:')[1].split(';')[0]),
                    getExtFetch(
                      'https://barrons.djmedia.djservices.io/apps/barrons/theaters/default-article?screen_ids=' +
                        article_id,
                      '',
                      {
                        headers: {
                          'app-identifier': 'http://com.news.screens',
                          'device-type': 'phone',
                          'x-access-token':
                            'eyJhbGciOiJSUzI1NiJ9.WFZsaHN3MXd3Smw0V3kwRXBzclQ.qwwBedAUNXHTQchowQZ5zMwmnXqDKeMhoRJlkB7drjWmb0ktZCScIhq5lpIiWaMyNJA_ODYgHAfIoi7DKWkS8g8GunFNAXpJDUOLdI2rtQkTEi_E3o90rdZHunPR7p0ULjRmHCnDofAdpTQdJtTXjQ9eEDZT2xoooVGdBpoVKhE',
                        },
                      },
                      fix_dowjones_fetch,
                      [
                        article,
                        'div.media-layout img[src], div[data-block="big-top-media"] img[src]',
                      ]
                    ));
                }
                removeDOMElement(...article.parentNode.querySelectorAll('div:empty'));
              }
            }, 2e3));
        }
        let signin_links = document.querySelectorAll('div[class] > div#duplicated-paywall-shell');
        for (let elem of signin_links) {
          let link_new = document.createElement('a');
          ((link_new.href = '#'),
            (link_new.innerText = 'Open article'),
            link_new.addEventListener('click', function () {
              window.location.reload();
            }),
            elem.parentNode.parentNode.replaceChild(link_new, elem.parentNode));
        }
        hideDOMStyle('div[class]:has(> div.uds-ad-container)');
      } else if (matchDomain('benzinga.com'))
        waitDOMElement(
          'div.paywall-content',
          'DIV',
          function (node) {
            if ((removeDOMElement(node), window.location.pathname.startsWith('/report/'))) {
              let blurred = document.querySelector('div.blur-lg');
              blurred && blurred.classList.remove('blur-lg');
            } else {
              let blurred = document.querySelector('div.article-content-paywalled');
              if (blurred) {
                blurred.classList.remove('article-content-paywalled');
                let key_points = document.querySelectorAll('li.blur-sm');
                for (let elem of key_points) elem.classList.remove('blur-sm');
                let article = document.querySelector('div#article-body');
                if (article) {
                  let json_script = document.querySelector('script#__NEXT_DATA__');
                  if (json_script)
                    try {
                      let json = JSON.parse(json_script.text);
                      if (json && json.props.pageProps.article.primaryImage) {
                        let img_data = json.props.pageProps.article.primaryImage;
                        if (img_data.url) {
                          let img = document.createElement('img');
                          ((img.src = img_data.url), (img.alt = img_data.alt), article.before(img));
                        }
                      }
                    } catch (err) {}
                }
              }
            }
          },
          !1
        );
      else if (matchDomain('bhaskar.com'))
        if (window.location.pathname.startsWith('/amp/')) ampToHtml();
        else {
          let paywall_sel = 'div.paywallBlockedContent',
            paywall = document.querySelector(paywall_sel);
          if (paywall) {
            let banner = document.querySelector(paywall_sel + ' ~ div');
            (banner && removeDOMElement(banner), paywall.removeAttribute('class'));
          }
          let ads = document.querySelectorAll(
            'div[style*="overflow:hidden"][style*="max-height:"], div[id^="Ad--"], article  div:empty'
          );
          ads && removeDOMElement(...ads);
        }
      else if (matchDomain('bizjournals.com')) {
        if (window.location.pathname.includes('/subscriber-only/')) header_nofix('div.primary');
        else {
          let paywall = document.querySelector('div.paywall-content:empty');
          if (paywall) {
            paywall.classList.remove('paywall-content');
            let gallery_intro = document.querySelector('header div[data-dev="MediaGallery"]');
            if (window.location.search.startsWith('?rel=gallery'))
              return void (
                gallery_intro &&
                header_nofix(
                  'header',
                  '',
                  'BPC > go to full article',
                  window.location.href.split('?')[0] + '?rel=plus'
                )
              );
            if (!window.location.search.startsWith('?rel=plus'))
              return void (window.location.href = window.location.pathname + '?rel=plus');
            gallery_intro &&
              header_nofix(
                'header',
                '',
                'BPC > show intro gallery',
                window.location.href.split('?')[0] + '?rel=gallery'
              );
            let header_images = document.querySelectorAll('img[loading][width][src]');
            for (let img of header_images) {
              let img_width_match = img.src.match(/(\*\d+xx)/);
              img_width_match &&
                (img.src = img.src.replace(img_width_match[1], '*' + img.width + 'xx'));
            }
            let article = document.querySelector('div#ContentTease');
            if (article) {
              let json_script = document.querySelector('script#__NUXT_DATA__');
              if (json_script)
                try {
                  let json = JSON.parse(json_script.text);
                  if (json) {
                    let pars_index = json.indexOf('paragraph') + 1;
                    if (pars_index) {
                      let intro = article.querySelector('p[class]'),
                        par_class = intro ? intro.className : 'content mb-4';
                      article.innerHTML = '';
                      let parser = new DOMParser(),
                        hl_index = json.indexOf('highlights') + 1;
                      if (hl_index) {
                        let container = document.createElement('div');
                        ((container.className = 'border border-gray-300 p-2 font-sans'),
                          (container.style = 'margin-bottom: 20px;'));
                        let title = document.createElement('h3');
                        ((title.className = 'pb-4 font-bold'),
                          (title.innerText = 'Story Highlights'));
                        let highlights = parser
                          .parseFromString(
                            '<div class="' + par_class + '">' + json[hl_index] + '</div>',
                            'text/html'
                          )
                          .querySelector('div');
                        (container.append(title, highlights), article.appendChild(container));
                      }
                      let json_images = json.filter((x) => x && x.ord && x.url);
                      for (let i = pars_index; i < json.length; i++) {
                        let style,
                          par = json[i];
                        if (par) {
                          if ('header' === par)
                            ((style = 'font-weight: bold;'), i++, (par = json[i]));
                          else if (par.type && 'media' === json[par.type] && par.ord) {
                            let json_img = json_images.find((x) => x.ord === par.ord);
                            if (json_img) {
                              let figure = makeFigure(
                                json[json_img.url],
                                json[json_img.caption].replace(/<br>/g, '') +
                                  (json[json_img.byline]
                                    ? '\r\n' + json[json_img.byline].toUpperCase()
                                    : ''),
                                { alt: json[json_img.altText] }
                              );
                              ((figure.className = par_class), article.appendChild(figure));
                            }
                          } else if (par.type && 'gallery' === json[par.type] && par.id) {
                            let gallery = json.find(
                              (x) =>
                                x &&
                                x.media &&
                                x.id === par.id &&
                                x.type &&
                                'gallery' === json[x.type]
                            );
                            if (gallery) {
                              if (gallery.title) {
                                let title = document.createElement('p');
                                ((title.innerText = json[gallery.title]),
                                  (title.style = 'font-weight: bold;'),
                                  article.appendChild(title));
                              }
                              for (let img of json[gallery.media]) {
                                let json_img = json[img],
                                  figure = makeFigure(
                                    json[json_img.url],
                                    json[json_img.caption].replace(/<br>/g, '') +
                                      (json[json_img.byline]
                                        ? '\r\n' + json[json_img.byline].toUpperCase()
                                        : ''),
                                    { alt: json[json_img.altText] }
                                  );
                                ((figure.className = par_class), article.appendChild(figure));
                              }
                            }
                          }
                          if (
                            'string' != typeof par ||
                            [
                              'blockquote',
                              'default',
                              'embed',
                              'gallery',
                              'horizontal_line',
                              'image',
                              'list',
                              'media',
                              'top25list',
                            ].includes(par)
                          ) {
                            if (par.is_primary || par['primary-channel']) break;
                          } else {
                            if (par.match(/^\d{4}-\d{2}-/)) break;
                            let par_new = parser
                              .parseFromString(
                                '<div class="' + par_class + '">' + par + '</div>',
                                'text/html'
                              )
                              .querySelector('div');
                            (par_new
                              .querySelectorAll('iframe[allow*="fullscreen"][allowfullscreen]')
                              .forEach((e) => e.removeAttribute('allowfullscreen')),
                              style && (par_new.style = style),
                              article.appendChild(par_new));
                          }
                        }
                      }
                    }
                    let audio_tts = document.querySelector('div[data-dev="ArticlePlayer"]');
                    if (audio_tts) {
                      let audio_src = json.find(
                        (x) => x && 'string' == typeof x && x.endsWith('.mp3')
                      );
                      if (audio_src) {
                        let audio_new = document.createElement('audio');
                        ((audio_new.src = 'https://audiop.bizjournals.com' + audio_src),
                          audio_new.setAttribute('controls', ''),
                          audio_tts.parentNode.replaceChild(audio_new, audio_tts));
                      }
                    }
                  }
                } catch (err) {}
            }
          } else if (window.location.pathname.endsWith('.html')) {
            let main_content = document.querySelector('div.main-content');
            main_content &&
              !main_content.querySelector('div') &&
              header_nofix(
                main_content,
                '',
                'BPC > go to main page to pass Cloudflare check',
                window.location.href.split(/\/(news|stories)\//)[0]
              );
          }
          window.setTimeout(function () {
            let dialog = document.querySelector('div[id^="headlessui-dialog-"], div.sheet-overlay');
            if (dialog) {
              removeDOMElement(dialog);
              let html = document.querySelector('html[style]');
              html && html.removeAttribute('style');
              let overlays = document.querySelectorAll('html.is-overlaid, body.is-overlaid');
              for (let elem of overlays) elem.classList.remove('is-overlaid');
              let nuxt_inert = document.querySelector('div#__nuxt[inert]');
              nuxt_inert && nuxt_inert.removeAttribute('inert');
            }
          }, 1e3);
        }
        hideDOMStyle('div.adwrap, div[data-dev="MovableAd"]');
      } else if (matchDomain('bloomberg.com')) {
        function addLink(item, elem) {
          item.data &&
            item.content &&
            (!item.data.href.startsWith('http') &&
              item.data.webUrl &&
              (item.data.href = item.data.webUrl),
            item.content && item.content[0] && item.content[0].value) &&
            addLinkValue(
              item.content[1] ? item.content.map((x) => x.value).join('') : item.content[0].value,
              item.data.href,
              elem
            );
        }
        function addLinkValue(item_text, item_href, elem) {
          if (
            item_href.startsWith('bbg://securities/') &&
            !item_href.split('bbg://securities/')[1].includes('/')
          ) {
            let path = '/quote/';
            (item_href.match(/\/\d+[A-Z]%20[A-Z]{2}%20Equity/) &&
              ((path = '/profile/company/'),
              (item_href = item_href.replace('%20Equity', '').replace('%20', ':'))),
              (item_href =
                'https://www.bloomberg.com' + path + item_href.split('bbg://securities/')[1]));
          }
          let sub_elem = makeLink(item_href, item_text, 'text-decoration: underline;');
          (!item_href.startsWith('http') ||
            (item_href.startsWith(window.location.origin) &&
              !item_href.match(/\.bloomberg\.com\/(quote|profile\/company)\//)) ||
            (sub_elem.target = '_blank'),
            elem.appendChild(sub_elem));
        }
        function addEmbed(item, elem) {
          if (item.iframeData && item.iframeData.html) {
            let embed_elem = new DOMParser()
              .parseFromString('<div>' + item.iframeData.html + '</div>', 'text/html')
              .querySelector('div');
            elem.appendChild(embed_elem);
          }
        }
        function addMedia(par, elem) {
          if ('video' === par.subType) {
            if (par.data.video && par.data.video.src) {
              let video = document.createElement('video');
              if (
                ((video.src = par.data.video.src),
                video.setAttribute('controls', ''),
                elem.appendChild(video),
                par.data.video.caption)
              ) {
                let caption = document.createElement('span');
                ((caption.innerText =
                  parseHtmlEntities(par.data.video.caption) +
                  (par.data.video.credit ? ' ' + par.data.video.credit : '')),
                  elem.appendChild(caption));
              }
            }
          } else if ('photo' === par.subType) {
            if (par.data.photo && par.data.photo.src) {
              let figure = makeFigure(
                par.data.photo.src,
                parseHtmlEntities(
                  (par.data.photo.caption + ' ' + par.data.photo.credit).replace(/<\/?\w+>/g, '')
                )
              );
              elem.appendChild(figure);
            }
          } else if ('chart' === par.subType) {
            if (par.data.chart) {
              let figure = document.createElement('figure'),
                img = document.createElement('img');
              ((img.src = par.data.chart.fallback),
                figure.appendChild(img),
                elem.appendChild(figure));
            }
          } else
            'audio' === par.subType &&
              (function (data, elem) {
                if (
                  data.attachment &&
                  data.attachment.url &&
                  data.attachment.title &&
                  data.attachment.description
                ) {
                  let title = document.createTextNode(data.attachment.title),
                    audio = document.createElement('audio');
                  ((audio.src = data.attachment.url), audio.setAttribute('controls', ''));
                  let description = new DOMParser()
                    .parseFromString(
                      '<div>' + data.attachment.description.replace(/\n\n/g, '<br><br>') + '</div>',
                      'text/html'
                    )
                    .querySelector('div');
                  ((description.style =
                    'margin: 20px 0px; padding: 20px; border: 1px solid black;'),
                    description.firstChild.before(title, audio),
                    elem.appendChild(description));
                }
              })(par.data, elem);
        }
        function addEntity(item, elem) {
          item.content &&
            item.content[0] &&
            item.content[0].value &&
            ('person' === item.subType
              ? elem.appendChild(document.createTextNode(item.content[0].value))
              : 'security' === item.subType
                ? item.data && item.data.link && item.data.link.href
                  ? addLinkValue(item.content[0].value, item.data.link.href, elem)
                  : elem.appendChild(document.createTextNode(item.content[0].value))
                : 'story' === item.subType &&
                  item.data &&
                  item.data.link &&
                  (item.data.link.destination && item.data.link.destination.web
                    ? addLinkValue(item.content[0].value, item.data.link.destination.web, elem)
                    : item.data.link.title &&
                      elem.appendChild(document.createTextNode(item.data.link.title))));
        }
        function addList(content, elem) {
          let ul = document.createElement('ul');
          ul.setAttribute('style', 'list-style-type: disc; padding: 5px;');
          for (let item of content)
            if ('listItem' === item.type) {
              let li = document.createElement('li');
              for (let list_item of item.content)
                'text' === list_item.type && list_item.value
                  ? li.appendChild(document.createTextNode(list_item.value))
                  : 'paragraph' === list_item.type && list_item.content
                    ? addPar(list_item.content, li)
                    : 'link' === list_item.type
                      ? addLink(list_item, li)
                      : 'entity' === list_item.type &&
                          list_item.content &&
                          list_item.content[0] &&
                          list_item.content[0].value
                        ? addEntity(list_item, li)
                        : 'div' === list_item.type &&
                          list_item.content &&
                          addPar(list_item.content, li);
              ul.appendChild(li);
            }
          elem.appendChild(ul);
        }
        function addPar(content, elem) {
          for (let item of content)
            if ('text' === item.type && item.value) {
              let span = document.createElement('span');
              ((span.innerText = item.value),
                item.attributes &&
                  (item.attributes.strong
                    ? (span.style = 'font-weight: bold')
                    : item.attributes.emphasis && (span.style = 'font-style: italic')),
                elem.appendChild(span));
            } else if (['br', 'hr'].includes(item.type))
              elem.appendChild(document.createElement(item.type));
            else if ('link' === item.type) addLink(item, elem);
            else if ('entity' === item.type) addEntity(item, elem);
            else if (['paragraph', 'div'].includes(item.type)) addPar(item.content, elem);
            else if ('list' === item.type && item.content) addList(item.content, elem);
            else if ('media' === item.type && item.data) addMedia(item, elem);
            else if ('embed' === item.type) addEmbed(item, elem);
            else if (
              'footnoteRef' === item.type &&
              item.data &&
              item.data.footnoteContent &&
              item.data.footnoteContent[0]
            ) {
              let sub_item = item.data.footnoteContent[0];
              if (['paragraph', 'div'].includes(sub_item.type)) {
                footnote_nr++;
                let footnote_link = makeLink(
                  '#footer-ref-footnote-' + footnote_nr,
                  footnote_nr,
                  'font-size: smaller; vertical-align: super; margin: 0px 5px; text-decoration: underline;'
                );
                ((footnote_link.id = 'inline-ref-footnote-' + footnote_nr),
                  elem.appendChild(footnote_link));
                let footnote_bottom = document.createElement('li');
                ((footnote_bottom.id = 'footer-ref-footnote' + footnote_nr),
                  addPar(sub_item.content, footnote_bottom));
                let footnote_bottom_link = makeLink(
                  '#inline-ref-footnote-' + footnote_nr,
                  'View in article',
                  'margin: 0px 5px; text-decoration: underline;'
                );
                ((footnote_bottom_link.id = 'footer-ref-footnote-' + footnote_nr),
                  footnote_bottom.appendChild(footnote_bottom_link),
                  footnotes.appendChild(footnote_bottom));
              }
            }
        }
        (document
          .querySelectorAll('html[class], body[class]')
          .forEach((e) => e.removeAttribute('class')),
          waitDOMAttribute('html', 'HTML', 'class', (node) => node.removeAttribute('class'), !0),
          waitDOMAttribute('body', 'BODY', 'class', (node) => node.removeAttribute('class'), !0));
        let footnote_nr = 0,
          footnotes = document.createElement('ol');
        if (
          ((footnotes.style = 'font-style: italic; margin: 20px 0px; list-style-type: decimal;'),
          window.location.href.match(/s\/\d{4}-/))
        ) {
          let json_next_script = document.querySelector('script#__NEXT_DATA__');
          if (json_next_script)
            try {
              let article,
                json = JSON.parse(json_next_script.text),
                json_pars = json.props.pageProps.story.body.content,
                article_pars = document.querySelectorAll('div > p[class^="ArticleBodyText_"]');
              if (article_pars.length && article_pars.length < 5 && json_pars.length) {
                let par_class = article_pars[0].getAttribute('class');
                if (
                  ((article = article_pars[0].parentNode),
                  (article.style =
                    '-webkit-mask-image: none !important; mask-image: none !important;'),
                  json.props.pageProps.story.readingUrl)
                ) {
                  let audio_tts = document.createElement('audio');
                  ((audio_tts.src = json.props.pageProps.story.readingUrl),
                    audio_tts.setAttribute('controls', ''),
                    article.before(audio_tts));
                }
                json.props.pageProps.story.aiSummary &&
                  json.props.pageProps.story.aiSummary.text &&
                  json.props.pageProps.story.aiSummary.text.length &&
                  window.setTimeout(function () {
                    let ai_summary = document.querySelector(
                      'div[class^="SummaryOnlyTakeaways_takeaways_"], div.done'
                    );
                    if (!ai_summary) {
                      ((ai_summary = document.createElement('div')),
                        (ai_summary.style = 'width: 80%; margin: auto;'));
                      let title = document.createElement('div');
                      ((title.innerText = 'Takeaways'),
                        (title.style = 'font-weight: bold; margin: 20px;'),
                        ai_summary.appendChild(title),
                        article.firstChild.before(document.createElement('br'), ai_summary));
                    }
                    if ('done' !== ai_summary.className) {
                      ((ai_summary.className = 'done'), (ai_summary.style.border = 'solid'));
                      let ul = document.createElement('ul');
                      ul.style = 'margin: 20px 50px; list-style-type: disc;';
                      for (let elem of json.props.pageProps.story.aiSummary.text) {
                        let item = document.createElement('li');
                        ((item.innerText = elem), ul.appendChild(item));
                      }
                      ai_summary.appendChild(ul);
                    }
                  }, 2e3);
                for (let par of json_pars) {
                  let elem = document.createElement('p');
                  if (
                    (elem.setAttribute('class', par_class),
                    elem.setAttribute('data-component', 'paragraph'),
                    'heading' === par.type && (elem.style = 'font-weight: bold;'),
                    ['heading', 'paragraph'].includes(par.type) && par.content)
                  )
                    addPar(par.content, elem);
                  else if (par.type.endsWith('quote') && par.content) {
                    elem.appendChild(document.createElement('br'));
                    for (let item of par.content)
                      if ('paragraph' === item.type && item.content)
                        for (let sub_item of item.content)
                          'text' === sub_item.type && sub_item.value
                            ? elem.appendChild(document.createTextNode(sub_item.value + ' '))
                            : 'entity' === sub_item.type && addEntity(sub_item, elem);
                    elem.style = 'font-style: italic';
                  } else if ('media' === par.type && par.data) addMedia(par, elem);
                  else if ('embed' === par.type) addEmbed(par, elem);
                  else if ('div' === par.type && par.content)
                    (addPar(par.content, elem), elem.appendChild(document.createElement('br')));
                  else if ('list' === par.type && par.content) addList(par.content, elem);
                  else if ('byTheNumbers' === par.type && par.content) {
                    for (let item of par.content)
                      if ('byTheNumbersItem' === item.type && item.content)
                        for (let sub_item of item.content)
                          sub_item.content &&
                            (addPar(sub_item.content, elem),
                            elem.appendChild(document.createElement('br')));
                  } else
                    ['ad', 'inline-newsletter', 'inline-recirc', 'tabularData'].includes(par.type);
                  elem.hasChildNodes && article.appendChild(elem);
                }
                (removeDOMElement(...article_pars), footnote_nr && article.appendChild(footnotes));
              }
            } catch (err) {}
        } else if (window.location.pathname.includes('/live-blog/')) {
          let paywall = document.querySelector('div[class*="Liveblog_regwallWaiting_"]');
          if (paywall) {
            paywall.removeAttribute('class');
            let json_next_script = document.querySelector('script#__NEXT_DATA__');
            if (json_next_script)
              try {
                let liveblog = JSON.parse(json_next_script.text).props.pageProps.liveblog;
                if (liveblog) {
                  paywall.innerHTML = '';
                  let posts = liveblog.posts;
                  for (let post of posts) {
                    let elem = document.createElement('div');
                    if (
                      ((elem.style = 'margin: 20px;'),
                      (elem.style.width = document.querySelector('main')
                        ? 0.8 * document.querySelector('main').offsetWidth + 'px'
                        : '400px'),
                      post.body &&
                        post.body.content &&
                        post.body.content[0] &&
                        post.body.content[0].content)
                    ) {
                      let header = post.header;
                      if (header && header.contributorName) {
                        if (header.contributorThumbnailUrl) {
                          let author_img = document.createElement('img');
                          ((author_img.src = header.contributorThumbnailUrl),
                            elem.appendChild(author_img));
                        }
                        if (header.contributorSlug) {
                          let author_link = makeLink(
                            '/authors/' + header.contributorSlug,
                            header.contributorName,
                            'text-decoration: underline;'
                          );
                          elem.appendChild(author_link);
                        } else
                          elem.append(
                            document.createTextNode(header.contributorName),
                            document.createElement('br')
                          );
                        header.timestamp &&
                          header.timestamp.raw &&
                          elem.append(
                            document.createTextNode(
                              ' - ' +
                                new Intl.DateTimeFormat('en-US', {
                                  dateStyle: 'long',
                                  timeStyle: 'short',
                                  timeZone: 'America/New_York',
                                }).format(new Date(header.timestamp.raw))
                            ),
                            document.createElement('br')
                          );
                      }
                      let pars = post.body.content[0].content;
                      for (let par of pars)
                        'text' === par.type && par.value
                          ? elem.appendChild(document.createTextNode(par.value))
                          : 'link' === par.type
                            ? addLink(par, elem)
                            : par.type.endsWith('quote') && par.content
                              ? (elem.appendChild(document.createElement('br')),
                                (elem.style['font-style'] = 'italic'),
                                addPar(par.content, elem))
                              : 'div' === par.type
                                ? (elem.appendChild(document.createElement('br')),
                                  addPar(par.content, elem))
                                : 'br' === par.type &&
                                  elem.appendChild(document.createElement('br'));
                      elem.appendChild(document.createElement('hr'));
                    }
                    elem.hasChildNodes() && paywall.appendChild(elem);
                  }
                }
              } catch (err) {}
          }
        } else if (window.location.pathname.startsWith('/quote/')) {
          let blur_chart = document.querySelector('div[class^="summary_contentBlur_"]');
          blur_chart && blur_chart.removeAttribute('class');
        } else if (window.location.pathname.startsWith('/profile/company/')) {
          let json_script,
            scripts = document.querySelectorAll('script[type="application/ld+json"]');
          for (let script of scripts)
            if (script.innerText.includes(',"@type":"Organization",')) {
              json_script = script;
              break;
            }
          if (json_script)
            try {
              let json = JSON.parse(json_script.text),
                article = document.querySelector('section[class^="info__"]');
              if (article) {
                let data = document.createElement('div');
                ((data.style = 'margin: 20px;'), document.createElement('br'));
                let json_items = [
                  'address',
                  'telephone',
                  'url',
                  'foundingDate',
                  'numberOfEmployees',
                  'employee',
                ];
                for (let item of json_items)
                  if (json[item]) {
                    let div = document.createElement('div');
                    if (((div.style = 'margin: 20px 0px;'), 'employee' === item)) {
                      if (json[item].length) {
                        div.innerText = 'EXECUTIVES/BOARD\r\n';
                        for (let person of json[item])
                          if (person.name) {
                            let sub_div = document.createElement('div');
                            ((sub_div.innerText =
                              person.name +
                              (person.jobTitle
                                ? ' - ' + person.jobTitle
                                : person.worksFor
                                  ? ' - ' + person.worksFor
                                  : '')),
                              div.appendChild(sub_div));
                          }
                      }
                    } else if ('url' === item) {
                      div.innerText = 'WEBSITE\r\n';
                      let sub_div = document.createElement('a');
                      ((sub_div.innerText = json[item]),
                        json[item].startsWith('https://') || (json[item] = 'https://' + json[item]),
                        (sub_div.href = json[item]),
                        (sub_div.target = '_blank'),
                        div.appendChild(sub_div));
                    } else div.innerText = item.toUpperCase() + '\r\n' + json[item];
                    data.appendChild(div);
                  }
                article.appendChild(data);
              }
              removeDOMElement(json_script);
            } catch (err) {}
          hideDOMStyle('div[class^="shimmering"]');
        } else
          window.location.pathname.startsWith('/live/') &&
            setInterval(function () {
              window.localStorage.clear();
            }, 9e5);
        hideDOMStyle(
          [
            'div[id^="leaderboard"], div[class^="leaderboard"], div.canopy-container',
            'article.first-story div[class*="Placeholder_placeholderParagraphWrapper-"]',
            'div[data-ad-status], div[data-ad-type], div[class*="FullWidthAd_"], div.adWrapper, div.dvz-v0-ad',
          ].join(',')
        );
      } else if (matchDomain('bloombergadria.com')) {
        let article_hidden = document.querySelector('article.single-news[style]');
        (article_hidden && article_hidden.removeAttribute('style'), hideDOMStyle('.banner'));
      } else if (matchDomain('bostonglobe.com'))
        window.location.search.startsWith('?outputType=amp') && amp_unhide_subscr_section();
      else if (matchDomain('business-standard.com')) {
        function bs_main(node) {
          removeDOMElement(node);
          let audio_tts = document.querySelector('audio[src$="-short.mp3"]');
          audio_tts && (audio_tts.src = audio_tts.src.replace('-short', ''));
          let json_script = document.querySelector('script#__NEXT_DATA__');
          if (json_script)
            try {
              let json = JSON.parse(json_script.text);
              if (json && json.props.pageProps.data.htmlContent) {
                let json_text = json.props.pageProps.data.htmlContent,
                  article_sel =
                    'article div[class^="MainStory_storycontent__"], article div[class^="blueprint-story-detail_storbpcontent_"]',
                  article = document.querySelector(article_sel);
                if (json_text && article) {
                  (removeDOMElement(json_script), (article.innerHTML = ''));
                  let article_new = new DOMParser()
                    .parseFromString('<div>' + json_text + '</div>', 'text/html')
                    .querySelector('div');
                  if (article_new) {
                    mobile && (article_new.style = 'width: 90%; margin: 20px;');
                    let zoom_images = article_new.querySelectorAll('div > img.zoomimg');
                    for (let img of zoom_images) {
                      img.style = 'margin: 20px 0px;';
                      let caption = img.parentNode.querySelector('div[style]');
                      caption && caption.removeAttribute('style');
                    }
                    window.setTimeout(function () {
                      article.appendChild(article_new);
                    }, 1e3);
                  }
                }
              } else refreshCurrentTab();
            } catch (err) {}
        }
        if (window.location.pathname.startsWith('/amp/')) ampToHtml();
        else {
          {
            let paywall_sel =
                'div[class^="subscription_subscriptionPlan_"], div[class^="BluePrintPaywall_BPstoryPaywall_"]',
              paywall = document.querySelector(paywall_sel);
            paywall ? bs_main(paywall) : waitDOMElement(paywall_sel, 'DIV', bs_main, !1);
          }
          hideDOMStyle(
            'section.sbcrbtmlfull, div.subscribfixed_artcalLeftBox__hmpMi, div.advertisement-bg, div[id^="between_article_content_"]'
          );
        }
      } else if (matchDomain('businessinsider.com'))
        hideDOMStyle(
          'div.masthead-ad, div.l-ad, div.in-post-sticky, aside.has-video-ad, div.ad-callout-wrapper'
        );
      else if (matchDomain('businessinsider.jp')) {
        let paywall = document.querySelector('div.piano-paywall-container[hidden]');
        paywall && paywall.removeAttribute('hidden');
      } else if (matchDomain('businessoffashion.com'))
        hideDOMStyle('div[class^="default__AdsBlockWrapper"], div[data-test="common-nbabanner"]');
      else if (matchDomain('capital.bg')) {
        let paywall = document.querySelector(
          'section > article a[href^="/paywall_click/"], section[data-paywall-id]'
        );
        if (paywall) {
          removeDOMElement(paywall);
          let json_script = getArticleJsonScript();
          if (json_script) {
            let json = JSON.parse(json_script.text);
            if (json) {
              let json_text = json.articleBody,
                img_main = document.querySelector('div.story--header picture > img[src]'),
                article = document.querySelector('div.story-content');
              if (json_text && article) {
                ((article.innerHTML = ''), document.createElement('p'));
                let json_pars = parseHtmlEntities(json_text)
                  .replace(/\s{2,}/g, '\r\n\r\n')
                  .split(/[\[\]]{2}/);
                for (let elem of json_pars) {
                  let par;
                  if (!elem.match(/[\[\]]{2}/))
                    if (elem.match(/img:\d+/)) {
                      if (img_main) {
                        let img_new_id = elem.split('img:')[1];
                        img_new_id &&
                          ((par = document.createElement('img')),
                          (par.src = img_main.src
                            .replace(/_\d+\./, '_' + img_new_id + '.')
                            .split('?')[0]),
                          (par.style = 'margin: 20px; width: 90%;'));
                      }
                    } else
                      elem.match(/(embed|quote):\d+/) ||
                        ((par = document.createElement('p')), (par.innerText = elem));
                  par && article.appendChild(par);
                }
              }
            }
          }
        } else
          header_nofix('div.story-content > p', 'section > header > a[data-referral="Paywall"]');
        hideDOMStyle('div.banner');
      } else if (matchDomain('caravanmagazine.in')) {
        func_post = function () {
          removeDOMElement(document.querySelector(article_src_sel + ' footer'));
        };
        let url = (url_old = window.location.href),
          paywall_sel = 'div.pay_wall, div.reg_wall',
          article_sel = 'div.article_content',
          article_src_sel = 'div#app > header ~ div';
        setInterval(function () {
          ((url = window.location.href),
            url !== url_old && ((url_old = url), url.match(/(\w+-){3,}/) && refreshCurrentTab()),
            getArchive(url, paywall_sel, '', article_sel, '', article_src_sel));
          let subscr_button = document.querySelector(
            'button[aria-label="Close subscription prompt"]'
          );
          subscr_button && subscr_button.click();
        }, 1e3);
      } else if (matchDomain(['chronicle.com', 'philanthropy.com'])) {
        removeDOMElement(document.querySelector('div[data-content-summary]'));
        let article_hidden = document.querySelector('div[class~="contentBody" i][hidden]');
        if (article_hidden) {
          let attributes = [...article_hidden.attributes].filter((x) => 'class' !== x.name);
          for (let elem of attributes) article_hidden.removeAttribute(elem.name);
        }
        hideDOMStyle('div.GoogleDfpAd-container');
      } else if (matchDomain('cnbc.com'))
        (window.setTimeout(function () {
          let paywall = document.querySelector('div.ArticleGate-proGate');
          if (paywall) {
            removeDOMElement(paywall);
            let article = document.querySelector('div.ArticleBody-articleBody');
            if (article) {
              let intro = article.querySelectorAll('div.group > p');
              if (intro.length < 5) {
                (removeDOMElement(...intro),
                  (article.style =
                    'margin: 20px; font-family: Lyon,Helvetica,Arial,sans-serif; font-size: 18px; line-height: 1.66'));
                let span_hidden = document.querySelectorAll('span[hidden]');
                for (let elem of span_hidden)
                  (elem.removeAttribute('hidden'),
                    elem.removeAttribute('class'),
                    elem.innerText &&
                      (elem.innerText = elem.innerText.split('DISCLOSURES: (None)')[0]),
                    article.appendChild(elem));
              }
            }
          }
        }, 2e3),
          hideDOMStyle('div.TopBanner-container'));
      else if (matchDomain('cnn.com')) {
        let subwall = document.querySelector('div[data-component-id="subwall"]');
        if (subwall) {
          removeDOMElement(subwall);
          let noscroll = document.querySelectorAll('html[style], body[style]');
          for (let elem of noscroll) elem.removeAttribute('style');
          (waitDOMAttribute('html', 'HTML', 'style', (node) => node.removeAttribute('style'), !0),
            waitDOMAttribute('body', 'BODY', 'style', (node) => node.removeAttribute('style'), !0));
        }
        let regwall_keys = Object.keys(window.localStorage).filter((x) => x.match(/reg_?wall/i));
        for (let item of regwall_keys) window.localStorage.removeItem(item);
        hideDOMStyle('div[class*="ad-slot-"], div.container__ads');
      } else if (matchDomain('columbian.com'))
        (setCookie('blaize_session', '', 'columbian.com', '/', 0),
          header_nofix('story', 'div#inline-paywall'),
          hideDOMStyle(
            'div.modal, div#sticky-footer-cta, div.article-inline, div[id^="ad-instory-"], div#onesignal-slidedown-container'
          ));
      else if (matchDomain('csmonitor.com'))
        (removeDOMElement(document.querySelector('div.paywall')), window.localStorage.clear());
      else if (matchDomain('cyclingnews.com')) {
        let paywall = document.querySelector('div.paywall-locker');
        paywall &&
          (paywall.classList.remove('paywall-locker'), hideDOMStyle('div.kiosq-main-layer'));
        let metered_keys = Object.keys(window.localStorage).filter((x) =>
          x.match(/^kiosq_article/)
        );
        for (let item of metered_keys) window.localStorage.removeItem(item);
      } else if (matchDomain('dailyherald.com'))
        hideDOMStyle('div.dhTopAd, div[data-widget-host="revcontent"]');
      else if (matchDomain('dailywire.com')) {
        let article_sel = 'div[data-narration-container]',
          article = document.querySelector(article_sel + ':not(.done)');
        if (article) {
          article.classList.add('done');
          let body = document.querySelector(article_sel + ' > div[class]:first-child');
          (body && body.removeAttribute('class'),
            removeDOMElement(
              ...document.querySelectorAll(article_sel + ' > div:not(:first-child)')
            ));
        }
        if (window.localStorage.getItem('article-gate-data'))
          try {
            let gate_data = JSON.parse(window.localStorage.getItem('article-gate-data'));
            gate_data &&
              ((gate_data.articlesViewed = 1),
              (gate_data.viewedArticles = {}),
              window.localStorage.setItem('article-gate-data', JSON.stringify(gate_data)));
          } catch (err) {}
        hideDOMStyle('div.ad-wrapper, div.css-1d84fd8');
      } else if (matchDomain('dallasnews.com'))
        (window.location.search.startsWith('?outputType=amp') && amp_unhide_subscr_section(),
          hideDOMStyle('div[data-block-type="ad"], div[data-pub-id]'));
      else if (matchDomain('defector.com')) {
        let paywall = document.querySelector('div[class^="ContentGate_wrapper__"]');
        if (paywall) {
          removeDOMElement(paywall);
          let article_sel = 'div[class^="PostContent_wrapper__"]',
            article = document.querySelector(article_sel);
          article &&
            window.setTimeout(function () {
              article.querySelectorAll('p').length < 3 &&
                replaceDomElementExt(window.location.href.split('?')[0], !1, !1, article_sel);
            }, 1e3);
        }
      } else if (matchDomain('denik.cz')) {
        let paywall = document.querySelector('div#js-subscriptionBox');
        if (paywall) {
          removeDOMElement(paywall);
          let article_sel = 'div.article-content';
          document.querySelector(article_sel) &&
            ((func_post = function () {
              let div_hidden = document.querySelector('div.paywall');
              div_hidden && div_hidden.removeAttribute('class');
              let videos = document.querySelectorAll('video[id]:not([src])');
              for (let elem of videos)
                (elem.removeAttribute('class'),
                  removeDOMElement(elem.parentNode.querySelector('button')));
            }),
            replaceDomElementExt(window.location.href, !1, !1, article_sel));
        }
        hideDOMStyle(
          'div.leaderboard-top, div.outstream, div[class^="sticky-"], div[class*="wallpaper-"]'
        );
      } else if (matchDomain(['digiday.com', 'glossy.co', 'modernretail.co']))
        hideDOMStyle('div[class^="ad_"]');
      else if (matchDomain('discovermagazine.com'))
        window.setTimeout(function () {
          let body = document.querySelector('body');
          (body && (body.style = 'overflow: auto !important;'),
            hideDOMStyle(
              'div.fIkXwQ, div[style*="fadeIn"], div[role="button"][aria-label="Dismiss Dialog"]'
            ));
        }, 1e3);
      else if (matchDomain('dnevnik.bg'))
        (window.setTimeout(function () {
          let paywall = document.querySelector('div.paywall-container');
          if (paywall) {
            function addGST() {
              let url = window.location.href,
                article = document.querySelector('div.story-body');
              article && article.firstChild.before(googleSearchToolLink(url));
            }
            removeDOMElement(paywall);
            let article_lock = document.querySelector('div.article-lock');
            if (article_lock) {
              let intro_start,
                intro = article_lock.querySelector('div[role="paragraph"]');
              intro && (intro_start = intro.innerText.substring(0, 25));
              let json_script,
                link_script,
                scripts = document.querySelectorAll('script:not([src], [type])'),
                script_start = 'self.__next_f.push([1,"';
              for (let script of scripts)
                if (
                  script.text.startsWith(script_start) &&
                  (!link_script && script.text.includes('significantLink\\":')
                    ? (link_script = script)
                    : !json_script &&
                      (script.text.includes('[storyid:') ||
                        (intro_start &&
                          script.text
                            .replace(/\\u003c/g, '<')
                            .replace(/\\u003e/g, '>')
                            .replace(/<[^<]*>\s?/g, '')
                            .replace(/\\"/g, '"')
                            .startsWith(script_start + intro_start))) &&
                      (json_script = script),
                  json_script && link_script)
                )
                  break;
              if (json_script) {
                (article_lock.classList.remove('article-lock'),
                  hideDOMStyle('div.paywall-content'));
                let links,
                  img_main = document.querySelector('div.story-gallery-main figure > img[src]');
                link_script &&
                  (links = link_script.text
                    .split('significantLink\\":[')[1]
                    .split('\\"],')[0]
                    .replace(/\\"/g, '')
                    .split(','));
                let json_pars = json_script.text
                  .split('self.__next_f.push([1,"')[1]
                  .split('"])')[0]
                  .replace(/\\u003c/g, '<')
                  .replace(/\\u003e/g, '>')
                  .replace(/\\"/g, '"')
                  .replace(/<br \/>/g, '')
                  .split(/[\[\]]{2}/);
                json_pars.length &&
                  ((article_lock.innerHTML = ''),
                  json_pars.find((x) => x.includes('embed:') || 'gallery' === x) && addGST());
                let parser = new DOMParser();
                for (let elem of json_pars) {
                  let par;
                  if (!elem.match(/[\[\]]{2}/)) {
                    if (elem.match(/img:\d+/)) {
                      if (img_main) {
                        let img_new_id = elem.split('img:')[1];
                        img_new_id &&
                          ((par = document.createElement('img')),
                          (par.src = img_main.src
                            .replace(/_\d+\./, '_' + img_new_id + '.')
                            .split('?')[0]),
                          (par.style = 'margin: 20px; width: 90%;'));
                      }
                    } else if (elem.match(/storyid:\d+/)) {
                      if (links) {
                        let story_id = elem.split('storyid:')[1];
                        if (story_id) {
                          let story = links.find((x) => x.includes(story_id + '_'));
                          story &&
                            ((par = document.createElement('a')),
                            (par.href = story),
                            (par.innerText = story
                              .split(story_id + '_')[1]
                              .replace(/_/g, ' ')
                              .replace('/', '')),
                            (par.className = 'story-related'),
                            matchUrlDomain(window.location.hostname, story) ||
                              (par.target = '_blank'));
                        }
                      }
                    } else
                      elem.match(/quote:\d+/) ||
                        (par = parser
                          .parseFromString('<div role="paragraph">' + elem + '</div>', 'text/html')
                          .querySelector('div'));
                    par && article_lock.appendChild(par);
                  }
                }
              }
            } else addGST();
          }
        }, 1e3),
          hideDOMStyle('div[data-ad-page]'));
      else if (matchDomain('dwell.com')) {
        if (window.location.pathname.match(/^(\/amp)?\/article\//))
          if (window.location.search.startsWith('?rel=plus')) {
            let paywall = document.querySelector('div#mainPanel div[class^="FCR_"]'),
              article = document.querySelector('div > section[class]');
            if (paywall && article) {
              (removeDOMElement(paywall), article.classList.remove('_2S7l9_l2eDI5b8DSR29ijf'));
              let filter = /^window\.INITIAL_STATE\s?=\s?/,
                json_script = getSourceJsonScript(filter);
              if (json_script) {
                let state = json_script.text.split(filter)[1].split('};')[0] + '}';
                if (state)
                  try {
                    let json = JSON.parse(state);
                    if (json) {
                      let items = json.articles.items,
                        id = Object.keys(items)[0],
                        photos = json.photos ? json.photos.items : '',
                        json_text = items[id].attributes.body.replace(
                          /(<br>|<span style=".+;">|<\/span>)/g,
                          ''
                        );
                      json_text = json_text.replace(
                        /<dwell-photo photoId="(\d+)"\scaption="([^"]+)"[^<]+photoUserId="\d*"\/>/g,
                        function (match, p1, p2, offset, string) {
                          let contributorId, format;
                          photos &&
                            photos[p1] &&
                            ((contributorId = photos[p1].attributes.userId),
                            (format = photos[p1].attributes.format));
                          let result = '<p>missing photo: ' + p1 + '</p>';
                          return (
                            contributorId &&
                              (result =
                                '<figure><img src="https://images2.dwell.com/photos/' +
                                contributorId +
                                '/' +
                                p1 +
                                '/original.' +
                                format +
                                '?auto=format&q=35&w=1280"><figcaption>' +
                                p2 +
                                '</figcaption></figure>'),
                            result
                          );
                        }
                      );
                      let article_new = new DOMParser()
                        .parseFromString(
                          '<section class="' + article.className + '">' + json_text + '</section>',
                          'text/html'
                        )
                        .querySelector('section');
                      article.parentNode.replaceChild(article_new, article);
                    }
                  } catch (err) {}
              }
            }
            let photo_links = document.querySelectorAll(
              'div > a[href^="' + window.location.pathname.replace(/\/\d{8,}/, '') + '"]'
            );
            for (let elem of photo_links) elem.href += '?rel=plus';
            let close_button = document.querySelector('header > div > span > svg');
            if (!document.querySelector('a#bpc_close') && close_button) {
              let elem = document.createElement('a');
              ((elem.href = window.location.pathname.split('?')[0].replace(/\/\d{8,}/, '')),
                (elem.id = 'bpc_close'),
                (elem.innerText = 'close'),
                (elem.style.color = 'white'),
                close_button.parentNode.parentNode.appendChild(elem));
            }
          } else {
            let paywall = pageContains('div > a', /Try Dwell\+ for FREE/);
            paywall.length &&
              (removeDOMElement(paywall[0]),
              (window.location.href =
                window.location.pathname.replace(/^\/amp/, '') + '?rel=plus'));
          }
        hideDOMStyle('div.EYrS5iukqzJMkNAcFQ0ho');
      } else if (matchDomain('economictimes.com'))
        if (window.location.pathname.includes('/amp_')) {
          let paywall = document.querySelector('.paywall_wrap');
          if (paywall) {
            let content = document.querySelector('.paywall[style="display:none;"]');
            if (content) {
              let content_new = new DOMParser()
                  .parseFromString(
                    '<div style="margin: 20px 0px;">' +
                      breakText(content.innerText).replace(/\n/g, '<br>') +
                      '</div>',
                    'text/html'
                  )
                  .querySelector('div'),
                charts = content_new.querySelectorAll('iframe[id^="datawrapper-chart-"]');
              for (let elem of charts) ((elem.width = '100%'), (elem.style.margin = '20px 0px'));
              content_new &&
                content.parentNode &&
                content.parentNode.replaceChild(content_new, content);
            } else
              window.location.href =
                'https://economictimes.indiatimes.com' +
                window.location.pathname.replace('amp_prime', 'prime');
            removeDOMElement(
              paywall,
              document.querySelector('.art_wrap'),
              document.querySelector('.articleBlocker')
            );
          }
          hideDOMStyle('amp-ad, amp-fx-flying-carpet, div.ads, div.taboolaAd');
        } else
          window.setTimeout(function () {
            let paywall = document.querySelector('div#blocker_layer'),
              data_prime = document.querySelector('div[data-prime="1"]'),
              amphtml = document.querySelector('head > link[rel="amphtml"]');
            paywall || data_prime
              ? (removeDOMElement(paywall),
                data_prime && data_prime.removeAttribute('data-prime'),
                amphtml
                  ? amp_redirect_not_loop(amphtml)
                  : window.location.pathname.startsWith('/epaper/') &&
                    (window.location.href =
                      'https://economictimes.indiatimes.com' + window.location.pathname))
              : hideDOMStyle('.adContainer');
          }, 500);
      else if (matchDomain('economictimes.indiatimes.com')) {
        let paywall = document.querySelector('section.prime_paywall');
        if (paywall) {
          removeDOMElement(paywall);
          let content = document.querySelector('div.content1, div.artText'),
            full_text = document.querySelector('div.paywall.p1');
          if (content && full_text)
            if (full_text.innerText.includes('["datawrapper-height"]')) {
              let amphtml = document.querySelector('head > link[rel="amphtml"]');
              amphtml && amp_redirect_not_loop(amphtml);
            } else content.innerText = breakText(full_text.innerText);
          addStyle('div.pageContent {height: auto !important;}');
          let article_wrap = document.querySelector('div.article_wrap[style]');
          article_wrap && article_wrap.removeAttribute('style');
        }
        if (mobile) {
          let pageholder = document.querySelector('main.pageHolder');
          if (pageholder) {
            pageholder.classList.remove('pageHolder');
            let header = document.querySelector('header');
            (header && (header.style = 'width: 100% !important;'),
              removeDOMElement(document.querySelector('div.f_col')));
          }
        }
        hideDOMStyle(
          'div.topAdContainer, div.vdo_ai_can_ani, div[data-ad-slot], div#topAd, div.stickyAd, div.blocker_wrap'
        );
      } else if (matchDomain('economist.com')) {
        if (window.location.pathname.includes('/podcasts/'))
          header_nofix('section[data-body-id]', 'div[aria-labelledby="paywall-heading"]');
        else if (!window.location.pathname.startsWith('/interactive/')) {
          let paywall_sel = 'div#tp-regwall',
            video = document.querySelector('iframe[src^="https://www.youtube.com/"]');
          ((func_post = function () {
            if (video) {
              let video_new = document.querySelector('div[old-src^="https://www.youtube.com/"]');
              video_new &&
                video_new.parentNode &&
                video_new.parentNode.replaceChild(video, video_new);
            }
            if (mobile) {
              let grids = document.querySelectorAll('div[style*="grid-template-columns"]');
              for (let elem of grids) elem.removeAttribute('style');
              let lazy_images = document.querySelectorAll('figure img[loading="lazy"][style]');
              for (let elem of lazy_images) elem.style = 'width: 95%;';
            }
          }),
            getArchive(window.location.href, paywall_sel, '', 'main'));
        }
        hideDOMStyle('div[class*="adComponent"]');
      } else if (matchDomain('enotes.com')) {
        let paywall = document.querySelector('section#enotes-paywall');
        if (paywall) {
          (removeDOMElement(paywall, ...pageContains('p', /\(This entire section contains/)),
            setCookie('ENOTESID', '', 'enotes.com', '/', 0));
          let blurred = document.querySelectorAll('div.u-paywall');
          for (let elem of blurred) elem.className = 'o-rte-text u-space--top';
          let ads = document.querySelectorAll('div:not([class]) > div[id^="div-gpt-ad-"]');
          for (let ad of ads) hideDOMElement(ad.parentNode);
        }
      } else if (matchDomain('entrepreneur.com'))
        hideDOMStyle('div.ad__wrapper, div#piano-metered-container');
      else if (matchDomain('epoch.org.il')) getJsonUrl('div.register-login-box', '', 'div.paywall');
      else if (matchDomain('espn.com'))
        getArchive(
          window.location.href,
          'aside.espn-plus-container-wrapper',
          '',
          'section#article-feed > article'
        );
      else if (matchDomain('euobserver.com')) {
        let paywall = pageContains('div > h4', 'To read this story, log in or subscribe');
        if (paywall.length) {
          removeDOMElement(paywall[0].parentNode);
          let article = document.querySelector('div:has(> p.rte-p)');
          if (article) {
            let filter = /^window\.__basedcache__\s?=\s?/,
              json_script = getSourceJsonScript(filter);
            if (json_script)
              try {
                let json = JSON.parse(json_script.text.split(filter)[1]);
                if (json) {
                  let body = findKeyJson(json, ['body']);
                  if (body) {
                    article.innerHTML = '';
                    let article_new = new DOMParser()
                      .parseFromString(
                        '<div style="font-size: 18px; line-height: 30px; position: relative;">' +
                          body +
                          '</div>',
                        'text/html'
                      )
                      .querySelector('div');
                    article.appendChild(article_new);
                  }
                }
              } catch (err) {}
          }
        }
      } else if (matchDomain('fastcompany.com')) {
        let paywall = document.querySelector('div.paywall');
        if (paywall) {
          removeDOMElement(paywall);
          let filter = /\\"content\\":\[/,
            json_script = getSourceJsonScript(filter, ':not([id], [src], [type])');
          if (json_script)
            try {
              let json = JSON.parse(
                '[' +
                  json_script.text
                    .split(filter)[1]
                    .split(/\],\\"/)[0]
                    .replace(/\\\\\\"/g, '\\"')
                    .replace(/(\[|,)\\"/g, '$1"')
                    .replace(/\\"(\]|,)/g, '"$1')
                    .replace(/\\\\n/g, '') +
                  ']'
              );
              if (json) {
                let article = document.querySelector('article');
                if (article) {
                  ((article.innerHTML = ''), (article.className = 'article-container'));
                  let parser = new DOMParser();
                  for (let pars of json)
                    for (let par of pars)
                      if (!par.match(/^\$\w{2}$/)) {
                        let elem = parser
                            .parseFromString(
                              '<div class="content-chunk">' + par + '</div>',
                              'text/html'
                            )
                            .querySelector('div'),
                          img_srcset_drop = elem.querySelectorAll('figure > img[srcset]');
                        for (let img of img_srcset_drop) img.removeAttribute('srcset');
                        article.appendChild(elem);
                      }
                }
              }
            } catch (err) {}
        }
        hideDOMStyle('div[class*="ad-wrapper"]');
      } else if (matchDomain('fieldandstream.com')) {
        removeDOMElement(...document.querySelectorAll('div[class^="mailmunch-"]'));
        let noscroll = document.querySelector('html.mailmunch-pop-open');
        noscroll && noscroll.removeAttribute('class');
      } else if (matchDomain('financialexpress.com')) {
        let paywall = document.querySelector('div.paywall');
        (paywall && paywall.classList.remove('paywall'),
          hideDOMStyle(
            'div.pcl-wrap, ' +
              (window.location.pathname.endsWith('/lite/')
                ? '.ad-bg-container'
                : 'div[class*="-ads-blocks-ad-unit"]')
          ));
      } else if (matchDomain('firstthings.com'))
        (hideDOMStyle('div.leaky_paywall_message_wrap'), (leaky_paywall_unhide_disable = !0));
      else if (matchDomain('fmrmagazine.com')) {
        let paywall = document.querySelector('div.fr-gate-overlay');
        if (paywall) {
          removeDOMElement(paywall);
          let article = document.querySelector('div.issue-article-intro');
          if (article) {
            let url_src = window.location.href.split(/[#\?]/)[0] + '.json';
            fetch(url_src).then((response) => {
              response.ok &&
                response.json().then((json) => {
                  try {
                    let json_text = json.page.body_html;
                    if (json_text) {
                      let article_new = new DOMParser()
                        .parseFromString(json_text, 'text/html')
                        .querySelector('div.issue-article-content');
                      (article_new && article.after(article_new),
                        addStyle('body {overflow: auto !important;}'));
                    }
                  } catch (err) {}
                });
            });
          }
        }
      } else if (matchDomain('forbes.com')) {
        if (
          (waitDOMAttribute('html', 'HTML', 'class', (node) => node.removeAttribute('class'), !0),
          waitDOMAttribute('body', 'BODY', 'class', (node) => node.removeAttribute('class'), !0),
          removeDOMElement(...document.querySelectorAll('div.halfway-hardwall-insertion-point')),
          window.location.pathname.startsWith('/newsletters/'))
        ) {
          let paywall = document.querySelector('div > div.newsletter-teaser');
          paywall &&
            (paywall.classList.remove('newsletter-teaser'), header_nofix(paywall.parentNode));
        }
        hideDOMStyle(
          'fbs-ad, div[class^="fbs-ad"], div[data-testid^="fbs-ad"], div.inlineAdContainer, div[data-testid="locked-top-ad-container"]'
        );
      } else if (matchDomain('forbes.ua')) {
        let paywall = document.querySelector('div.js-closed-part');
        if (paywall) {
          removeDOMElement(paywall);
          let json_script = getArticleJsonScript();
          if (json_script) {
            let json = JSON.parse(json_script.text);
            if (json) {
              let json_text = parseHtmlEntities(json.articleBody).replace(/\n/g, '$&\r\n'),
                article = document.querySelector('div.c-post-text');
              json_text && article && (article.innerText = json_text);
            }
          }
        }
      } else if (matchDomain('foreignaffairs.com')) {
        removeDOMElement(
          document.querySelector('.paywall'),
          document.querySelector('.loading-indicator'),
          document.querySelector('.messages--container--bottom')
        );
        let article_dropcap = document.querySelectorAll('.article-dropcap');
        for (let elem of article_dropcap) elem.classList.add('loaded');
      } else if (matchDomain('foreignpolicy.com')) {
        let content_ungated = document.querySelector('div.content-ungated');
        if (content_ungated) {
          removeDOMElement(content_ungated);
          let content_gated = document.querySelector('div.content-gated');
          content_gated &&
            (document.querySelector('body.is-fp-insider')
              ? (getJsonUrl(
                  'div.content-gated',
                  { rm_class: 'content-gated' },
                  'div.content-gated'
                ),
                window.setTimeout(function () {
                  let lazy_images = document.querySelectorAll('img[loading="lazy"]');
                  for (let elem of lazy_images) elem.removeAttribute('loading');
                }, 1e3))
              : content_gated.classList.remove('content-gated'));
        }
        let audio_tts_overlay = document.querySelector(
          'div.fp-audio-embed--disabled > div.share-dropdown > span.bigfoot-footnote__container'
        );
        audio_tts_overlay &&
          (audio_tts_overlay.parentNode.removeAttribute('class'),
          removeDOMElement(audio_tts_overlay));
      } else if (matchDomain('fortune.com'))
        hideDOMStyle(
          'div.leaderboard-ad-wrapper-parent, div.leading-0, div.z-body-popover, div.nativo-wrapper, div#outbrain-container, div[class^="sc-"]:has(> div[data-cy^="in-stream-"], > div > div[data-google-query-id]), div[class^="min-h-"]:has(> div#article-piano-template-plea-container)'
        );
      else if (matchDomain('foxnews.com')) {
        removeDOMElement(document.querySelector('div.article-gating-wrapper'));
        let overlay = document.querySelector('div[class*="gated-overlay"]');
        overlay && overlay.removeAttribute('class');
      } else if (matchDomain('ftm.eu')) {
        let videos = document.querySelectorAll('div.body > div.video-pp');
        for (let video of videos) {
          let video_id_dom = video.querySelector('a.video[data-youtube-id]');
          video_id_dom &&
            ((video_new = document.createElement('iframe')),
            (video_new.src =
              'https://www.youtube.com/embed/' + video_id_dom.getAttribute('data-youtube-id')),
            (video_new.style = 'width: 95%; height: 400px; margin: 0px 20px;'),
            video.parentNode.replaceChild(video_new, video));
        }
        let audio_controls = document.querySelectorAll('audio[controls][style]');
        for (let elem of audio_controls) elem.removeAttribute('style');
        (document.querySelectorAll('div.foldable').forEach((e) => e.classList.remove('foldable')),
          hideDOMStyle('div.banner-pp'));
      } else if (matchDomain(['haaretz.co.il', 'haaretz.com', 'themarker.com'])) {
        if (window.location.pathname.match(/\/ty-article(-\w+)?\//)) {
          let body_wrapper_sel = 'section[data-testid="article-body-wrapper"]',
            paywall_sel =
              'div[data-test="paywallMidpage"], ' +
              body_wrapper_sel +
              ' div[data-testid="logo-loading-indicator"]',
            article_sel = 'main',
            article_link_sel =
              'main header, main.article-page p:not([id]), h1#article-header, ' + body_wrapper_sel,
            url = window.location.href;
          if (mobile) {
            let paywall = document.querySelector(paywall_sel);
            if (paywall) {
              removeDOMElement(paywall);
              let article_link = document.querySelector(article_link_sel);
              article_link && article_link.before(archiveLink(url));
            }
          } else
            ((func_post = function () {
              hideDOMStyle(
                'section[data-testid="zoidberg-list"], section#comments-section, div[old-position="sticky"]'
              );
            }),
              getArchive(url, paywall_sel, '', article_sel, '', article_sel, article_link_sel));
        } else
          window.location.pathname.startsWith('/hblocked') &&
            window.location.search.startsWith('?returnTo=') &&
            header_nofix(
              'main',
              '',
              'BPC > return to article',
              decodeURIComponent(window.location.search.split('?returnTo=')[1])
            );
        let history_keys = Object.keys(window.localStorage).filter((x) =>
          x.match(/^(reading(Count)?History|raData)/i)
        );
        for (let item of history_keys) window.localStorage.removeItem(item);
        hideDOMStyle('div[data-testid="static-ad-slot"], div.fe-button-ruler');
      } else if (matchDomain('harpers.org')) {
        setCookie('hr_session', '', 'harpers.org', '/', 0);
        let paywall = document.querySelector('div#full-paywall');
        if (paywall) {
          (paywall.removeAttribute('id'),
            header_nofix(paywall, '', 'BPC > no fix (for older articles archive may work)'));
          let url = window.location.href;
          paywall.before(archiveLink(url));
        }
        hideDOMStyle('div#ctas');
      } else if (matchDomain('hbr.org'))
        if (window.location.pathname.startsWith('/data-visuals')) {
          let feed_entries = document.querySelectorAll('li.feed-entry');
          for (let feed_entry of feed_entries) {
            let download = feed_entry.querySelector('span.entry-download > a[href*="/undefined/"]');
            if (download) {
              let figure = feed_entry.querySelector('figure.entry-image > img[src]');
              figure && ((download.href = figure.src), download.setAttribute('download', ''));
            }
          }
        } else {
          removeDOMElement(document.querySelector('.persistent-banner'));
          let paywall = document.querySelector('div[id^="paywall"]');
          paywall &&
            (removeDOMElement(paywall),
            insert_script(function () {
              window.top.postMessage({ type: 'article-paywall:full-content' }, '*');
            }));
        }
      else if (matchDomain('hilltimes.com')) {
        function hilltimes_main(node) {
          getJsonUrl('div.paywallcont', '', 'div#entry-content');
        }
        let paywall_sel = 'div.paywallcont';
        (document.querySelector(paywall_sel)
          ? hilltimes_main()
          : waitDOMElement(paywall_sel, 'DIV', hilltimes_main, !1),
          hideDOMStyle('section.hide_this_section'));
      } else if (matchDomain('hindustantimes.com'))
        if (window.location.pathname.endsWith('-amp.html'))
          hideDOMStyle(
            'div[class^="sub-paywall-version"], section[amp-access="NOT userSubscribed"], div.storyAd'
          );
        else {
          let paywall = document.querySelector('.paywall');
          (paywall && paywall.classList.remove('paywall'),
            document.querySelectorAll('.hide').forEach((e) => e.classList.remove('hide')));
          let trc_overlay = document.querySelector(
            'section.trc_related_container > div[role="button"]'
          );
          trc_overlay && trc_overlay.click();
          let body_sticky = document.querySelector('body.desktopAdSticky');
          (body_sticky && body_sticky.classList.remove('desktopAdSticky'),
            hideDOMStyle('div.paywall-container, div[class^="adHeight"], div.desktopAd'));
        }
      else if (matchDomain('historyextra.com'))
        hideDOMStyle('div.ad-slot, div.ad-banner, div.stitcher-ad--dai-placeholder');
      else if (matchDomain('historytoday.com'))
        if (window.location.hostname.startsWith('app.')) restorePugpigPage();
        else {
          let paywall = document.querySelector('div.nopremium-message');
          if (paywall) {
            let app_link = document.createElement('a'),
              app_url = 'app.historytoday.com';
            ((app_link.href = 'https://' + app_url),
              (app_link.innerText = 'BPC > no fix, search article on: ' + app_url),
              (app_link.style = 'color: red; font-weight: bold;'));
            let app_div = document.createElement('div');
            ((app_div.style = 'margin: 20px;'),
              app_div.appendChild(app_link),
              paywall.before(app_div),
              removeDOMElement(paywall));
          }
        }
      else if (matchDomain('inc.com')) {
        let paywall = document.querySelector('div.paywall');
        if (paywall) {
          removeDOMElement(paywall);
          let locked = document.querySelector('div.article-container--locked');
          locked && (locked.className = 'article-container');
        }
        hideDOMStyle('div.second-scroll-border');
      } else if (matchDomain('inc42.com'))
        window.location.pathname.endsWith('/amp/')
          ? amp_unhide_access_hide('="status"', '="NOT status"', 'div.wru-widget')
          : removeDOMElement(document.querySelector('div[id*="_leaderboard_"]'));
      else if (matchDomain('indianexpress.com'))
        window.location.pathname.endsWith('/lite/')
          ? amp_unhide_access_hide('="metering.result=\'ALLOW_ACCESS\'"')
          : hideDOMStyle(
              'div[class^="adsbox"], div.adboxtop, div.add-first, div.osv-ad-class, div.ie-int-campign-ad'
            );
      else if (matchDomain('indiatoday.in'))
        window.location.pathname.match(/(\/amp)?\/magazine\//) &&
          (window.location.href.includes('/amp/')
            ? amp_unhide_access_hide(
                '="granted"',
                '="NOT NOT granted"',
                'div[class^="wrapper_main-container_"], div.readmore__box'
              )
            : amp_redirect('div#csc-paywall'));
      else if (matchDomain('infzm.com')) {
        let url = window.location.href;
        if (url.includes('.com/wap/#/content/')) {
          let container = document.querySelector('section.container');
          (container && container.classList.remove('container'),
            getArchive(url, 'div.infzm-content__payment-prompt', '', 'article'));
        } else if (url.includes('.com/contents/')) {
          let paywall = document.querySelector('div.nfzm-article-jumbotron--unauth');
          paywall &&
            (removeDOMElement(paywall),
            (window.location.href = url
              .replace('.com/contents/', '.com/wap/#/content/')
              .split('?')[0]));
        }
      } else if (matchDomain('inkl.com')) {
        let url = window.location.href;
        if (url.includes('/signin?') && url.includes('redirect_to='))
          window.setTimeout(function () {
            window.location.href =
              'https://www.inkl.com' + decodeURIComponent(url.split('redirect_to=')[1]);
          }, 500);
        else {
          let paywall = document.querySelector('div.paywall');
          (paywall &&
            (paywall.removeAttribute('class'),
            removeDOMElement(document.querySelector('div.gradient-container'))),
            removeDOMElement(
              document.querySelector('.what-is-inkl-container, .features-panel'),
              ...document.querySelectorAll(
                '.article-signup-container, .locked-sign-up-container, div[class*="/inkl-watermark.svg"]'
              ),
              document.querySelector('div.shared-article-inline-banner')
            ));
          let dismiss_button = document.querySelector('div.dismiss-button-container button.btn');
          dismiss_button && dismiss_button.click();
          let dive_deeper_summary_bodies = document.querySelectorAll(
            'div.dive-deeper-container div.summary-body'
          );
          if (dive_deeper_summary_bodies)
            for (let summary_body of dive_deeper_summary_bodies)
              if (!summary_body.querySelector('a')) {
                let ng_click = summary_body
                    .getAttribute('ng-click')
                    .replace("showArticle('", '')
                    .replace("')", ''),
                  weblink = document.createElement('a');
                ((weblink.text = 'open'),
                  (weblink.href = 'https://www.inkl.com/news/' + ng_click),
                  summary_body.appendChild(weblink));
              }
        }
      } else if (matchDomain('inquirer.com')) {
        let audio_paywall = document.querySelector('button.audio-paywall-trigger');
        if (audio_paywall) {
          let audio = audio_paywall.parentNode.querySelector('audio[src]');
          audio &&
            (audio.setAttribute('controls', ''),
            audio_paywall.parentNode.parentNode.replaceChild(audio, audio_paywall.parentNode));
        }
        hideDOMStyle('div[data-ad-name]');
      } else if (matchDomain('insidehighered.com'))
        hideDOMStyle(
          'div[id^="block-dfptag"], div.wp-block-ihe-ad, section.section-ad_slot, div#roadblock'
        );
      else if (matchDomain('insights.citeline.com')) {
        let paywall = document.querySelector('div.paywall-wrap'),
          article = document.querySelector('article.b-article-body');
        if (paywall && article) {
          removeDOMElement(paywall);
          let fusion_script = document.querySelector('script#fusion-metadata');
          if (fusion_script && fusion_script.text.includes('Fusion.contentCache='))
            try {
              let json = JSON.parse(
                fusion_script.text.split('Fusion.contentCache=')[1].split(';Fusion.')[0]
              );
              if (json && json['content-api']) {
                let pars,
                  content_api = json['content-api'];
                for (let key in content_api) {
                  let content = content_api[key];
                  if (
                    content.data &&
                    content.data.content_elements &&
                    content.data.content_restrictions
                  ) {
                    pars = content.data.content_elements;
                    break;
                  }
                }
                if (pars && pars.length) {
                  article.innerHTML = '';
                  let parser = new DOMParser();
                  for (let par of pars) {
                    let elem;
                    if (['header', 'text'].includes(par.type))
                      par.content &&
                        ((elem = parser
                          .parseFromString(
                            '<p class="c-paragraph">' + par.content + '</p>',
                            'text/html'
                          )
                          .querySelector('p')),
                        'header' === par.type && (elem.style = 'font-weight: bold;'));
                    else if ('image' === par.type) {
                      if (par.url) {
                        let caption_text = par.caption;
                        (par.credits &&
                          par.credits.affiliation &&
                          par.credits.affiliation[0] &&
                          par.credits.affiliation[0].name &&
                          (caption_text += ' (' + par.credits.affiliation[0].name + ')'),
                          (elem = makeFigure(par.url, caption_text)));
                      }
                    } else if ('raw_html' === par.type) {
                      elem = parser
                        .parseFromString(
                          '<div>' + parseHtmlEntities(par.content) + '</div>',
                          'text/html'
                        )
                        .querySelector('div');
                      let infogram = elem.querySelector('div.infogram-embed[data-id]');
                      if (infogram) {
                        elem.innerHTML = '';
                        let iframe = document.createElement('iframe');
                        ((iframe.src =
                          'https://e.infogram.com/' + infogram.getAttribute('data-id')),
                          (iframe.style = 'width: 100%; aspect-ratio: 4 / 3;'),
                          elem.appendChild(iframe));
                      } else
                        elem
                          .querySelectorAll('iframe[allow*="fullscreen"][allowfullscreen]')
                          .forEach((e) => e.removeAttribute('allowfullscreen'));
                    } else if ('custom_embed' === par.type) {
                      let html = getNestedKeys(par, 'embed.config.html');
                      if (html) {
                        ((elem = document.createElement('div')),
                          (elem.className = 'key-takeaways align-right'));
                        let headline = getNestedKeys(par, 'embed.config.headline');
                        if (headline) {
                          let embed_headline = document.createElement('h6');
                          ((embed_headline.innerText = headline), elem.appendChild(embed_headline));
                        }
                        let doc = parser.parseFromString(html, 'text/html'),
                          elem_type_match = html.match(/^<(\w+)/);
                        if (elem_type_match) {
                          let elem_type = elem_type_match[1],
                            embed_content = doc.querySelector(elem_type);
                          embed_content && elem.appendChild(embed_content);
                        }
                      }
                    } else ['divider'].includes(par.type);
                    elem && article.appendChild(elem);
                  }
                }
              }
            } catch (err) {}
        }
        hideDOMStyle('div[id^="arcad-"]');
      } else if (matchDomain('interestingengineering.com'))
        (addStyle(
          'body {overflow: auto !important; position: relative !important; top: unset !important;}'
        ),
          hideDOMStyle('main > div[class*="t-hidden"], div.t-bg-black'));
      else if (matchDomain('investors.com')) {
        func_post = function () {
          let videos = document.querySelectorAll('div.jwp-placement[data-jw-video_url]');
          for (let video of videos) {
            let video_new = document.createElement('video');
            ((video_new.src = video.getAttribute('data-jw-video_url')),
              video_new.setAttribute('controls', ''),
              (video_new.style = 'width: 100%;'),
              video.parentNode.replaceChild(video_new, video));
          }
        };
        let paywall_sel = 'div.investors-paywall-overlay';
        (document.querySelector(paywall_sel) &&
          (setCookie('__tbc', '', 'investors.com', '/', 0),
          getJsonUrl(paywall_sel, '', 'div.investors-paywall-excerpt')),
          hideDOMStyle('div.ads, div.headerAds'));
      } else if (matchDomain('ipolitics.ca')) {
        let login = document.querySelector('div.login');
        if (login) {
          removeDOMElement(login);
          let json_script = document.querySelector('script#__NEXT_DATA__');
          if (json_script)
            try {
              let json = JSON.parse(json_script.innerText);
              if (json && json.props.pageProps.post && json.props.pageProps.post.content) {
                let url_next = json.props.pageProps.post.slug;
                url_next && !window.location.pathname.includes(url_next) && refreshCurrentTab();
                let article_new = json.props.pageProps.post.content,
                  article = document.querySelector('.post-body');
                if (article) {
                  ((article.innerHTML = ''), article.classList.remove('locked'));
                  let content_new = new DOMParser()
                    .parseFromString('<div>' + article_new + '</div>', 'text/html')
                    .querySelector('div');
                  article.appendChild(content_new);
                }
              } else refreshCurrentTab();
            } catch (err) {}
        }
      } else if (matchDomain('japantimes.co.jp'))
        (setCookie('xbc', '', 'japantimes.co.jp', '/', 0),
          getArchive(window.location.href, 'div.blocker', '', 'div#jtarticle'),
          removeDOMElement(document.querySelector('div.subscribe')),
          hideDOMStyle('div.DisplayAd'));
      else if (matchDomain('jpost.com'))
        removeDOMElement(
          ...document.querySelectorAll('.hide-for-premium, #hiddenPremiumForm, #hiddenLink')
        );
      else if (matchDomain('kathimerini.gr')) hideDOMStyle('div.nx-billboard-ad-row');
      else if (matchDomain('kompas.id')) {
        let url_artikel = window.location.pathname.startsWith('/artikel/'),
          delay = url_artikel ? 2500 : 0;
        window.setTimeout(function () {
          let paywall = document.querySelector('kompasid-paywall');
          if (paywall)
            if ((removeDOMElement(paywall), url_artikel)) {
              removeDOMElement(document.querySelector('div.paywall'));
              let art_hidden = document.querySelector('div.non-paywall[style]');
              art_hidden && art_hidden.removeAttribute('style');
              let audio_tts = document.querySelector('button.button-play-audio');
              if (audio_tts) {
                let audio_src_dom = document.querySelector('audio > source[src$=".mp3"]');
                if (audio_src_dom) {
                  let audio_new = document.createElement('audio');
                  ((audio_new.src = audio_src_dom.src),
                    audio_new.setAttribute('controls', ''),
                    audio_tts.parentNode.replaceChild(audio_new, audio_tts));
                }
              }
              let gallery = document.querySelector('div.mx-auto > div > div.swiper-free-mode');
              if (gallery) {
                let article = gallery.parentNode.parentNode;
                article.innerHTML = '';
                let json_script = document.querySelector('script#__NUXT_DATA__');
                if (json_script)
                  try {
                    let pars = JSON.parse(json_script.text),
                      img_pars = pars.filter(
                        (x) => x && 'object' == typeof x && x.src && x.caption
                      );
                    for (let elem of img_pars) {
                      let figure = makeFigure(
                        pars[elem.src],
                        pars[elem.caption] + (elem.credit ? ' ' + pars[elem.credit] : ''),
                        { alt: pars[elem.alt] }
                      );
                      ((figure.style = 'width: 80%; margin: 50px;'), article.appendChild(figure));
                    }
                  } catch (err) {}
              }
            } else {
              let art_hidden = document.querySelector('section.paywall.hidden');
              art_hidden && art_hidden.classList.remove('hidden');
            }
        }, delay);
      } else if (matchDomain('latimes.com')) {
        let subscribers = pageContains('div.infobox > p.infobox-title', /subscribers/i);
        (subscribers.length && removeDOMElement(subscribers[0].parentNode),
          hideDOMStyle('div.google-dfp-ad-wrapper, div.revcontent'));
      } else if (matchDomain('livelaw.in')) {
        let amp = window.location.pathname.startsWith('/amp/'),
          paywall = document.querySelector(
            amp ? 'div.subscribeNow' : 'div#subscription_paid_message'
          );
        if (paywall) {
          removeDOMElement(
            paywall,
            document.querySelector(amp ? 'div.story' : 'div.details-story-wrapper')
          );
          let paywall_content = document.querySelector('div.paywall-content.hide');
          if (paywall_content)
            if (paywall_content.innerText.length)
              paywall_content.className = amp ? '' : 'news_details_page_row2 details-story-wrapper';
            else if (!amp) {
              let amphtml = document.querySelector('head > link[rel="amphtml"]');
              amphtml && amp_redirect_not_loop(amphtml);
            }
        }
        hideDOMStyle('inside-post-ad, amp-ad');
      } else if (matchDomain('livemint.com'))
        if (window.location.pathname.includes('/amp-'))
          removeDOMElement(...document.querySelectorAll('[amp-access="NOT subscribed"]'));
        else {
          let paywall = document.querySelector('div.paywall');
          (paywall && paywall.classList.remove('paywall'),
            removeDOMElement(
              ...document.querySelectorAll('div[class^="storyPaywall_paywallStory_"]')
            ),
            hideDOMStyle('[class^="ad"], [id^="ad"], #subscribeAd, .taboolaHeight'));
        }
      else if (matchDomain('magazine.atavist.com')) {
        removeDOMElement(
          document.querySelector('div.bottom-notification'),
          document.querySelector('div.notification-overlay')
        );
        let paywall = document.querySelector('body.paywall-notification-visible');
        (paywall && paywall.classList.remove('paywall-notification-visible'),
          window.localStorage.clear());
      } else if (matchDomain('manoramaonline.com'))
        window.location.pathname.endsWith('.amp.html')
          ? amp_unhide_access_hide('="result=\'ALLOW_ACCESS\'"')
          : amp_redirect('ev-engagement');
      else if (matchDomain('marketwatch.com')) {
        if (window.location.pathname.startsWith('/livecoverage/'))
          window.setTimeout(function () {
            fix_dowjones_live();
          }, 1500);
        else {
          let paywall = document.querySelector('div#cx-snippet-container');
          paywall &&
            (removeDOMElement(paywall),
            window.setTimeout(function () {
              let article = document.querySelector('section[class*="-Container"]');
              if (article) {
                addStyle('section[class*="-Container"]::after {background: none !important;}');
                let article_id_dom = document.querySelector(
                  'head > meta[name="article.id"][content]'
                );
                article_id_dom &&
                  getExtFetch(
                    'https://mwatch.djmedia.djservices.io/apps/marketwatch/theaters/article?screen_ids=' +
                      article_id_dom.content,
                    '',
                    {
                      headers: {
                        'app-identifier': 'com.news.screens',
                        'device-type': 'phone',
                        'x-access-token':
                          'eyJ0eXAiOiJKV1QiLCJhbGciOiJSUzI1NiJ9.eyJBVVRIX1BIUkFTRV9QUk9EX0FORFJPSUQiOiJjWXhLbURPOENOcm9wOEZINmtkTyJ9.xozUBM2fH4fY5JvdSrpvesZHeNFqS9aIFSZdXsvty5tgE1qb5w26I5Fe-FGQTYQdfPCQmJ60dh-QsxPKmsm3J0l50zF5CiiTAzmvD5D_XWkV_sa_L5eMqLvLyV2Wcrld9-LhKfmyqQlqDSU79276_sgo6yxDA2xqKDzFzwoOj3OILFKdkXvZNbN9NGapoTEV6z8CWt2nOxnBsN-_cXjpeDE_Al2EgWtSLA32nlRcbJ9Nm4ZRpZzCr5AKaUtysnjiAFpWyp1MSnYMJEZImYbt7nm9k2qd3ResXx0vZu-PZkr9bi4zprCUmvbLzopRbVbDwcDJowWpwyo3DnrjOhBKgw',
                      },
                    },
                    fix_dowjones_fetch,
                    [article, 'div.img__lead img[src]']
                  );
              }
            }, 2e3));
        }
        hideDOMStyle('div.element--ad, div.j-ad, div.adWrapper, div#cx-articlecover');
      } else if (matchDomain('medscape.com'))
        hideDOMStyle('div.text-ad-unit, div[id^="ads-"], div.adswrapper');
      else if (matchDomain('mexiconewsdaily.com')) {
        let div_hidden = document.querySelector(
          'body.single div.td-post-content > div.tdb-block-inner'
        );
        div_hidden && div_hidden.classList.remove('tdb-block-inner');
      } else if (matchDomain('mid-day.com'))
        if (window.location.pathname.startsWith('/amp/'))
          amp_unhide_access_hide(
            '="granted"',
            '="NOT granted"',
            '[class*="BannerAd"], div.midday-wrapper'
          );
        else {
          amp_redirect('div#myModalFullscreen');
          let read_more = document.querySelector('#read-more-my');
          read_more && read_more.click();
        }
      else if (matchDomain('nationalgeographic.com')) {
        function natgeo_func(node) {
          removeDOMElement(node);
          let body = document.querySelector('body[class]');
          body && (body.removeAttribute('class'), body.removeAttribute('style'));
        }
        let paywall = document.querySelector('div[id^="fittPortal"]');
        (paywall && natgeo_func(paywall),
          waitDOMElement('div[id^="fittPortal"]', 'DIV', natgeo_func, !1),
          window.setTimeout(function () {
            let url = window.location.href,
              subscribed = document.querySelector('div.Article__Content--gated'),
              msg = document.querySelector('div#bpc_archive');
            subscribed &&
              !msg &&
              (subscribed.appendChild(archiveLink(url)),
              subscribed.setAttribute('style', 'overflow: visible !important;'));
            let overlay = document.querySelector('div.Article__Content__Overlay--gated');
            overlay && overlay.classList.remove('Article__Content__Overlay--gated');
          }, 2e3),
          hideDOMStyle('div.ad-slot, div.InsertedAd, div.natgeo-ad'));
      } else if (matchDomain('nationalreview.com')) {
        if (!window.location.pathname.endsWith('/amp/')) {
          let paywall_sel = 'div.continue-reading',
            paywall = document.querySelector(paywall_sel);
          if (paywall) {
            let amphtml = document.querySelector('head > link[rel="amphtml"][href]');
            amphtml && !amphtml.href.includes(window.location.pathname)
              ? (removeDOMElement(paywall), refreshCurrentTab())
              : getJsonUrl(paywall_sel, '', 'div.article-content', {
                  art_class: 'article-content article-content--headless',
                });
          }
        }
        hideDOMStyle(
          'div.zephr-wrapper, div#bc-root, div.cookie-text, amp-ad, .ad-unit, .ad-skeleton, amp-connatix-player, div[class*="-connatix-"]'
        );
      } else if (matchDomain('nature.com'))
        ((func_post = function () {
          header_nofix('article', 'h2#access-options', 'BPC > no archive-fix');
        }),
          getArchive(
            window.location.href,
            'div.app-access-wall, h2#access-options',
            '',
            'article'
          ));
      else if (matchDomain('nautil.us'))
        (window.setTimeout(function () {
          let adm_button = document.querySelector('div[class^="a__sc-np"] button');
          adm_button && adm_button.click();
        }, 500),
          hideDOMStyle('div.article-ad, div[class^="PrimisVideoAdBlock_wrapper__"]'));
      else if (matchDomain('ndtvprofit.com'))
        (window.location.pathname.startsWith('/amp/')
          ? amp_unhide_subscr_section()
          : window.setTimeout(function () {
              amp_redirect('div[class^="hard-paywall"], div[class*="geotag-container_"]');
            }, 1e3),
          hideDOMStyle('div.responsive-ad'));
      else if (matchDomain('newcriterion.com'))
        getJsonUrl('div.paywall-overlay', '', 'div.entry-content');
      else if (matchDomain('newrepublic.com'))
        (removeDOMElement(
          document.querySelector('div.article-scheduled-modal'),
          document.querySelector('div#pwPopups')
        ),
          hideDOMStyle('.ad-unit'));
      else if (matchDomain('newscientist.com'))
        if (window.location.hostname.startsWith('appan.')) restorePugpigPage();
        else {
          let paywall = document.querySelector('section#subscription-barrier');
          if (paywall) {
            removeDOMElement(paywall);
            let json_script = document.querySelector('script#ns-seo-schema');
            if (json_script)
              try {
                let json = JSON.parse(json_script.text);
                if (json && json.datePublished) {
                  let date = json.datePublished.split(/T\d/)[0].replace(/-/g, '/'),
                    path_new = window.location.pathname.split(/\/article\/(\d+-|mg\d+-\d+-)/)[2];
                  path_new &&
                    ((func_post = function () {
                      let lazy_images = document.querySelectorAll('img[src^="../"][data-src]');
                      for (let elem of lazy_images)
                        ((elem.src = elem.getAttribute('data-src')),
                          elem.removeAttribute('height'),
                          elem.removeAttribute('width'));
                      let videos = document.querySelectorAll(
                        'figure > div.pugpig-video[data-video-url]'
                      );
                      for (let elem of videos) {
                        let iframe = document.createElement('iframe');
                        ((iframe.src = elem.getAttribute('data-video-url')),
                          (iframe.style = 'width: 100%; height: 400px; margin: 20px 0px;'),
                          elem.parentNode.replaceChild(iframe, elem));
                      }
                    }),
                    replaceDomElementExt(
                      'https://appan.newscientist.com/' + date + '/' + path_new + 'content.html',
                      !1,
                      !1,
                      'section.ArticleContent',
                      'BPC > no fix (source file)',
                      'section[class$="-article__body"]'
                    ));
                }
              } catch (err) {}
          }
          hideDOMStyle('div[class*="Advert"]');
        }
      else if (matchDomain('newsday.com')) {
        let listen_button = document.querySelector(
          'div.listen-button > button[disabled], div.listen-button > div.loader'
        );
        if (listen_button) {
          let audio_src_dom = listen_button.parentNode.querySelector('audio > source[src]');
          if (audio_src_dom) {
            let audio = document.createElement('audio');
            ((audio.src = audio_src_dom.src),
              audio.setAttribute('controls', ''),
              listen_button.parentNode.replaceChild(audio, listen_button));
          }
        }
        let nd_lock = document.querySelector('html[class]');
        (nd_lock && nd_lock.removeAttribute('class'), hideDOMStyle('div.ad'));
      } else if (matchDomain('newslaundry.com')) {
        let paywall = document.querySelector('div > div > img[alt^="paywall"]');
        if (paywall) {
          let banner = document.querySelector('div.FrsvM');
          removeDOMElement(paywall.parentNode.parentNode, banner);
          let json_script = getArticleJsonScript();
          if (json_script) {
            let json = JSON.parse(json_script.text);
            if (json) {
              let json_text = parseHtmlEntities(json.articleBody).replace(/<\/p>\./g, '</p>'),
                article = document.querySelector('div.story-element-text');
              if (json_text && article) {
                let article_new = new DOMParser()
                  .parseFromString('<div>' + json_text + '</div>', 'text/html')
                  .querySelector('div');
                ((article.innerHTML = ''), article.appendChild(article_new));
              }
            }
          }
          let video = document.querySelector('a:has(figure)');
          if (video) {
            let video_script,
              scripts = document.querySelectorAll('script[type="application/ld+json"]');
            for (let script of scripts)
              if (script.innerText.includes('"embedUrl":"')) {
                video_script = script;
                break;
              }
            if (video_script) {
              let video_new = document.createElement('iframe');
              ((video_new.src = video_script.innerText.split('"embedUrl":"')[1].split('"')[0]),
                (video_new.style = 'width: 100%; aspect-ratio: 16 / 9;'),
                video_new.setAttribute('frameborder', 0),
                video.parentNode.replaceChild(video_new, video));
            }
          }
        }
        hideDOMStyle('div.story-card div[role="dialog"]');
      } else if (matchDomain('newsweek.com')) hideDOMStyle('div#topad, div[id^="dfp-ad-"]');
      else if (matchDomain(['nola.com', 'shreveportbossieradvocate.com', 'theadvocate.com']))
        if (window.location.pathname.endsWith('.amp.html')) {
          let body_hidden = document.querySelector('.site-container');
          body_hidden && body_hidden.setAttribute('style', 'display:block;');
        } else hideDOMStyle('div.tnt-ads-container, div.asset-breakout-ads');
      else if (matchDomain('nv.ua'))
        if (window.location.pathname.includes('/amp/')) {
          let paywall = document.querySelector('div.paywall-area');
          paywall &&
            (paywall.removeAttribute('class'),
            removeDOMElement(paywall.querySelector('div.make-subscription')));
          let article = document.querySelector('div.article__content');
          article && article.removeAttribute('class');
        } else amp_redirect('div[id^="media_paywall"]');
      else if (matchDomain('nybooks.com')) {
        let paywall_article = document.querySelector('.paywall-article');
        (paywall_article && paywall_article.classList.remove('paywall-article'),
          hideDOMStyle('div.toast-cta, div.inline-ad'));
      } else if (matchDomain('nypost.com')) {
        if (window.location.pathname.includes('/sports/')) {
          let paywall = document.querySelectorAll('head > meta[content*="Sports Plus"]');
          if (paywall.length) {
            removeDOMElement(...paywall);
            let article_sel = 'div.single__content',
              article = document.querySelector(article_sel);
            if (article)
              if (
                (article.classList.remove('entry-content-exclusive-covered'),
                window.navigator.userAgent.toLowerCase().includes('chrome'))
              )
                header_nofix(article);
              else {
                let json_url_dom = document.querySelector(
                  'head > link[rel="alternate"][type="application/json"][href]'
                );
                if (json_url_dom) {
                  let match = json_url_dom.href.match(/\d+$/);
                  if (match) {
                    let footer = article.querySelector('div.single__footer');
                    (getExtFetch(
                      'https://nypost.nypost.djservices.io/apps/nypost-v3/theaters/sports?screen_ids=' +
                        match[0],
                      '',
                      {
                        headers: {
                          'app-identifier': 'com.news.screens',
                          'device-type': 'phone',
                          'x-access-token':
                            'eyJ0eXAiOiJKV1QiLCJhbGciOiJSUzI1NiJ9.eyJBVVRIX1BIUkFTRV9QUk9EX0FORFJPSUQiOiJGNGYzYnhzZGlIa1M1QzVIWUxUNSJ9.OGooBqisVQznx2FDxZpHAqWu-oG1wPJLUxYBMd3xOKiLakoR2XQC2hXnyS3zb1Dw6AkKcMhWyZfRerUckFjga8Ii59lZtP8Xt92iCUyJjs0yjmLJgkZWEGqZ8szM2UK7jZJ5SsHaYfe2V3c7KrtfbY74aWGwddvg-Ex4O5hwv3aiAJpI_aZMFqniClGM2pYbGNSPxH-I0PMuwTzt-oyofhQsRJWOQES8fmQz1H-opqQVr3B0-ev4MxYZjfk8kKnXxRbf7tDbQvastQh8kLe3KN90ptSp_LWbkPgo0G8Vw9Jzxy1TUd_VNgTE21uNdRAoZbxDly7aw-9CTDtb1OtXVg',
                          'user-agent': 'okhttp/4.12.0',
                        },
                      },
                      fix_dowjones_fetch,
                      [article, 'div.featured-image img[src]']
                    ),
                      article
                        .querySelectorAll('p[class]')
                        .forEach((e) => e.removeAttribute('class')),
                      footer &&
                        window.setTimeout(function () {
                          article.appendChild(footer);
                        }, 1e3));
                  }
                }
              }
          }
        }
        hideDOMStyle('div.ad');
      } else if (matchDomain('nysun.com')) {
        let paywall = document.querySelector('p > a[href^="/login?"]');
        if (paywall) {
          removeDOMElement(paywall);
          let article = document.querySelector('div.article-wrapper > div:last-child');
          if (article) {
            let json_script = getArticleJsonScript();
            if (json_script) {
              let json = JSON.parse(json_script.text);
              if (json) {
                let json_text = json.articleBody;
                if (json_text) {
                  article.innerHTML = '';
                  let article_new = document.createElement('div');
                  ((article_new.innerText = breakText(json_text)),
                    article.appendChild(article_new));
                }
              }
            }
          }
        }
      } else if (matchDomain('nytimes.com'))
        hideDOMStyle(
          'div#top-wrapper, div#bottom-wrapper, div#dock-container, div[class$="ad-wrapper"], div[class^="adunit_"], div[data-testid^="Dropzone-"], div[class^="css-"]:has( > div[data-testid="StandardAd"])'
        );
      else if (matchDomain('on3.com')) {
        let url = window.location.href,
          paywall_sel = 'div[data-template-type="barrier"]';
        (window.setTimeout(function () {
          getArchive(url, paywall_sel + '[class]', { rm_attrib: 'class' }, 'article');
          let noscroll = document.querySelectorAll('html.scroll-lock, body.scroll-lock');
          for (let elem of noscroll) (elem.removeAttribute('class'), elem.removeAttribute('style'));
        }, 0),
          hideDOMStyle('div#blocker, div[data-ui="ad"], ' + paywall_sel));
      } else if (matchDomain('outlookbusiness.com')) {
        let paywall = document.querySelector('div#csc-paywall');
        if (paywall) {
          removeDOMElement(paywall);
          let json_script = document.querySelector('script#__NEXT_DATA__');
          if (json_script) {
            let json = JSON.parse(json_script.innerText);
            if (
              json &&
              json.props.initialState.dashboard.ARTICLE_POST_DETAIL_API.data.article_data
                .description
            ) {
              let article_new =
                  json.props.initialState.dashboard.ARTICLE_POST_DETAIL_API.data.article_data
                    .description,
                article = document.querySelector('div.story-content');
              if (article) {
                article.innerHTML = '';
                let content_new = new DOMParser()
                  .parseFromString('<div>' + article_new + '</div>', 'text/html')
                  .querySelector('div');
                article.appendChild(content_new);
              }
            }
          }
        }
      } else if (matchDomain('outlookindia.com')) {
        let paywall = document.querySelector('div.paywall');
        if (paywall) {
          removeDOMElement(paywall);
          let json_script = getArticleJsonScript();
          if (json_script) {
            let json = JSON.parse(json_script.text);
            if (json) {
              let json_text = parseHtmlEntities(json.articleBody).replace(/\n/g, '$&\r\n'),
                content = document.querySelector('div#articleBody');
              if (json_text && content) {
                content.innerHTML = '';
                let article_new = document.createElement('p');
                ((article_new.innerText = json_text), content.appendChild(article_new));
              }
            }
          }
        }
      } else if (matchDomain('philonomist.com')) {
        let paywall = document.querySelector('div.content-bandeau');
        if (paywall && window.location.pathname.startsWith('/en/')) {
          removeDOMElement(paywall);
          let json_script = getArticleJsonScript();
          if (json_script) {
            let json = JSON.parse(json_script.text);
            if (json) {
              let json_text = parseHtmlEntities(findKeyJson(json, ['articleBody'])).replace(
                  /\s{2,}/g,
                  '\r\n\r\n'
                ),
                article = document.querySelector('div.main-body');
              if (json_text && article) {
                let par_last = article.querySelector('div > p:last-child');
                if (par_last) {
                  let par_last_str = par_last.innerText.substring(0, 50);
                  if (json_text.replace(/<[^<]*>/g, '').includes(par_last_str))
                    par_last.innerText = json_text.substring(json_text.indexOf(par_last_str));
                  else {
                    article.innerHTML = ' ';
                    let article_new = document.createElement('p');
                    ((article_new.innerText = json_text), article.appendChild(article_new));
                  }
                }
              }
            }
          }
        }
      } else if (matchDomain('project-syndicate.org')) {
        func_post = function () {
          let hidden_images = document.querySelectorAll('img[src][new-cursrc]');
          for (let elem of hidden_images)
            (elem.src.startsWith('data:image/') && (elem.src = elem.getAttribute('new-cursrc')),
              (elem.style = 'width: 95%;'));
          let art_bodies = document.querySelectorAll('[itemprop="articleBody"]'),
            first = !0;
          for (let elem of art_bodies) {
            if (mobile) {
              let mobile_style = 'width: 90%; margin: 20px;';
              if (((elem.parentNode.style = mobile_style), first)) {
                let intro = document.querySelector('[itemprop="abstract"]');
                (intro && (intro.parentNode.parentNode.style = mobile_style),
                  document
                    .querySelectorAll('h1')
                    .forEach((e) => (e.style = mobile_style + ' font-size: 40px;')),
                  (first = !1));
              }
            }
            let pars = elem.querySelectorAll('div');
            pars.length && pars.length < 3 && header_nofix(article_sel, '', 'BPC > no archive-fix');
          }
          art_bodies.length || header_nofix(article_sel, '', 'BPC > no archive-fix');
        };
        let url = window.location.href,
          article_sel = 'main > article';
        getArchive(url, 'div.paywall--base', '', article_sel);
      } else if (matchDomain('puck.news')) {
        func_post = function () {
          let pars = document.querySelectorAll(
            article_sel + ' > div:not([id]) > div:not([style*="background-color:"]) > div'
          );
          pars.length && pars.length < 5 && header_nofix(article_sel, '', 'BPC > no archive-fix');
        };
        let url = window.location.href,
          article_sel = 'article';
        (getArchive(url, 'div[data-puck-paywall]', '', article_sel),
          hideDOMStyle('div#paywall-modal, div.paywall-gradient'));
        let pw_banner = document.querySelector(
          'div[data-dd="paywall"]:not(.hidden) > div > div.max-w-paywall-body'
        );
        pw_banner && removeDOMElement(pw_banner.parentNode);
        let overlay = document.querySelector('body.paywall-active');
        overlay && overlay.classList.remove('paywall-active');
      } else if (matchDomain('reuters.com'))
        hideDOMStyle('div[data-testid="ResponsiveAdSlot"], div[data-testid="Dianomi"]');
      else if (matchDomain('reviewjournal.com')) hideDOMStyle('div.ads-insert, div.rj-ads-wrapper');
      else if (matchDomain('rotowire.com')) {
        let paywall = document.querySelector(
          'div.pw-content, div.article__body > div[class^="img_"]'
        );
        (paywall &&
          (paywall.removeAttribute('class'),
          hideDOMStyle('div.article-preview-fader, div.paywall-full')),
          document
            .querySelectorAll('img.lozad[data-src]:not([src])')
            .forEach((e) => (e.src = e.getAttribute('data-src'))));
      } else if (matchDomain('rugbypass.com')) {
        if (window.location.pathname.match(/^\/plus\/\w/)) {
          let paywall = document.querySelector('.premium-fold-bottom');
          if (paywall) {
            (paywall.classList.remove('premium-fold-bottom'),
              removeDOMElement(document.querySelector('.plus-article-offer')));
            let fade = document.querySelector('.fade');
            fade && fade.classList.remove('fade');
          }
          let lazy_images = document.querySelectorAll('figure > img.lazy[data-src]:not([src])');
          for (let elem of lazy_images)
            ((elem.src = elem.getAttribute('data-src')), elem.removeAttribute('class'));
        }
      } else if (matchDomain('scientificamerican.com'))
        setCookie('article_meter', '', 'scientificamerican.com', '/', 0);
      else if (matchDomain('scholastic.com')) {
        let paywall = document.querySelector('div.paywallModalElement');
        if (paywall) {
          removeDOMElement(paywall, document.querySelector('div.modal-backdrop'));
          let body_modal = document.querySelector('body.modal-open');
          body_modal && body_modal.removeAttribute('class');
        }
      } else if (matchDomain('science.org')) {
        removeDOMElement(document.querySelector('div.alert-read-limit'));
        let overlay = document.querySelector('body.alert-read-limit__overlay');
        overlay && overlay.classList.remove('alert-read-limit__overlay');
      } else if (matchDomain('scmp.com'))
        (window.setTimeout(function () {
          if (matchDomain('amp.scmp.com')) {
            amp_unhide_subscr_section(
              'div.ad-banner, div.advert-fly-carpet-container, div.inline-advert'
            );
            let amp_images = document.querySelectorAll('section.article-body amp-img[src]');
            for (let elem of amp_images) {
              let img = document.createElement('img');
              ((img.src = elem.getAttribute('src')),
                (img.alt = elem.getAttribute('alt')),
                (img.style = 'width: 90%;'),
                elem.parentNode.replaceChild(img, elem));
            }
            removeDOMElement(...document.querySelectorAll('div[id^="default-meter-page-views"]'));
          } else {
            let paywall = document.querySelectorAll(
              'div[data-qa="GenericArticle-PaywallContainer"], div.js-reading-0-percent-completion-tracker'
            );
            if (paywall.length) {
              removeDOMElement(...paywall);
              let article = document.querySelector(
                'section[data-qa="ContentBody-ContentBodyContainer"]'
              );
              if (article) {
                let json_script = document.querySelector('script#__NEXT_DATA__');
                if (json_script)
                  try {
                    let json = JSON.parse(json_script.text);
                    if (json && json.props.pageProps.payload.data.article.body.json) {
                      let pars = json.props.pageProps.payload.data.article.body.json;
                      pars && pars.length && (article.innerHTML = '');
                      for (let par of pars) {
                        let elem = document.createElement('p');
                        if (
                          (window.location.pathname.startsWith('/magazines/') &&
                            (elem.style = 'margin: 20px 0px;'),
                          ['p', 'h3'].includes(par.type))
                        ) {
                          for (let sub_elem of par.children)
                            if ('text' === sub_elem.type)
                              sub_elem.data &&
                                elem.appendChild(document.createTextNode(sub_elem.data));
                            else if (['a', 'em', 'span', 'strong'].includes(sub_elem.type)) {
                              let first_child = sub_elem.children && sub_elem.children[0];
                              if (
                                (sub_elem.children &&
                                  sub_elem.children.length > 1 &&
                                  (first_child = {
                                    type: 'text',
                                    data: sub_elem.children
                                      .map((x) => x.data || x.children[0].data)
                                      .join(''),
                                  }),
                                first_child && 'text' === first_child.type && first_child.data)
                              ) {
                                let a_link = document.createElement('span');
                                (sub_elem.attribs && sub_elem.attribs.href
                                  ? ((a_link = document.createElement('a')),
                                    (a_link.style = 'text-decoration: underline;'),
                                    (a_link.href = sub_elem.attribs.href),
                                    matchUrlDomain(
                                      window.location.hostname,
                                      sub_elem.attribs.href
                                    ) || (a_link.target = '_blank'))
                                  : 'em' === sub_elem.type
                                    ? (a_link.style = 'font-style: italic;')
                                    : 'strong' === sub_elem.type &&
                                      (a_link.style = 'font-weight: bold;'),
                                  (a_link.innerText = first_child.data),
                                  elem.appendChild(a_link));
                              }
                            } else if ('img' === sub_elem.type && sub_elem.attribs) {
                              let attribs = sub_elem.attribs;
                              attribs.src &&
                                (elem = makeFigure(
                                  attribs.src,
                                  attribs.title,
                                  { alt: attribs.alt, style: 'width: 100%;' },
                                  { style: 'font-size: 80%;' }
                                ));
                            }
                        } else if ('div' === par.type) {
                          if (par.attribs)
                            if ('image-inline-container' === par.attribs.class) {
                              if (par.children && par.children[0]) {
                                let attribs = par.children[0].attribs;
                                attribs.src &&
                                  (elem = makeFigure(
                                    attribs.src,
                                    attribs.title,
                                    { alt: attribs.alt, style: 'width: 100%;' },
                                    { style: 'font-size: 80%;' }
                                  ));
                              }
                            } else if (
                              par.attribs.class.match(/(methode-html|oembed|video)-wrapper/) &&
                              par.children
                            ) {
                              function addIframe(par_children) {
                                for (let sub_elem of par_children)
                                  if (
                                    (sub_elem.children && addIframe(sub_elem.children),
                                    'iframe' === sub_elem.type)
                                  ) {
                                    let attribs = sub_elem.attribs;
                                    if (attribs.src) {
                                      let figure = document.createElement('figure'),
                                        iframe = document.createElement('iframe');
                                      if (
                                        ((iframe.src = attribs.src),
                                        attribs.width && attribs.height)
                                      )
                                        if (attribs.width.includes('%'))
                                          ((iframe.width = attribs.width),
                                            (iframe.height = attribs.height));
                                        else {
                                          let ratio = attribs.width / article.offsetWidth;
                                          ((iframe.width = attribs.width / ratio),
                                            (iframe.height = attribs.height / ratio));
                                        }
                                      else
                                        attribs.style &&
                                          ((iframe.style = attribs.style),
                                          attribs.height && (iframe.height = attribs.height));
                                      if ((figure.appendChild(iframe), attribs.title)) {
                                        let iframe_title = document.createElement('figcaption');
                                        ((iframe_title.innerText = attribs.title),
                                          (iframe_title.style = 'font-size: 80%;'),
                                          figure.appendChild(iframe_title));
                                      }
                                      elem.appendChild(figure);
                                    }
                                  }
                              }
                              addIframe(par.children);
                            } else par.attribs.class;
                        } else
                          [
                            'blockquote-quote',
                            'inline-ad-slot',
                            'inline-plus-widget',
                            'track-viewed-percentage',
                          ].includes(par.type);
                        elem.hasChildNodes() && article.appendChild(elem);
                      }
                    }
                  } catch (err) {}
              }
            }
          }
        }, 0),
          hideDOMStyle('div[data-qa*="AdSlot"], div.adblock-message'));
      else if (matchDomain('seattletimes.com'))
        hideDOMStyle('div.top-ad-wrapper, div.ad, div.native-ad-article');
      else if (matchDomain('sfstandard.com'))
        (setCookie('sfs-experiment-bucketing', '', 'sfstandard.com', '/', 0),
          hideDOMStyle('div.sticky, div#__next > div[style="display:inline"]'));
      else if (matchDomain(['businesstimes.com.sg', 'straitstimes.com'])) {
        if (matchDomain('straitstimes.com')) {
          let art_pars = document.querySelectorAll('div.storyline-wrapper > p');
          window.location.pathname.startsWith('/opinion/') ||
          window.location.search.startsWith('?rel=plus')
            ? art_pars.length &&
              art_pars.length < 5 &&
              ((func_post = function () {
                header_nofix('main', '', 'BPC > no archive-fix');
              }),
              getArchive(window.location.href, 'div.paywall', '', 'main'))
            : pageContains('div.headline-stack p', 'For subscribers') &&
              art_pars.length &&
              art_pars.length < 5 &&
              (window.location.href = window.location.pathname + '?rel=plus');
        } else if (matchDomain('businesstimes.com.sg')) {
          let article = document.querySelector('div.body-content > div[class]');
          if (article) {
            let par_hidden = article.querySelectorAll('p.hidden');
            if (par_hidden.length) {
              par_hidden[0].parentNode.removeAttribute('class');
              for (let elem of par_hidden) elem.classList.remove('hidden');
              let fade = article.querySelector('p[class*="bg-gradient-to-t"]');
              fade && (fade.className = par_hidden[0].className);
            }
          }
        }
        hideDOMStyle(
          'div.ads, div[id^="dfp-ad-"], div.cx_paywall_placeholder, div[data-testid="cas-block-component"]'
        );
      } else if (matchDomain('slate.com'))
        hideDOMStyle('.slate-roadblock, section[class*="-ad"], div.slate-ad');
      else if (matchDomain('slideshare.net')) {
        window.localStorage.clear();
        let limit_overlay = document.querySelector('.limit-overlay');
        limit_overlay && limit_overlay.classList.remove('limit-overlay');
      } else if (matchDomain('sloanreview.mit.edu'))
        window.location.pathname.startsWith('/article/') &&
          window.setTimeout(function () {
            let pars = document.querySelectorAll('div.article-content > p');
            pars.length && pars.length < 7 && refreshCurrentTab();
          }, 1e3);
      else if (matchDomain('sofrep.com')) {
        setCookie('sofrep_news_ids', '', 'sofrep.com', '/', 0);
        let paywall = document.querySelector('div.paywall');
        (paywall &&
          (paywall.removeAttribute('class'),
          removeDOMElement(document.querySelector('div.non-paywall')),
          waitDOMElement('div#paywall_wrap', 'DIV', (node) => removeDOMElement(node.parentNode))),
          removeDOMElement(...document.querySelectorAll('#scrollerCTA, #botCta')));
      } else if (matchDomain('spglobal.com')) setCookie('count', '', 'spglobal.com', '/', 0);
      else if (matchDomain('standardmedia.co.ke')) {
        let paywall = document.querySelector('div.spw-wrap');
        if (paywall) {
          removeDOMElement(paywall);
          let article = document.querySelector('div.content');
          if (article) {
            let json_script = getArticleJsonScript();
            if (json_script)
              try {
                let json = JSON.parse(json_script.text.replace(/[\r\n]/g, ''));
                if (json) {
                  let json_text = parseHtmlEntities(breakText(json.articleBody))
                    .replace(/\.\s{2}/g, '.<br><br>')
                    .replace(/[\r\n]/g, '<br>');
                  if (json_text) {
                    let figcaption = document.querySelector('figcaption');
                    if (figcaption) {
                      let file_regex = /\[File, Standard\]?/;
                      json_text = json_text.match(file_regex)
                        ? json_text.split(file_regex)[1]
                        : json_text
                            .replace(/\s{2,}/g, ' ')
                            .replace(figcaption.innerText.replace(/\s{2,}/g, ' '), '');
                    }
                    let article_new = new DOMParser()
                      .parseFromString('<p>' + json_text + '</p>', 'text/html')
                      .querySelector('p');
                    (removeDOMElement(
                      ...article.querySelectorAll('p:not([class]), div > .card-text')
                    ),
                      article.appendChild(article_new));
                  }
                }
              } catch (err) {}
          }
        }
        hideDOMStyle('div.ad');
      } else if (matchDomain('staradvertiser.com')) {
        let paywall = document.querySelectorAll('div.fade');
        if (paywall.length) {
          removeDOMElement(...paywall);
          let div_hidden = document.querySelector('div#hsa-paywall-content[style]');
          div_hidden && div_hidden.removeAttribute('style');
          let noscroll = document.querySelector('body.overflow-hidden');
          noscroll && noscroll.classList.remove('overflow-hidden');
        }
        hideDOMStyle('div.promo-container, div.teads-inread');
      } else if (matchDomain('startribune.com')) hideDOMStyle('div[data-testid$="-ad"]');
      else if (matchDomain('statnews.com'))
        (getArchive(
          window.location.href,
          'div.restricted-content-breaker',
          '',
          '.the-content',
          '',
          'main section, div[style*="counter-reset:"]'
        ),
          hideDOMStyle('div.header-ad-wrap, div[class*="dfp-ad"]'));
      else if (matchDomain('stereogum.com')) hideDOMStyle('div.adthrive-ad');
      else if (matchDomain('stocknews.com')) {
        removeDOMElement(document.querySelector('div#hideme'));
        let blurmes = document.querySelectorAll('div[id^="blurme"]');
        for (let i = 0; i < blurmes.length; i++) blurmes[i].setAttribute('id', 'blurmenot' + i);
      } else if (matchDomain('stratfor.com'))
        getArchive(
          window.location.href,
          'div#paywallCTAContainer:not(:empty)',
          '',
          'main',
          '',
          'main',
          'h1'
        );
      else if (matchDomain('study.com')) {
        let faded_content = document.querySelector('div.faded-content');
        faded_content && faded_content.removeAttribute('class');
        let div_hidden = document.querySelector('div.hidden[ng-non-bindable]');
        (div_hidden && div_hidden.removeAttribute('class'),
          removeDOMElement(...document.querySelectorAll('div.article-cutoff-div')));
      } else if (matchDomain('swarajyamag.com'))
        if (window.location.pathname.startsWith('/amp/')) {
          let amp_images = document.querySelectorAll('amp-img[src*=".jpg"]');
          for (let elem of amp_images) {
            let img_new = document.createElement('img');
            ((img_new.src = elem.getAttribute('src').split('?')[0]),
              (img_new.alt = elem.getAttribute('alt')),
              elem.parentNode.replaceChild(img_new, elem));
          }
        } else
          window.setTimeout(function () {
            amp_redirect('div#hide-partial-content', { rm_attrib: 'id' });
          }, 1e3);
      else if (matchDomain('techinasia.com')) {
        let paywall_sel = 'div.paywall-content',
          paywall = document.querySelector(paywall_sel + ':not(.done)');
        if (paywall) {
          (paywall.classList.add('done'), hideDOMStyle(paywall_sel));
          let content_new = paywall.querySelector('div.content');
          if (content_new)
            if (content_new.querySelector('a.flourish-credit:not(img), h2:not(:has(~ p))')) {
              let url_xhr = window.location.href
                .replace('.com/', '.com/wp-json/techinasia/2.0/posts/')
                .replace('/visual-story/', '/');
              fetch(url_xhr).then((response) => {
                response.ok &&
                  response.json().then((json) => {
                    let json_text = json.posts[0].content;
                    if (
                      ((json_text = json_text
                        .replace(/width\=\"(\d){3,}\"/g, 'width="100%"')
                        .replace(/height\=\"(\d){3,}\"/g, 'height="100%"')),
                      json_text)
                    ) {
                      let content_new = new DOMParser()
                          .parseFromString(
                            '<div class="content">' + json_text + '</div>',
                            'text/html'
                          )
                          .querySelector('div.content'),
                        content = document.querySelector('div.content');
                      (content && (content_new.className = content.className),
                        paywall.before(content_new));
                    }
                  });
              });
            } else {
              let div = document.createElement('div');
              ((div.className = paywall.className.replace('paywall-content', '')),
                div.appendChild(content_new),
                paywall.before(div));
              let container = document.querySelector('div.col div > div.container');
              container && container.classList.remove('container');
            }
        }
        removeDOMElement(
          document.querySelector('div.splash-subscribe'),
          document.querySelector('div.paywall-hard')
        );
      } else if (matchDomain(['techtarget.com', 'computerweekly.com'])) {
        let paywall = document.querySelector('div.paywall');
        paywall &&
          (paywall.classList.remove('paywall'),
          removeDOMElement(
            ...document.querySelectorAll('p#firstP, div#inlineRegistrationWrapper')
          ));
      } else if (matchDomain('the-american-interest.com'))
        removeDOMElement(document.getElementById('article-counter'));
      else if (matchDomain('the-scientist.com')) {
        let paywall = document.querySelector('div.paywall');
        paywall &&
          (paywall.classList.remove('paywall'),
          removeDOMElement(
            document.querySelector('div.gated-fader'),
            document.querySelector('div#Modal')
          ));
      } else if (matchDomain('theamericanconservative.com')) {
        let paywall_sel = 'section.c-blog-post__body--locked';
        if (document.querySelector(paywall_sel))
          getJsonUrl(
            paywall_sel,
            { rm_class: 'c-blog-post__body--locked' },
            'div.c-blog-post__content',
            {
              art_append: 1,
              func_text: function (json_text) {
                if (json_text.includes('<p class="has-drop-cap">')) {
                  let split = json_text.split(/(<p class="has-drop-cap">)/);
                  json_text = split[1] + split[2];
                }
                return json_text;
              },
            }
          );
        else {
          let img_dark = document.querySelector('div.c-hero-article__image-img.o-image');
          img_dark && img_dark.removeAttribute('class');
        }
        removeDOMElement(document.querySelector('div#emailsub-modal'));
        let noscroll = document.querySelector('body.modal-open');
        noscroll && noscroll.classList.remove('modal-open');
      } else if (matchDomain('theamericanscholar.org'))
        getJsonUrl('div.ssagk-form', '', 'div.post_summary');
      else if (matchDomain('theatlantic.com'))
        hideDOMStyle('aside#paywall, div[class^="LostInventoryMessage_"]');
      else if (matchDomain('thebanner.com'))
        hideDOMStyle('div.article-body__inline-ad, div#leaderboard-ad');
      else if (matchDomain('thebulletin.org'))
        getJsonUrl('div.article--cropped', '', 'div#body-copy', { art_append: 1 });
      else if (matchDomain('thedailybeast.com')) {
        let paywall = document.querySelector('div.Body__paywall-container');
        if (paywall) {
          removeDOMElement(paywall);
          let json_script = document.querySelector('script[displayName="initialState"]');
          if (json_script) {
            let json_str = json_script.text.substring(json_script.textContent.indexOf('{'));
            try {
              let json = JSON.parse(json_str);
              if (json.body) {
                let pars = json.body.sections,
                  cards = json.body.cards;
                if (pars) {
                  let mobile_doc = document.querySelector('div.Mobiledoc');
                  if (mobile_doc) {
                    let mobile_doc_text = mobile_doc.innerText.replace(/(\r|\n)/g, '');
                    for (let elem of pars) {
                      let par_elem = '';
                      if (1 === elem[0]) {
                        if ('p' === elem[1]) {
                          let par = '';
                          for (let part of elem[2]) par += part[3];
                          par &&
                            !mobile_doc_text.includes(par) &&
                            ((par_elem = document.createElement('p')), (par_elem.innerText = par));
                        }
                      } else if (10 === elem[0] && cards && cards[elem[1]]) {
                        let card = cards[elem[1]];
                        if ('pt-image' === card[0]) {
                          par_elem = document.createElement('p');
                          let par_fig = makeFigure(
                            card[1].url,
                            card[1].title + ' ' + card[1].credit
                          );
                          par_elem.appendChild(par_fig);
                        } else if ('pt-fancy-links-card' === card[0]) {
                          par_elem = document.createElement('p');
                          let par_link = document.createElement('a');
                          ((par_link.href = card[1].links),
                            (par_link.innerText = card[1].linksData[0].long_headline),
                            par_elem.appendChild(par_link));
                        }
                      }
                      par_elem && mobile_doc.appendChild(par_elem);
                    }
                  }
                }
              }
            } catch (err) {}
          }
        }
        let ads = document.querySelectorAll('div > div.tdb-ads-block');
        for (let ad of ads) hideDOMElement(ad.parentNode);
      } else if (matchDomain('thediplomat.com')) {
        if (matchDomain('magazine.thediplomat.com')) {
          let article = document.querySelector('article > section.h-96');
          if (article) {
            article.classList.remove('h-96');
            let art_body = article.querySelector('div.prose');
            if (art_body) {
              let art_img_src,
                art_img_match,
                art_img = article.parentNode.querySelector('figure > picture > img[src]');
              if (art_img) {
                art_img_src = art_img.getAttribute('src');
                let match = art_img.getAttribute('src').match(/\/media\/\d+\/(\w+)\.\w+/);
                match && (art_img_match = match[1]);
              }
              let url = window.location.href.split(/[#\?]/)[0];
              fetch(url).then((response) => {
                response.ok &&
                  response.text().then((html) => {
                    if (html.includes('<script>window.__remixContext.streamController.enqueue("'))
                      try {
                        let source = (
                            html
                              .split('<script>window.__remixContext.streamController.enqueue("')[1]
                              .split(/\](\\n)?"\);<\/script>/)[0] + ']'
                          )
                            .replace(/\\"/g, '"')
                            .replace(/\\\\"/g, '\\"'),
                          json = JSON.parse(source);
                        if (json) {
                          let body_index = json.indexOf('body') + 1;
                          if (body_index) {
                            art_body.innerHTML = '';
                            let parser = new DOMParser();
                            for (let i = body_index; i < json.length; i++) {
                              let par = json[i];
                              if ('string' == typeof par)
                                if (par.startsWith('<p>')) {
                                  let pars = parser
                                    .parseFromString('<div>' + par + '</div>', 'text/html')
                                    .querySelector('div')
                                    .querySelectorAll('p');
                                  for (let par of pars) art_body.appendChild(par);
                                } else if (
                                  art_img_match &&
                                  par !== art_img_match &&
                                  par.includes(art_img_match.slice(0, -1))
                                ) {
                                  let caption;
                                  for (let n = i - 5; n < i; n++) {
                                    let item = json[n];
                                    'string' != typeof item ||
                                      item.startsWith('<p>') ||
                                      ['caption', 'credit_name', 'file', 'image'].includes(item) ||
                                      item.match(/\d+_\d+/) ||
                                      (caption = caption ? caption + '\r\n' + item : item);
                                  }
                                  let figure = makeFigure(
                                    art_img_src.replace(art_img_match, par),
                                    caption
                                  );
                                  ((figure.style = 'margin: 20px 0px;'),
                                    art_body.appendChild(figure));
                                }
                            }
                          }
                        }
                      } catch (err) {}
                  });
              });
            }
            hideDOMStyle('aside.bg-gradient-to-b', 2);
            let banner = document.querySelector(
              'section > a[href^="https://thediplomat.com/subscriptions"]'
            );
            banner && removeDOMElement(banner.parentNode);
          }
        }
        hideDOMStyle('aside.td-ad-container--labeled, div[data-actirise]');
      } else if (matchDomain('thedispatch.com')) {
        setCookie('xbc', '', 'thedispatch.com', '/', 0);
        let paywall_sel = 'section.piano-paywall--active',
          paywall = document.querySelector(paywall_sel);
        if (paywall) {
          if (window.location.pathname.startsWith('/podcast/')) {
            let tablist = document.querySelector('div[role="tablist"]');
            if (tablist) {
              let yoast_script = document.querySelector(
                'script.yoast-schema-graph[type="application/ld+json"]'
              );
              if (yoast_script)
                try {
                  let json = JSON.parse(yoast_script.text);
                  if (json['@graph']) {
                    let audio_src_obj = json['@graph'].find(
                      (x) =>
                        x['@type'] &&
                        'PodcastEpisode' === x['@type'] &&
                        x.associatedMedia &&
                        x.associatedMedia.contentUrl
                    );
                    if (audio_src_obj) {
                      let audio_new = document.createElement('audio');
                      ((audio_new.src = audio_src_obj.associatedMedia.contentUrl),
                        audio_new.setAttribute('controls', ''),
                        tablist.before(audio_new));
                    }
                  }
                } catch (err) {}
            }
          } else if (window.location.pathname.startsWith('/next-250/')) {
            removeDOMElement(paywall);
            let article = document.querySelector('section#article-body');
            if (article) {
              let article_id_dom = document.querySelector('article[data-post-id]');
              if (article_id_dom) {
                let article_id = article_id_dom.getAttribute('data-post-id');
                article_id &&
                  fetch(
                    'https://thedispatch.com/wp-json/dispatch/v1/post-content/' + article_id
                  ).then((response) => {
                    response.ok &&
                      response.text().then((html) => {
                        try {
                          if (html) {
                            let article_new = new DOMParser()
                              .parseFromString(html, 'text/html')
                              .querySelectorAll('div');
                            for (let par of article_new) article.appendChild(par);
                            hideDOMStyle('div.pull-quote-contents', 2);
                          }
                        } catch (err) {}
                      });
                  });
              }
            }
          } else
            window.location.pathname.startsWith('/newsletter/') &&
              header_nofix('section#article-body > div > p', paywall_sel);
          document
            .querySelectorAll('[style*="overflow: hidden"]:is(html, body)')
            .forEach((e) => e.removeAttribute('style'));
        }
        hideDOMStyle('div.tp-container-inner, section.piano-fixed-bottom-overlay');
      } else if (matchDomain('theglobeandmail.com')) {
        let lazy_images = document.querySelectorAll('img[src^="data:image/"][data-src]');
        for (let elem of lazy_images) elem.src = elem.getAttribute('data-src');
        let fusion_script = document.querySelector('script#fusion-metadata');
        if (fusion_script && fusion_script.text.includes('Fusion.globalContent='))
          try {
            let json = JSON.parse(
              fusion_script.text.split('Fusion.globalContent=')[1].split(';Fusion.')[0]
            );
            window.setTimeout(function () {
              let audio_tts = document.querySelector(
                'div > div#audio-panel > p[class^="ArticlePlayer__LoginCopy"]'
              );
              if (audio_tts) {
                let audio_json = getNestedKeys(json, 'additional_properties.tts_audio');
                if (audio_json) {
                  let audio_src = audio_json.url_en_M || audio_json.url_en_F;
                  if (audio_src) {
                    let audio = document.createElement('audio');
                    ((audio.src = audio_src),
                      audio.setAttribute('controls', ''),
                      audio_tts.parentNode.parentNode.replaceChild(audio, audio_tts.parentNode));
                  }
                }
              }
              let video = document.querySelector('div > div.c-video:empty');
              if (video) {
                let streams = findKeyJson(json, 'streams');
                if (streams && streams.length) {
                  let stream = streams.pop();
                  if (stream.url) {
                    let video_new = document.createElement('video');
                    ((video_new.src = stream.url),
                      (video_new.style = 'width: 90%; margin: 0px 15px;'),
                      video_new.setAttribute('controls', ''),
                      video.parentNode.replaceChild(video_new, video));
                  }
                }
              }
            }, 2e3);
          } catch (err) {}
        hideDOMStyle('div.c-ad--base');
      } else if (matchDomain('thehill.com')) {
        if (!window.location.pathname.match(/^\/(video|hilltv)\//)) {
          let lead_video = document.querySelector(
            'div.wp-block-nexstar-video > div#lead-media-1.nexstar-video'
          );
          if (lead_video) {
            let json_script = getArticleJsonScript();
            if (json_script) {
              let json = JSON.parse(json_script.text);
              if (json && json.image && json.image.url) {
                let lead_fig = makeFigure(
                  json.image.url,
                  parseHtmlEntities(json.image.name || ''),
                  '',
                  { class: 'caption' }
                );
                (mobile && (lead_fig.style = 'width:90%; margin: 0px 20px;'),
                  lead_video.parentNode.parentNode.replaceChild(lead_fig, lead_video.parentNode));
              }
            }
          }
        }
        hideDOMStyle(
          'div[data-civicscience-widget]:empty, aside.ad-unit, iframe#instaread_iframe:not([src])'
        );
      } else if (matchDomain(['thehindu.com', 'thehindubusinessline.com'])) {
        if (window.location.pathname.endsWith('/amp/'))
          (addStyle('body {scrollbar-width: thin;}'),
            hideDOMStyle('div[class^="height"], div[class^="advt"], div[id^="piano"]'));
        else {
          if (matchDomain('www.thehindu.com')) {
            let paywall_sel = 'div#artmeterpv';
            document.querySelector(paywall_sel) &&
              !document.querySelector(paywall_sel + ' ~ div.also-read') &&
              amp_redirect(paywall_sel);
          } else if (matchDomain('frontline.thehindu.com')) {
            let paywall = document.querySelector('body.articlepaywall');
            (paywall && paywall.classList.remove('articlepaywall'),
              hideDOMStyle('div[class^="article-standard_piano-inline"]', 2));
          }
          hideDOMStyle(
            'div.ad, div.article-ad, div.dfp-ad, div#paywallbox, div[id^="piano-art-"], #test'
          );
        }
        let read_more = document.querySelector('div.paywallbox-btn > button');
        read_more && read_more.click();
      } else if (matchDomain(['thejuggernaut.com', 'jgnt.co'])) {
        let paywall = pageContains(
          'div.font-mono',
          /(Read this article and many more by subscribing today|Join today to read the full story)/
        );
        if (paywall.length) {
          removeDOMElement(paywall[0].parentNode);
          let json_script = document.querySelector('script#__NEXT_DATA__');
          if (json_script)
            try {
              let json = JSON.parse(json_script.text);
              if (json && getNestedKeys(json, 'props.pageProps.post.fields')) {
                let url_next = json.query.slug;
                url_next && !window.location.pathname.includes(url_next) && refreshCurrentTab();
                let fields = json.props.pageProps.post.fields,
                  pars = fields.fullText
                    ? fields.fullText.content
                    : fields.textEssay.fields.body.content;
                window.setTimeout(function () {
                  let article = document.querySelector('div[class*="opacity-"]');
                  if (article) {
                    ((article.innerHTML = ''),
                      article.removeAttribute('class'),
                      mobile && (article.style = 'margin: 0px 20px;'));
                    let fade = document.querySelectorAll('div.bg-gradient-to-b');
                    for (let elem of fade) elem.removeAttribute('class');
                    removeDOMElement(document.querySelector('div#headlessui-portal-root'));
                    let par_first = !0;
                    function attach_text(sub_item, elem) {
                      if (sub_item.value) {
                        let sub_elem = document.createElement('span');
                        if (
                          ((sub_elem.innerText = sub_item.value),
                          sub_item.marks && sub_item.marks.length)
                        ) {
                          let style = '';
                          for (let mark of sub_item.marks)
                            'bold' === mark.type
                              ? (style += 'font-weight: bold;')
                              : 'italic' === mark.type
                                ? (style += 'font-style: italic;')
                                : 'underline' === mark.type &&
                                  (style += 'text-decoration: underline;');
                          sub_elem.style = style;
                        }
                        elem.appendChild(sub_elem);
                      }
                    }
                    function attach_hyperlink(sub_item, elem) {
                      if (
                        sub_item.content &&
                        sub_item.content[0] &&
                        sub_item.content[0].value &&
                        sub_item.data &&
                        sub_item.data.uri
                      ) {
                        let sub_elem = document.createElement('a');
                        ((sub_elem.href = sub_item.data.uri),
                          (sub_elem.innerText = sub_item.content[0].value),
                          matchUrlDomain(['thejuggernaut.com', 'jgnt.co'], sub_item.data.uri) ||
                            (sub_elem.target = '_blank'),
                          (sub_elem.style = 'text-decoration: underline;'),
                          elem.appendChild(sub_elem));
                      }
                    }
                    function attach_paragraph(par, elem) {
                      if (par.content && par.content.length) {
                        let span_elem = document.createElement('span');
                        for (let item of par.content)
                          'text' === item.nodeType
                            ? attach_text(item, span_elem)
                            : 'hyperlink' === item.nodeType && attach_hyperlink(item, span_elem);
                        elem.appendChild(span_elem);
                      }
                    }
                    for (let par of pars) {
                      let elem = document.createElement('p');
                      if (par.nodeType.match(/^(paragraph|heading-\d)$/))
                        attach_paragraph(par, elem);
                      else if (['blockquote'].includes(par.nodeType)) {
                        if (par.content && par.content.length)
                          for (let item of par.content)
                            'paragraph' === item.nodeType &&
                              ((elem.style = 'margin: 0px 20px; font-style: italic;'),
                              attach_paragraph(item, elem));
                      } else if ('hr' === par.nodeType)
                        elem.appendChild(document.createElement('hr'));
                      else if ('embedded-asset-block' === par.nodeType) {
                        if (par_first) par_first = !1;
                        else if (
                          par.data &&
                          par.data.target &&
                          par.data.target.fields &&
                          par.data.target.fields.file &&
                          par.data.target.fields.file.url
                        ) {
                          let figure = makeFigure(
                            par.data.target.fields.file.url,
                            par.data.target.fields.description
                          );
                          elem.appendChild(figure);
                        }
                      } else if (
                        'unordered-list' === par.nodeType &&
                        par.content &&
                        par.content.length
                      ) {
                        let ul = document.createElement('ul');
                        for (let item of par.content)
                          if ('list-item' === item.nodeType && item.content)
                            for (let sub_item_par of item.content)
                              if ('paragraph' === sub_item_par.nodeType) {
                                let li = document.createElement('li');
                                (attach_paragraph(sub_item_par, li), ul.appendChild(li));
                              }
                        elem.appendChild(ul);
                      }
                      elem.hasChildNodes() &&
                        (article.appendChild(document.createElement('br')),
                        article.appendChild(elem));
                    }
                  }
                }, 1e3);
              } else refreshCurrentTab();
            } catch (err) {}
        }
      } else if (matchDomain('thelampmagazine.com')) {
        let paywall = document.querySelector('div.paywall-gradient');
        paywall &&
          (paywall.removeAttribute('class'),
          removeDOMElement(document.querySelector('section.p-8')));
        let login = document.querySelectorAll('a.js-login-modal-trigger');
        for (let elem of login) {
          elem.removeAttribute('class');
          let url_search = '/search?q=' + elem.innerText.replace(/\s/g, '+');
          ((elem.href = url_search), (elem.onclick = (x) => (window.location.href = url_search)));
        }
      } else if (matchDomain('thelogic.co')) setCookie('firstarticle', '', 'thelogic.co', '/', 0);
      else if (matchDomain('thenewatlantis.com')) {
        let article_gated = document.querySelector('.article-gated');
        article_gated && article_gated.classList.remove('article-gated');
      } else if (matchDomain('thenewsminute.com')) {
        let paywall = document.querySelector('div#paywall-banner');
        if (paywall) {
          removeDOMElement(paywall);
          let fade = document.querySelector('div[class^="paywall-story-styles-"]');
          fade && fade.removeAttribute('class');
          let json_script = getArticleJsonScript();
          if (json_script) {
            let json = JSON.parse(json_script.text);
            if (json) {
              let json_text = breakText(
                  parseHtmlEntities(json.articleBody.replace(/\.\./g, '.\r\n\r\n'))
                ),
                article = document.querySelector('div.arr--story-page-card-wrapper');
              if (json_text && article) {
                article.innerHTML = '';
                let article_new = document.createElement('p');
                ((article_new.innerText = json_text), article.appendChild(article_new));
              }
            }
          }
        }
      } else if (matchDomain('thepointmag.com'))
        (setCookie('monthly_history', '', 'thepointmag.com', '/', 0),
          removeDOMElement(...document.querySelectorAll('div.overlay, div#tpopup-')));
      else if (matchDomain('thequint.com'))
        window.setTimeout(function () {
          let lock = document.querySelector('div > img[alt^="lock"]');
          if (lock) {
            lock.removeAttribute('alt');
            let paywall = document.querySelector('div#paywall-widget');
            if (paywall) {
              removeDOMElement(paywall);
              let article = document.querySelector('div.story-element');
              if (article) {
                let article_new = getArticleQuintype();
                article_new &&
                  article.parentNode &&
                  article.parentNode.replaceChild(article_new, article);
              }
            }
            let body_hidden = document.querySelector('div#story-body-wrapper');
            (body_hidden &&
              (body_hidden.removeAttribute('class'), body_hidden.removeAttribute('style')),
              waitDOMAttribute(
                'div#story-body-wrapper',
                'DIV',
                'style',
                function (node) {
                  node.removeAttribute('style');
                },
                !0
              ));
          }
        }, 4e3);
      else if (matchDomain('theweek.com')) {
        removeDOMElement(document.querySelector('div.kiosq-main-layer'));
        let locker = document.querySelector('div.paywall-locker');
        locker && locker.classList.remove('paywall-locker');
      } else if (matchDomain('thewrap.com'))
        (setCookie('blaize_session', '', 'thewrap.com', '/', 0),
          getJsonUrl('div#zephr-payment-form-root', '', 'div.entry-content', { art_append: 1 }),
          removeDOMElement(
            document.querySelector(
              'div.content-area div[style*="background-image: linear-gradient"]'
            )
          ));
      else if (matchDomain('timeshighereducation.com')) {
        let paywall = document.querySelector('div.paywall-active');
        if (paywall) {
          removeDOMElement(paywall);
          let fade = document.querySelectorAll('div.paywall-fade');
          for (let elem of fade) elem.classList.remove('paywall-fade');
        }
        let hidden_images = document.querySelectorAll('img.b-lazy[src^="data:image/"][data-src]');
        for (let hidden_image of hidden_images)
          (hidden_image.setAttribute('src', hidden_image.getAttribute('data-src')),
            hidden_image.classList.remove('b-lazy'),
            hidden_image.parentElement.classList.remove('media--loading'));
        hideDOMStyle('div[data-ad-page], section.block-the-dfp');
      } else if (matchDomain('epaper.indiatimes.com')) {
        if (
          (removeDOMElement(document.querySelector('div.epaperBlockerWrap')),
          window.location.pathname.startsWith('/english-news-paper-today-toi-print-edition/'))
        ) {
          let paywall = document.querySelector('section#blocker');
          if (paywall) {
            removeDOMElement(paywall, document.querySelector('section#fq'));
            let json_script = getArticleJsonScript();
            if (json_script) {
              let json = JSON.parse(json_script.text);
              if (json) {
                let json_text = json.articleBody,
                  content = document.querySelector('section[type="synopsis"]');
                if (json_text && content) {
                  let article_new = document.createElement('p');
                  ((article_new.innerText = breakText(json_text)),
                    (content.innerHTML = ''),
                    addStyle('[type="synopsis"]::after {background: none !important;}'),
                    content.appendChild(article_new));
                }
              }
            }
          }
        }
      } else if (matchDomain('tomshardware.com')) {
        let paywall = document.querySelector('div.paywall-locker');
        (paywall && paywall.classList.remove('paywall-locker'),
          hideDOMStyle('div#kiosq-app-paywall-js, div.dfp-leaderboard-container'));
      } else if (
        matchDomain([
          'dn.no',
          'europower.no',
          'fiskeribladet.no',
          'hydrogeninsight.com',
          'intrafish.com',
          'intrafish.no',
          'kystens.no',
          'rechargenews.com',
          'tradewindsnews.com',
          'upstreamonline.com',
        ])
      ) {
        if (matchDomain('tradewindsnews.com') && window.location.pathname.startsWith('/markets/')) {
          removeDOMElement(document.querySelector('iframe[src]'));
          let overflow = document.querySelector('body[style]');
          overflow && overflow.removeAttribute('style');
          let blurred = document.querySelector('body > div[style]');
          blurred && blurred.removeAttribute('style');
        } else
          window.setTimeout(function () {
            let paywall = document.querySelector('div#sub-paywall-container');
            if (paywall)
              if (
                matchDomain('dn.no') &&
                window.location.pathname.match(/^\/(d2|magasinet|smak)\//)
              ) {
                removeDOMElement(paywall);
                let article = document.querySelector('article');
                if (article) {
                  let url = window.location.href;
                  if ((article.firstChild.before(googleSearchToolLink(url)), mobile)) {
                    let body = document.querySelector('body');
                    body && (body.style.width = '80%');
                  }
                }
              } else
                window.location.pathname.match(/^\/(e-avis|editions)/)
                  ? (removeDOMElement(paywall), header_nofix('section.wrapper'))
                  : (function (node) {
                      let article = document.querySelector('div#dn-content'),
                        json_script = document.querySelector('script#__NUXT_DATA__');
                      if (article && json_script) {
                        removeDOMElement(node.parentNode);
                        try {
                          let pars = JSON.parse(json_script.text),
                            article_id_index =
                              pars.indexOf('global-article') + 1 || pars.indexOf('dn-article') + 1;
                          if (!article_id_index) return void refreshCurrentTab();
                          {
                            let article_id = pars[article_id_index];
                            if (article_id && !window.location.pathname.endsWith(article_id))
                              return void refreshCurrentTab();
                          }
                          ((article.innerHTML = ''), article.classList.remove('shadow'));
                          let img_first = !0,
                            parser = new DOMParser();
                          for (let par of pars) {
                            let elem;
                            if (par && par.type) {
                              let type = pars[par.type];
                              if (['text', 'subhead'].includes(type)) {
                                if (par.html || par.value) {
                                  let json_text = pars[par.html || par.value];
                                  ((elem = parser
                                    .parseFromString(
                                      '<p class="dn-text">' + json_text + '</p>',
                                      'text/html'
                                    )
                                    .querySelector('p')),
                                    par.value && (elem.style = 'font-weight: bold;'));
                                }
                              } else if ('picture' === type)
                                if (img_first) img_first = !1;
                                else {
                                  let caption_text = pars[par.caption];
                                  (par.credit && (caption_text += ' (' + pars[par.credit] + ')'),
                                    (elem = makeFigure(pars[par.src], caption_text)),
                                    (elem.className = 'dn-image'));
                                }
                              else if ('embed' === type) {
                                if (((elem = document.createElement('div')), par.value)) {
                                  let video = parser
                                    .parseFromString(
                                      '<div>' + pars[par.value] + '</div>',
                                      'text/html'
                                    )
                                    .querySelector('div');
                                  elem.appendChild(video);
                                }
                              } else if ('factbox' === type) {
                                if (
                                  ((elem = document.createElement('div')),
                                  (elem.className = 'dn-factbox'),
                                  addStyle('div.dn-factbox p {margin: 20px 0px;}'),
                                  par.title)
                                ) {
                                  let title = document.createElement('p');
                                  ((title.innerText = pars[par.title]),
                                    (title.className = 'dn-text'),
                                    (title.style = 'font-weight: bold;'),
                                    elem.appendChild(title));
                                }
                                if (par.html) {
                                  let box = parser
                                    .parseFromString(
                                      '<div>' + pars[par.html] + '</div>',
                                      'text/html'
                                    )
                                    .querySelector('div');
                                  (box
                                    .querySelectorAll('p')
                                    .forEach((e) => (e.className = 'dn-text')),
                                    elem.appendChild(box));
                                }
                              } else if ('summary' === type) {
                                if (par.bullets) {
                                  elem = document.createElement('ul');
                                  for (let item of pars[par.bullets]) {
                                    let li = document.createElement('li');
                                    ((li.innerText = pars[item]), elem.appendChild(li));
                                  }
                                }
                              } else if ('news' === type && par.title && par.url) {
                                elem = document.createElement('p');
                                let sub_elem = document.createElement('a');
                                ((sub_elem.href = pars[par.url]),
                                  (sub_elem.innerText = 'Related: ' + pars[par.title]),
                                  (sub_elem.style = 'font-weight: bold;'),
                                  elem.appendChild(sub_elem));
                              } else if (
                                ![
                                  'ad',
                                  'ad_group',
                                  'adobetarget',
                                  'author',
                                  'break',
                                  'Emne',
                                  'job',
                                  'Location',
                                  'ma_frame',
                                  'news',
                                  'Organisasjon',
                                  'Organisation',
                                  'Organization',
                                  'promobox',
                                  'Person',
                                  'Personer',
                                  'quote',
                                  'readpeak',
                                  'Region',
                                  'Regions',
                                  'related',
                                  'Sector',
                                  'Sectors',
                                  'Selskap',
                                  'Shiptype',
                                  'Sted',
                                  'Steder',
                                  'Topic',
                                  'ukens_stilling',
                                  'video',
                                ].includes(type)
                              )
                                for (let item in par);
                              elem && article.appendChild(elem);
                            }
                          }
                        } catch (err) {}
                      }
                    })(paywall);
          }, 1e3);
        hideDOMStyle('div.top-ad-placeholder, div.dn-dfp-container');
      } else if (
        matchDomain([
          'architecturaldigest.com',
          'bonappetit.com',
          'cntraveler.com',
          'epicurious.com',
          'gq.com',
          'newyorker.com',
          'vanityfair.com',
          'vogue.co.uk',
          'vogue.com',
          'wired.com',
        ])
      ) {
        let maps = document.querySelectorAll('div.map_wrapper');
        for (let elem of maps) elem.style.visibility = 'visible';
        hideDOMStyle(
          'div.ad, aside.paywall-bar, div[class^="MessageBannerWrapper-"], div.ad-stickyhero, div.ad_wrapper'
        );
      } else if (matchDomain(['daytondailynews.com', 'journal-news.com', 'springfieldnewssun.com']))
        removeDOMElement(...document.querySelectorAll('div.arc_ad'));
      else if (
        (domain = matchDomain([
          '360dx.com',
          'adage.com',
          'autonews.com',
          'chicagobusiness.com',
          'crainscleveland.com',
          'crainsdetroit.com',
          'crainsgrandrapids.com',
          'crainsnewyork.com',
          'european-rubber-journal.com',
          'genomeweb.com',
          'modernhealthcare.com',
          'pionline.com',
          'plasticsnews.com',
          'precisionmedicineonline.com',
          'rubbernews.com',
          'tirebusiness.com',
          'utech-polyurethane.com',
        ]))
      ) {
        if (matchDomain('european-rubber-journal.com')) {
          let paywall = document.querySelector('div.article-overlay');
          if (paywall) {
            removeDOMElement(paywall, document.querySelector('div.gradient'));
            let truncated = document.querySelector('div.truncated');
            truncated && truncated.classList.remove('truncated');
          }
        } else {
          let fusion_script = document.querySelector('script#fusion-metadata');
          if (fusion_script && fusion_script.text.includes('Fusion.globalContent=')) {
            let paywall = document.querySelector('div#piano-paywall-container'),
              article = document.querySelector('article.b-article-body');
            if (paywall && article) {
              removeDOMElement(paywall);
              try {
                let json = JSON.parse(
                  fusion_script.text.split('Fusion.globalContent=')[1].split(';Fusion.')[0]
                );
                if (json) {
                  article.innerHTML = '';
                  let parser = new DOMParser(),
                    pars = json.content_elements;
                  for (let par of pars) {
                    let par_new;
                    if (['header', 'text'].includes(par.type))
                      par.content &&
                        (par_new = parser
                          .parseFromString(
                            '<p class="c-paragraph">' + par.content + '</p>',
                            'text/html'
                          )
                          .querySelector('p'));
                    else if ('image' === par.type) {
                      if (par.url) {
                        let caption_text = par.caption;
                        (par.credits &&
                          par.credits.affiliation &&
                          par.credits.affiliation[0] &&
                          par.credits.affiliation[0].name &&
                          (caption_text += ' (' + par.credits.affiliation[0].name + ')'),
                          (par_new = makeFigure(par.url, caption_text)));
                      }
                    } else if ('raw_html' === par.type)
                      par_new = parser
                        .parseFromString(
                          '<div>' + parseHtmlEntities(par.content) + '</div>',
                          'text/html'
                        )
                        .querySelector('div');
                    else if (par.raw_oembed) {
                      if (par.raw_oembed._id) {
                        par_new = document.createElement('p');
                        let par_link = document.createElement('a');
                        ((par_link.href = par_link.innerText =
                          par.raw_oembed._id.replace(/\/$/, '')),
                          (par_link.target = '_blank'),
                          par_new.appendChild(par_link));
                      }
                    } else if ('video' === par.type) {
                      if (par.canonical_url && domain.startsWith(par.canonical_website)) {
                        par_new = document.createElement('p');
                        let par_link = document.createElement('a');
                        ((par_link.href = par_link.innerText =
                          'https://www.' + domain + par.canonical_url.replace(/\/$/, '')),
                          (par_link.target = '_blank'),
                          par_new.appendChild(par_link));
                      }
                    } else if ('list' === par.type) {
                      if (par.items) {
                        par_new = document.createElement('ul');
                        for (let item of par.items) {
                          let li = document.createElement('li'),
                            span = parser
                              .parseFromString('<span>' + item.content + '</span>', 'text/html')
                              .querySelector('span');
                          (li.appendChild(span), par_new.appendChild(li));
                        }
                      }
                    } else ['custom_embed'].includes(par.type);
                    par_new && article.appendChild(par_new);
                  }
                }
              } catch (err) {}
            }
          }
        }
        hideDOMStyle('div.footer__ads-footer');
      } else if (
        matchDomain([
          'bicycling.com',
          'cosmopolitan.com',
          'countryliving.com',
          'delish.com',
          'elle.com',
          'elledecor.com',
          'esquire.com',
          'goodhousekeeping.com',
          'harpersbazaar.com',
          'housebeautiful.com',
          'menshealth.com',
          'oprahdaily.com',
          'popularmechanics.com',
          'prevention.com',
          'roadandtrack.com',
          'runnersworld.com',
          'townandcountrymag.com',
          'womenshealthmag.com',
        ])
      ) {
        let audio_tts = document.querySelector('audio[data-media-manager-id^="tts-"][style]');
        if (audio_tts) {
          let json_script = document.querySelector('script#__NEXT_DATA__');
          if (json_script)
            try {
              let audio_src = findKeyJson(JSON.parse(json_script.text), 'speech_file_url');
              audio_src && (audio_tts.removeAttribute('style'), (audio_tts.src = audio_src));
            } catch (err) {}
        }
      } else if (
        matchDomain(['curbed.com', 'grubstreet.com', 'nymag.com', 'thecut.com', 'vulture.com'])
      )
        (document
          .querySelectorAll('iframe[data-src]:not([src])')
          .forEach((e) => (e.src = e.getAttribute('data-src'))),
          hideDOMStyle('div.m-ad, section.ad-splash, aside.ad_static'));
      else if (
        matchDomain([
          'backpacker.com',
          'betamtb.com',
          'betternutrition.com',
          'cleaneatingmag.com',
          'climbing.com',
          'outsideonline.com',
          'oxygenmag.com',
          'skimag.com',
          'trailrunnermag.com',
          'triathlete.com',
          'vegetariantimes.com',
          'womensrunning.com',
          'yogajournal.com',
        ])
      )
        hideDOMStyle('div.js-ad');
      else if (
        matchDomain([
          'billboard.com',
          'rollingstone.com',
          'sourcingjournal.com',
          'sportico.com',
          'variety.com',
          'wwd.com',
        ])
      )
        hideDOMStyle('div.admz');
      else if (
        matchDomain([
          'baltimoresun.com',
          'capitalgazette.com',
          'chicagotribune.com',
          'courant.com',
          'dailypress.com',
          'mcall.com',
          'nydailynews.com',
          'orlandosentinel.com',
          'pilotonline.com',
          'sun-sentinel.com',
        ])
      )
        (getJsonUrl('div.paywall-container', '', 'div.body-copy', { art_class: 'body-copy' }),
          hideDOMStyle('div.dfp-ad, div.bx-slab, div.sbn-widget-body, div#mobile-adhesion'));
      else if (matchDomain('vice.com'))
        (getJsonUrl('section.unlock-container', '', 'div.entry-content'),
          hideDOMStyle('div.lngtd-dyn-ph, div.lngtd-ad-wrapper-banner'));
      else if (matchDomain('vikatan.com'))
        (window.setTimeout(function () {
          let paywall = document.querySelector('div[class^="styles-m__paywall-main-wrapper_"]');
          if (paywall)
            if ((removeDOMElement(paywall), window.location.pathname.startsWith('/amp/')))
              ampToHtml();
            else {
              let json_script = getArticleJsonScript();
              if (json_script)
                try {
                  let json = JSON.parse(json_script.text);
                  if (json) {
                    let article = document.querySelector('div.story-element');
                    if (article) {
                      let parser = new DOMParser(),
                        json_text = parseHtmlEntities(json.articleBody).replace(/>\.</g, '><br><'),
                        content_new = parser
                          .parseFromString('<div>' + json_text + '</div>', 'text/html')
                          .querySelector('div'),
                        content = document.querySelector('div.story-element > div');
                      content
                        ? content.parentNode.replaceChild(content_new, content)
                        : article.appendChild(content_new);
                    }
                  }
                } catch (err) {}
              let story_hidden = document.querySelector(
                'div[class^="styles-m__story-card-wrapper_"]'
              );
              story_hidden && story_hidden.removeAttribute('class');
            }
        }, 1500),
          hideDOMStyle(
            'div[class^="styles-m__popup-wrapper-adb_"], div[class^="styles-m__subscribe-banner-wrapper_"], div[id^="ps-floating-container-"], div[id^="div-ad-desk-"], div[data-type="_mgwidget"]'
          ));
      else if (matchDomain('voguebusiness.com')) {
        setCookie('userId', '', 'voguebusiness.com', '/', 0);
        let article = document.querySelector('article');
        if (article) {
          let pars = article.querySelectorAll('p:not([class]), p.paywall');
          if (pars.length < 5) {
            removeDOMElement(...pars);
            let filter = /^window\.__PRELOADED_STATE__\s?=\s?/,
              json_script = getSourceJsonScript(filter, '[type]:not([src])');
            if (json_script)
              try {
                let json = JSON.parse(json_script.text.split(filter)[1].split('};')[0] + '}'),
                  body = document.querySelector('div.body__inner-container');
                if (body) {
                  let pars = json.transformed.article.body;
                  function makeElem(elem, par_elem) {
                    if (Array.isArray(elem) && elem.length) {
                      let elem_new,
                        item = elem[0];
                      if ('string' == typeof item) {
                        if (['p', 'h2', 'em', 'strong'].includes(item))
                          ((elem_new = document.createElement(item)),
                            par_elem.appendChild(elem_new),
                            elem.shift(),
                            makeElem(elem, elem_new));
                        else if ('a' === item && elem.length > 2) {
                          elem_new = document.createElement('a');
                          let a_data = elem[1];
                          ((elem_new.href = a_data.href),
                            a_data.isExternal && (elem_new.target = '_blank'),
                            makeElem(elem.slice(2), elem_new),
                            par_elem.appendChild(elem_new));
                        } else if (
                          'inline-embed' === item ||
                          !(
                            [
                              'ad',
                              'cm-unit',
                              'inline-newsletter',
                              'journey-inline-newsletter',
                              'native-ad',
                            ].includes(item) ||
                            (item.length < 30 && item.includes('inline-embed'))
                          )
                        )
                          if ('inline-embed' === item) {
                            let img_data = elem[1];
                            if (
                              img_data &&
                              'image' === img_data.type &&
                              img_data.props &&
                              img_data.props.image &&
                              img_data.props.image.sources
                            ) {
                              let caption_text;
                              img_data.props.dangerousCaption &&
                                ((caption_text = img_data.props.dangerousCaption.replace(
                                  /<\/?\w+>/g,
                                  ''
                                )),
                                img_data.props.dangerousCredit &&
                                  (caption_text += ' ' + img_data.props.dangerousCredit));
                              let figure = makeFigure(
                                img_data.props.image.sources.lg.url,
                                caption_text
                              );
                              par_elem.appendChild(figure);
                            }
                          } else
                            ((elem_new = document.createTextNode(item)),
                              par_elem.appendChild(elem_new),
                              elem.shift(),
                              makeElem(elem, par_elem));
                      } else
                        Array.isArray(item)
                          ? ['a', 'em', 'strong'].includes(item[0]) &&
                            (makeElem(item, par_elem), elem.shift(), makeElem(elem, par_elem))
                          : 'object' == typeof item &&
                            (item.class, elem.shift(), makeElem(elem, par_elem));
                    } else
                      'string' != typeof elem ||
                        ['div'].includes(elem) ||
                        par_elem.appendChild(document.createTextNode(elem));
                  }
                  for (let par of pars) makeElem(par, body);
                }
              } catch (err) {}
          }
        }
      } else if (matchDomain('vox.com')) hideDOMStyle('div[data-concert]');
      else if (matchDomain('warontherocks.com'))
        window.location.pathname.startsWith('/episode/')
          ? header_nofix(
              'div.single-content-section',
              'a[href ^= "https://warontherocks.com/subscribe"]'
            )
          : getJsonUrl(
              'a[href^="https://warontherocks.com/membership"]',
              '',
              'div.tw\\:container.tw\\:mb-20>div'
            );
      else if (matchDomain('washingtonexaminer.com')) hideDOMStyle('div.wex-inarticle-ad');
      else if (matchDomain('washingtonpost.com')) {
        let paywall_sel = 'div.meteredContent';
        if (
          document.querySelector(paywall_sel) &&
          !document.querySelector('div:not(.teaser-content) > div.article-body')
        ) {
          func_post = function () {
            if (mobile) {
              let article = document.querySelector(article_src_sel.split(' ').pop());
              if (article) {
                let topper = document.querySelector('div#default-topper');
                if (topper) {
                  let art_width = topper.offsetWidth;
                  ((article.style.width = art_width),
                    article
                      .querySelectorAll('div[style*="max-width:"]')
                      .forEach((e) => (e.style = 'margin: 0px 20px; width: ' + 0.9 * art_width)));
                }
              }
            }
          };
          let url = window.location.href,
            article_src_sel = 'article > div > div[style*="grid-column-end"]';
          getArchive(
            url,
            paywall_sel,
            { rm_class: 'meteredContent' },
            'div.teaser-content',
            '',
            article_src_sel
          );
        }
        hideDOMStyle(
          'wp-ad-wrapper, div[data-qa$="-ad"], div[data-component="Ad"], div[data-qa="outbrain"], div.PJLV-ifmxCWD-css'
        );
      } else if (matchDomain('winnipegfreepress.com'))
        hideDOMStyle('.billboard-ad-space, .ad, .article-ad, .fixed-sky');
      else if (matchDomain('wsj.com')) {
        if (window.location.pathname.startsWith('/livecoverage/'))
          window.setTimeout(function () {
            fix_dowjones_live();
          }, 1500);
        else {
          let paywall_sel = '.snippet-promotion, div[id*="-snippet-overlay"]',
            paywall = document.querySelector(paywall_sel);
          if (paywall) {
            let url = window.location.href,
              article_sel = 'article section';
            if (
              (paywall.querySelector('a[href^="https://wsjpro.com/"]') && (article_sel = 'article'),
              document.querySelector(article_sel))
            ) {
              let video_sel = 'div[data-type="video"]',
                video = document.querySelector(video_sel),
                schema_script = document.querySelector('script#articleschema');
              ((func_post = function () {
                if (
                  (document.querySelectorAll(article_sel + ' div[style*="font-family:"]').length <
                    5 && header_nofix(article_sel, '', 'BPC > no archive-fix'),
                  video)
                ) {
                  let video_new = document.querySelector(video_sel);
                  video_new &&
                    video_new.parentNode &&
                    video_new.parentNode.replaceChild(video, video_new);
                }
                let inline_videos = document.querySelectorAll('div[id^="video"]');
                for (let inline_video of inline_videos) {
                  let video_id = inline_video.id.replace('video', '');
                  if (video_id && schema_script)
                    try {
                      let video_data = JSON.parse(schema_script.text).find(
                        (x) => 'VideoObject' === x['@type'] && x.embedUrl.includes(video_id)
                      );
                      if (video_data && inline_video.parentNode) {
                        let elem = document.createElement('iframe');
                        ((elem.src = video_data.embedUrl),
                          (elem.style =
                            'width: ' +
                            inline_video.offsetWidth +
                            'px; height: ' +
                            (3 * inline_video.offsetWidth) / 4 +
                            'px;'),
                          inline_video.parentNode.replaceChild(elem, inline_video));
                      }
                    } catch (err) {}
                }
                if (mobile) {
                  let inline_images = document.querySelectorAll(
                    'div[style] > figure > picture > img'
                  );
                  for (let elem of inline_images)
                    ((elem.style = 'width: 100%;'),
                      elem.removeAttribute('height'),
                      elem.removeAttribute('width'),
                      elem.parentNode.removeAttribute('style'),
                      elem.parentNode.parentNode.parentNode.removeAttribute('style'));
                  let inline_data = document.querySelectorAll('div[data-layout="inline"][style]');
                  for (let elem of inline_data) elem.removeAttribute('style');
                }
                removeDOMElement(document.querySelector('div#cx-what-to-read-next'));
                let inline_wrappers = document.querySelectorAll(
                  'div[style*="background-position"] > div[id^="wrapper-INLINEIMM_"]'
                );
                for (let elem of inline_wrappers) removeDOMElement(elem.parentNode);
              }),
                getArchive(url, paywall_sel, '', article_sel));
            }
          }
        }
        hideDOMStyle(
          'div.wsj-ad, div.adWrapper, div.css-xgokil-Box, div#cx-article-cover-overlay, div#dianomi-module'
        );
      } else if (matchDomain('ynet.co.il')) {
        let paywall = document.querySelector('div.paywall-wrapper');
        (paywall &&
          (removeDOMElement(paywall),
          (window.location.href = window.location.pathname + '?rel=plus')),
          removeDOMElement(document.querySelector('#yitwall')));
      } else if (matchDomain(['businessdailyafrica.com', 'nation.africa'])) {
        let paywall = document.querySelectorAll('div.modal, [id*="wall"], section.wall-guard');
        (paywall.length &&
          (removeDOMElement(...paywall),
          (func_post = function () {
            let div_hidden = document.querySelectorAll('div.article-page .nmgp');
            for (let elem of div_hidden) elem.classList.remove('nmgp');
            let page_hidden = document.querySelector('div.article-page .hidden');
            page_hidden && page_hidden.classList.remove('hidden');
            let lazy_images = document.querySelectorAll('img.lazy-img[data-srcset]:not([src])');
            for (let elem of lazy_images)
              ((elem.src = elem.getAttribute('data-srcset').split(',').pop().split(' ')[0]),
                elem.classList.remove('lazy-img'),
                (elem.style = 'margin: 0px 20px'));
            let videos = document.querySelectorAll(
              'iframe.lazy-iframe_iframe[data-src]:not([src])'
            );
            for (let elem of videos)
              ((elem.src = elem.getAttribute('data-src')), elem.removeAttribute('class'));
          }),
          replaceDomElementExt(window.location.href, !1, !1, 'div.blk-txt')),
          hideDOMStyle('div.banner, div.spinner'));
      } else if (
        (domain = matchDomain([
          'azcentral.com',
          'cincinnati.com',
          'commercialappeal.com',
          'courier-journal.com',
          'democratandchronicle.com',
          'desmoinesregister.com',
          'detroitnews.com',
          'dispatch.com',
          'freep.com',
          'indystar.com',
          'jacksonville.com',
          'jsonline.com',
          'knoxnews.com',
          'news-press.com',
          'northjersey.com',
          'oklahoman.com',
          'palmbeachpost.com',
          'tennessean.com',
        ])) ||
        document.querySelector(
          'head > link[href*="/gannett_net.js"], footer a[href^="https://www.gannett.com"]'
        )
      ) {
        if (
          (domain || (domain = window.location.hostname.replace(/^(www|amp|eu)\./, '')),
          setCookie('firefly_akamai_meter', '', domain, '/', 0),
          window.location.pathname.endsWith('/restricted/') &&
            window.location.search.startsWith('?return='))
        ) {
          let url = decodeURIComponent(window.location.href.split('?return=')[1]),
            paywall = pageContains('div.message', 'This content is only available to subscribers.');
          if (paywall.length) {
            removeDOMElement(...paywall);
            let article = document.querySelector('article');
            article && article.firstChild.before(archiveLink(url));
          }
        }
        hideDOMStyle('aside[aria-label="advertisement"]');
      } else if (
        matchDomain([
          'ctinsider.com',
          'ctpost.com',
          'expressnews.com',
          'houstonchronicle.com',
          'nhregister.com',
          'sfchronicle.com',
          'statesman.com',
          'timesunion.com',
        ]) ||
        document.querySelector('head > script[src*="/treg.hearstnp.com/"]')
      )
        (addStyle('body {overflow: visible!important;}'),
          hideDOMStyle(
            'div[data-block-type="ad"], div[data-widget-host="revcontent"], div.b-gray300, div#mod-target-div'
          ));
      else if (
        matchDomain(
          [
            'buffalonews.com',
            'journalnow.com',
            'journalstar.com',
            'madison.com',
            'nwitimes.com',
            'omaha.com',
            'richmond.com',
            'stltoday.com',
            'tucson.com',
            'tulsaworld.com',
          ].concat([
            'niagarafallsreview.ca',
            'stcatharinesstandard.ca',
            'thepeterboroughexaminer.com',
            'therecord.com',
            'thespec.com',
            'thestar.com',
            'wellandtribune.ca',
          ])
        ) ||
        document.querySelector('head > meta[name="tncms-access-version"]')
      ) {
        function unscramble(t) {
          for (var n = '', i = 0, r = t.length; i < r; i++) {
            var s = t.charCodeAt(i);
            n += s >= 33 && s <= 126 ? String.fromCharCode(33 + ((s - 33 + 47) % 94)) : t.charAt(i);
          }
          return n;
        }
        removeDOMElement(document.querySelector('div.subscriber-offers'));
        let subscriber_only = document.querySelectorAll('div.subscriber-only');
        for (let elem of subscriber_only)
          (elem.classList.contains('encrypted-content') &&
            (elem.innerHTML = unscramble(elem.textContent)),
            elem.removeAttribute('style'),
            elem.removeAttribute('class'));
        (removeDOMElement(
          ...document.querySelectorAll('div.subscription-required, div.redacted-overlay')
        ),
          hideDOMStyle(
            'div.tnt-ads-container, div[class*="adLabelWrapper"], div.globalHeaderBillboard'
          ));
      } else if (
        matchDomain([
          'bnd.com',
          'charlotteobserver.com',
          'elnuevoherald.com',
          'fresnobee.com',
          'kansas.com',
          'kansascity.com',
          'kentucky.com',
          'mcclatchydc.com',
          'miamiherald.com',
          'newsobserver.com',
          'sacbee.com',
          'star-telegram.com',
          'thestate.com',
          'tri-cityherald.com',
        ]) ||
        document.querySelector(
          'head > link[href^="https://mcclatchy-d.openx.net"], footer a[href^="https://www.mcclatchy.com/privacy-policy"]'
        )
      ) {
        if (window.location.hostname.startsWith('amp.')) {
          amp_unhide_subscr_section();
          let subscriptions_action = document.querySelector(
            'div[subscriptions-action][subscriptions-display="NOT data.hasError"]'
          );
          subscriptions_action && subscriptions_action.removeAttribute('subscriptions-action');
          let art_cropped = document.querySelector('div.article-body.cropped');
          (art_cropped && art_cropped.classList.remove('cropped'),
            removeDOMElement(
              document.querySelector('div#subscriber-exclusive-tag'),
              ...document.querySelectorAll('amp-connatix-player, amp-iframe.trinity-player')
            ));
          let amp_images = document.querySelectorAll('amp-img[srcset]:not([src])');
          for (let elem of amp_images) {
            let img = document.createElement('img');
            ((img.src = elem.getAttribute('srcset').split(' ')[0]),
              (img.alt = elem.getAttribute('alt')),
              (img.style = 'width: 100%;'),
              elem.parentNode.replaceChild(img, elem));
          }
        } else
          window.setTimeout(function () {
            let paywall = document.querySelector('p#yzwall');
            if (paywall) {
              removeDOMElement(paywall);
              let pars_hidden = document.querySelectorAll('.yzfade, .yzarret');
              for (let elem of pars_hidden) elem.removeAttribute('class');
            }
          }, 1e3);
        hideDOMStyle(
          'div[data-type="ad"], div.vf-promo, div#ymovrly, div.ad__slot--wrapper, connatix-video-ad, px-sellwild'
        );
      } else if (
        matchDomain([
          'bostonherald.com',
          'denverpost.com',
          'eastbaytimes.com',
          'mercurynews.com',
          'ocregister.com',
          'pressenterprise.com',
          'sandiegouniontribune.com',
          'twincities.com',
        ]) ||
        document.querySelector(
          'head > link[rel="stylesheet"][id^="dfm-accuweather-"], footer li > a[href^="https://www.medianewsgroup.com"]'
        )
      )
        if (window.location.pathname.endsWith('/amp/'))
          amp_unhide_subscr_section('div.ampWrapperInside, div#paywall');
        else {
          let paywall_sel = '#server-paywall',
            paywall = document.querySelector(paywall_sel),
            article_sel = 'div.body-copy',
            article = document.querySelector(article_sel);
          if (paywall && article) {
            func_post = function () {
              let slideshow = article.querySelector('div.article-slideshow');
              if (slideshow) {
                slideshow.removeAttribute('class');
                let image_wrappers = slideshow.querySelectorAll('div.image-wrapper');
                for (let elem of image_wrappers)
                  (elem.removeAttribute('class'), (elem.style = 'margin: 20px 0px;'));
                hideDOMStyle('div.mng-gallery-information-container, button.icon-close', 2);
              }
              iframe && article.appendChild(iframe);
            };
            let iframe = article.querySelector('iframe');
            getJsonUrl(paywall_sel, '', article_sel, { art_append: 1, art_class: 'body-copy' });
          }
          hideDOMStyle('div.dfp-ad');
        }
      else if (document.querySelector('head > script[src*=".axate.io/"]')) {
        let premium = document.querySelector('.premium, div[class*="-premium"]');
        premium && premium.removeAttribute('class');
      } else if (
        document.querySelector('head > meta[property][content^="https://cdn.forumcomm.com/"]')
      )
        hideDOMStyle('div.GoogleDfpAd-Content');
      else if (
        document.querySelector(
          'head > script[src*=".postmedia.digital/"], head > meta[content*=".postmedia.digital/"]'
        )
      )
        hideDOMStyle('div.ad__section-border, div[id^="tbl_"], div.js-widget-content, section.ad');
      else if (document.querySelector('head > link[href$=".wallkit.net"]')) {
        let paywall = document.querySelector('div.wkwp-paywall');
        (paywall && paywall.removeAttribute('class'), hideDOMStyle('div.wkwp-paywall-block'));
      }
      (ads_hide(), leaky_paywall_unhide_disable || leaky_paywall_unhide());
    }, 1e3);
  }
  function fix_dowjones_live() {
    let paywall = document.querySelector('div#cx-lc-snippet');
    if (paywall) {
      removeDOMElement(paywall);
      let json_script = getArticleJsonScript();
      if (json_script)
        try {
          let json = JSON.parse(json_script.text);
          if (json) {
            let article = document.querySelector('div[data-id="StreamBody_index_MainContainer"]');
            if (article) {
              let pars = json[0].liveBlogUpdate;
              for (let par of pars)
                if (par.headline && par.articleBody) {
                  let headline = document.createElement('p');
                  ((headline.innerText = par.headline), (headline.style = 'font-weight: bold;'));
                  let author = document.createElement('a');
                  par.author &&
                    par.author.sameAs &&
                    ((author.href = par.author.sameAs[0]), (author.innerText = par.author.name));
                  let date = document.createElement('p');
                  par.dateModified &&
                    par.datePublished &&
                    (date.innerText =
                      'Updated ' +
                      new Intl.DateTimeFormat('en-US', {
                        dateStyle: 'long',
                        timeStyle: 'short',
                        timeZone: 'America/New_York',
                      }).format(new Date(par.dateModified)) +
                      ' ET / Original ' +
                      new Intl.DateTimeFormat('en-US', {
                        dateStyle: 'long',
                        timeStyle: 'short',
                        timeZone: 'America/New_York',
                      }).format(new Date(par.datePublished)) +
                      ' ET');
                  let body = document.createElement('p');
                  ((body.innerText = par.articleBody),
                    article.after(
                      document.createElement('br'),
                      headline,
                      author,
                      date,
                      document.createElement('br'),
                      body
                    ));
                }
            }
          }
        } catch (err) {}
      let fade = document.querySelectorAll('div[class*="-CardWrapper"]');
      for (let elem of fade) elem.removeAttribute('class');
    }
  }
  function fix_dowjones_fetch(url_src, data, article) {
    try {
      if (data) {
        let json = JSON.parse(data);
        if (json && json.screens && json.screens[0] && json.screens[0].frames) {
          let par_class,
            pars = json.screens[0].frames,
            intro = article.querySelector('p[class*="Paragraph"], [data-type="hed"]');
          if (intro)
            if ('P' === intro.tagName) par_class = intro.className;
            else {
              let par_first = article.querySelector('p[class*="Paragraph"]');
              par_first && (par_class = par_first.className);
            }
          if (
            (par_class || addStyle('div.crawler p:not([class]) {margin: 20px 0px;}'),
            matchDomain('barrons.com'))
          ) {
            let style_fl = document.querySelector('head > style[data-emotion-css]');
            if (style_fl && style_fl.innerHTML.includes(':first-letter')) {
              let styles_fl = document.querySelectorAll(
                'head > style[data-emotion-css], head > style[data-emotion="emotion"]'
              );
              for (let style of styles_fl)
                style.innerHTML.includes(':first-letter') && removeDOMElement(style);
            }
            if (document.title.startsWith('Sign in or subscribe') && json.screens[0].metadata) {
              let metadata = json.screens[0].metadata;
              metadata.title && (document.title = metadata.title + " - Barrons's");
            }
          }
          let img_lead_url,
            body_first = !0,
            img_lead = document.querySelector(img_lead_sel);
          function addBodyText(elem, par) {
            if (par.body && par.body.text && 'Visit page' !== par.body.text)
              if (par.body.additions) {
                let items = par.body.additions.filter(
                  (x) =>
                    'link' === x.type && x.value && x.hasOwnProperty('rangeStart') && x.rangeLength
                );
                if (items.length) {
                  function addLink(item, body_part, start_index) {
                    item.rangeStart = item.rangeStart - start_index;
                    let start = body_part.substring(0, item.rangeStart),
                      middle = body_part.substring(
                        item.rangeStart,
                        item.rangeStart + item.rangeLength
                      ),
                      end = body_part.substring(item.rangeStart + item.rangeLength),
                      sub_elem = document.createElement('a');
                    ((sub_elem.href = item.value),
                      (sub_elem.innerText = middle),
                      (sub_elem.style = 'text-decoration: underline;'),
                      elem.append(
                        document.createTextNode(start.replace(/\s_/g, '')),
                        sub_elem,
                        document.createTextNode(end.replace(/\s_/g, ''))
                      ));
                  }
                  let start_index = (end_index = 0);
                  for (let item of items)
                    ((end_index = start_index + item.rangeStart + item.rangeLength),
                      addLink(item, par.body.text.substring(start_index, end_index), start_index),
                      (start_index = end_index));
                  end_index &&
                    elem.appendChild(
                      document.createTextNode(
                        par.body.text.substring(end_index).replace(/\s_/g, '')
                      )
                    );
                } else elem.innerText = par.body.text.replace(/\s_/g, '');
              } else elem.innerText = par.body.text.replace(/\s_/g, '');
          }
          (img_lead && (img_lead_url = img_lead.src.split('?')[0]), (article.innerHTML = ''));
          for (let par of pars) {
            let elem = document.createElement('p');
            if ((par_class && (elem.className = par_class), 'body' === par.type))
              par.body &&
                par.styleID.match(/(default|bodyFrame|article-body)/) &&
                (body_first && intro
                  ? ((elem = intro), (body_first = !1))
                  : addBodyText(elem, par));
            else if ('listelement' === par.type)
              par.body &&
                (elem.appendChild(document.createTextNode(' • ')), addBodyText(elem, par));
            else if ('image' === par.type) {
              if (
                par.image &&
                par.image.url &&
                (!img_lead_url || !par.image.url.startsWith(img_lead_url))
              ) {
                let caption =
                  (par.caption ? par.caption.text + ' - ' : '') +
                  (par.credit ? par.credit.text : '');
                ((elem = makeFigure(par.image.url, caption, {
                  style: 'width: 80%; margin: auto;',
                })),
                  (elem.style = 'margin: 20px 0px;'));
              }
            } else if (
              ['article', 'inlineArticle'].includes(par.type) &&
              par.title &&
              par.title.text &&
              par.shareUrl
            ) {
              let sub_elem = document.createElement('a');
              ((sub_elem.href = par.shareUrl),
                (sub_elem.innerText = par.title.text),
                (sub_elem.style = 'font-weight: bold;'),
                elem.appendChild(sub_elem));
            } else if ('dynamicinset' === par.type) {
              if (
                par.webview &&
                par.webview.value &&
                par.webview.value !== window.location.href.split('?')[0]
              ) {
                let iframe = document.createElement('iframe');
                ((iframe.src = par.webview.value),
                  (iframe.style = 'height: 600px; width: 100%; border: none;'),
                  elem.appendChild(iframe));
              }
            } else if ('screen' === par.type) {
              if (par.screen && par.screen.frames) {
                elem = document.createElement('p');
                let tr,
                  sub_elem = document.createElement('table'),
                  row_new = !0;
                for (let frame of par.screen.frames)
                  if (
                    (row_new && ((tr = document.createElement('tr')), (row_new = !1)), frame.body)
                  ) {
                    let td = document.createElement('td');
                    ((td.innerText = frame.body.text), tr.appendChild(td));
                  } else 'divider' === frame.type && (sub_elem.appendChild(tr), (row_new = !0));
                (sub_elem.appendChild(tr), elem.appendChild(sub_elem));
              }
            } else if ('video' === par.type) {
              if (par.url) {
                let video = document.createElement('video');
                ((video.src = par.url),
                  video.setAttribute('controls', ''),
                  (video.style = 'width: 100%;'),
                  elem.appendChild(video));
              }
            } else if ('youtube' === par.type) {
              if (par.videoId) {
                elem = document.createElement('p');
                let sub_elem = document.createElement('iframe');
                ((sub_elem.src = 'https://www.youtube.com/embed/' + par.videoId),
                  (sub_elem.style = 'width: 100%; height: 400px;'),
                  elem.appendChild(sub_elem));
              }
            } else if ('jwplayer' === par.type) {
              if (par.videos && par.videos[0] && par.videos[0].url)
                if (window.navigator.userAgent.toLowerCase().includes('chrome')) {
                  let video = document.createElement('video');
                  ((video.src = par.videos[0].url),
                    video.setAttribute('controls', ''),
                    (video.style = 'width: 100%;'),
                    elem.appendChild(video));
                } else {
                  let sub_elem = document.createElement('a');
                  ((sub_elem.href = sub_elem.innerText = par.videos[0].url),
                    elem.appendChild(sub_elem));
                }
            } else if ('audioplayer' === par.type) {
              if (par.media && par.media.contentUrl) {
                let audio_tts = document.createElement('audio');
                ((audio_tts.src = par.media.contentUrl),
                  audio_tts.setAttribute('controls', ''),
                  elem.appendChild(audio_tts));
              }
            } else
              [
                'ad',
                'authorBios',
                'byline',
                'caption',
                'columnistHeader',
                'divider',
                'inlineComment',
                'paywall-promotion',
                'pullquote',
                'pullquotebody',
                'scrollinggallery',
                'sectionTitle',
                'tagCloud',
                'tags',
                'title',
                'verticalContainer',
                'webview',
              ].includes(par.type);
            elem.hasChildNodes() && article.appendChild(elem);
          }
        }
      }
    } catch (err) {}
  }
})();
